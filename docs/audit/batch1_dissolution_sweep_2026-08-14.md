# Batch-1 dissolution sweep + batch review (2026-08-14, wf_0aaa7331-640)

## [0] STILL_OPEN
Untouched; batch-1 only shifted line numbers (placement split). `retain_child_live` (crates/sim/src/stub.rs:8212-8243) still validates only the payload routing key and stores `home` = the frame sender, passed as bare `*from` at the SignalDelta dispatch (stub.rs:2501-2508, comment ":2500 the sender NodeId is the return address for the down-lanes"). That `home` still targets both down-lanes: `home_of_active_child` (stub.rs:6811-6813) → `active_children` (stub.rs:6784-6806) → RealmCascade, and ChildSceneSet. Receivers still take no sender: `on_realm_observation` (8254), `on_realm_shape_observation` (8338), `on_child_scene_set` (8430, replaces `from_above.0` whole), `on_realm_cascade` (7301); `MsgClass::Saga` still dispatches to `on_directory_reply` without `from` (stub.rs:2452-2465 vs Control's `*from` at 2440). `ParentRealmNode` remains send-target-only (stub.rs:7033, 7063, 7136). No new ledger row: D-RLM-8 (DEFERRED.md:3316-3319) is still `record_realm_demands`-scoped; D-WORLD-1..9 / D-PLACE-1..6 (DEFERRED.md:3861-4023) never mention these lanes. Wire's contrary rule persists (crates/wire/src/intershard.rs:343, 833).

## [1] STILL_OPEN
Untouched. `git diff --stat HEAD` for crates/wire/src/intershard.rs and crates/wire/tests/intershard_closed.rs is EMPTY (only channels.rs is modified in vd-wire), so batch-1 (saga_runtime, flight, WorldRoster, vd-physics placement split) never reached this surface. Current tree: the blanket `InterShardFlow::Directory(_) | Saga(_) | SagaAck(_) | DirectoryReply(_) | FlushSource(_) | TransferAck(_) | ... => ReDriven` group still opens at crates/wire/src/intershard.rs:668, under the still-false doc claim "EXHAUSTIVE ... NO `_` wildcard anywhere" at intershard.rs:633-635. Exactly three nested tripwires remain — crates/wire/tests/intershard_closed.rs:463, :472, :485 — and the golden pin keeps its own `_ => ReDriven` fallback at intershard_closed.rs:663. The silent one is unchanged: `grep -rn "impl DirectoryReply" crates/` returns nothing. Ledger unchanged too: docs/design/DEFERRED.md:1626 still claims "wildcard-free, exhaustive at every nesting level"; no D-WORLD-* (DEFERRED.md:3861-3957) or D-PLACE-* (:3958-4026) row mentions tripwires, durability_class or DirectoryReply.

## [2] STILL_OPEN
Untouched. The exact sentence survives verbatim in the working tree: crates/bins/src/lib.rs:2019-2021 still says "the source keeps speaking for its departing occupant — telling its parent where they are". The lane it describes is still a tombstone with no producer (crates/wire/src/intershard.rs:272-282; only constructions are tests, crates/wire/src/intershard.rs:1481, crates/sim/src/stub.rs:19821). speaks_for (crates/sim/src/stub.rs:635) still has one production consumer, the local observer fold (stub.rs:7657), whose only up-lanes are the Empty self-report (stub.rs:7669-7679) and the one-bit ChildLive (stub.rs:7688-7695) — no pose crosses. The verification's three extra survivors also remain: crates/bins/src/bin/shard.rs:213, crates/sim/src/stub.rs:613, :628, plus stub.rs:7504 ("up-relays each local occupant's interest"). No ledger cure: grep of docs/design/DEFERRED.md for speaks_for/handoff_hold/residue returns zero; no D-WORLD-*/D-PLACE-* row mentions it.

## [4] STILL_OPEN
Untouched by batch-1. (Report section is line 105, the HR2 "adopt_transient_batch has no receiver-side frame conversion" finding; twins at :374, :401.) At HEAD+worktree the transient dispatch arm still discards to_realm via `..` (crates/sim/src/stub.rs:4548-4552) and returns before the durable conversion; adopt_transient_batch (stub.rs:5720-5748) still has no to_realm/RealmRegions/PlacementBook parameter and stores the wire pose verbatim — `let pose = item.pose.sanitized();` (stub.rs:5737) straight into OwnedTransients. The self-admitted KNOWN GAP comment survives verbatim (stub.rs:5647-5651). `git diff HEAD -- crates/sim/src/stub.rs` contains no hunk in that function. The SL4 rework only re-signatured the durable guard place_arriving_pose (stub.rs:4054, called :3761, :4574). No new ledger row covers it: D-PLACE-1..6 (DEFERRED.md:3958-4030) never name the transient adopt, and the stale DEFERRED.md:3558-3561 claim still cites the now-deleted rebind_pose_to_dest. Gate still blind (tests/tests/p3_transient.rs:100-101 siblings, no frame assertion).

## [5] STILL_OPEN
Untouched, and now worse. All four arms survive unmodified (`git status` clean on crates/wire/src/intershard.rs, version.rs, sim/saga.rs): FlushSource:958, CrossingRequest:1079, TransientCrossingRequest:1096, TransientCrossingGrant:1115. Still no terminal reader — on_flush_source (crates/sim/src/stub.rs:3956) never reads flush.to_parent and calls flush_pose_for_dest (:3973); emit_transient_batch destructures TransientStatus::Crossing with `..` (crates/sim/src/stub.rs:5631-5638). Every `.to_parent` read is a copy (saga_runtime.rs:1068/1637/1715, stub.rs:3357/5514) or a test assert (stub.rs:17766/17802/17845). AGGRAVATED: batch-1 DELETED rebind_pose_to_dest entirely (no `fn rebind_pose_to_dest` in the tree; DEFERRED.md:3964 confirms), yet intershard.rs:1076/1094/1113, version.rs:24, saga.rs:58, stub.rs:745/5314/17813 still name it as the live consumer. Ledger uncured: DEFERRED.md:3043 keeps the false "rides to every rebind_pose_to_dest call site" claim; D-WORLD-1 (:3863) restates to_parent as real machinery merely unproven.

## [6] PARTIALLY_DISSOLVED
Cured half: a new fixture genuinely varies the shard kind. `drive_swept_crossing_feature` builds its world via `Rig::with_config_and_kind(config(), kind)` (crates/sim/src/stub.rs:18116-18120) and `a_kepler_child_and_a_thrusting_child_cross_by_identical_code` (stub.rs:18152) runs that one body on `NodeKind::StubShard` (stub.rs:18177) and `NodeKind::Shard(planet)` (stub.rs:18179), asserting field-identical crossings. It is truthfully ledgered: DEFERRED.md:3979-3980 (D-PLACE-1 🟩) — "one fixture, two motions, two shard kinds".

Residue: the finding's actual target is untouched. `assert_feature_anywhere` (stub.rs:18201) still calls `drive_inward_crossing_feature`, which builds `Rig::new()` (stub.rs:18054) = `NodeKind::StubShard` (stub.rs:8747-8752) for both runs; the profiles remain decorative `.voxel()` asserts (stub.rs:18204-18209, 18217-18222). D-38's claim that it runs on a planet AND a station profile (DEFERRED.md:304-309, pin 332-334) is unchanged and still false, and capability.rs:13-17 still contradicts the 🟩.

## [7] STILL_OPEN
Untouched by batch-1. `region_level` still panics on a Ship: crates/sim/src/stub.rs:8174-8177 (`level_of(region.realm).expect("a hosted child region is a seed-lineage realm (never an entity-backed ship)")`), moved from :7946 but byte-equivalent, still called unconditionally on every direct child at stub.rs:6800, :7720, :7906, :14935. `level_of` still returns None for Ship (crates/core/src/worldgen.rs:87), the lineage coordinate still cannot name one (crates/core/src/realm_path.rs:39-46, six tags, no Ship), a Ship own-realm still cannot build a config (stub.rs:246) and boot still rejects it (crates/bins/src/lib.rs:1413-1422). The worldgen move into vd-physics preserved the semantics (crates/physics/src/worldgen.rs:290 silently `filter_map`s a Ship away). Ledger unchanged: `grep level_of docs/design/DEFERRED.md` = 0 hits; no D-WORLD-1..9 (DEFERRED.md:3861-3956) or D-PLACE-1..6 (:3958-4026) row names it; D-37 Slice 4 (:252-253) and D-39.4 (:367-368) are as the audit found them.

## [8] STILL_OPEN
Untouched by batch-1. The C-6c claim is byte-identical to HEAD, only shifted by the new rows: docs/design/DEFERRED.md:3405 still reads "Plus sim unit tests `stub::tests::{symmetric_recross_resolves_the_full_container_sequence_both_ways, escape_soi_lands_in_the_immediate_parent_not_a_skipped_ancestor}` assert the `container()` sequence". `git diff HEAD -- docs/design/DEFERRED.md | grep -c "^[+-].*symmetric_recross"` = 0. Neither test exists: `grep -rn "fn symmetric_recross|fn escape_soi" crates/ tests/` returns nothing; the only hits repo-wide are docs (DEFERRED.md:3405, rehome_one_mechanism.md:149,152, containment_realm_membership_design.md:418-419). No new row covers it: the 16 added rows are D-WORLD-1..9 and D-PLACE-1..6 (cluster shapes and placement split), none naming the deleted tests; DEFERRED.md has zero hits for "COVERAGE OWED" and one hit for "container()" — the stale claim itself. The e2e half (tests/tests/three_shard_round_trip_e2e.rs) still exists, as the audit said.

## [9] PARTIALLY_DISSOLVED
The named defect is cured. The smoke is rewritten to fly THE world's own home shell with realms derived from `world_roster` (dual_cluster_crossing_smoke.rs:1-27,189-190; bins/src/lib.rs:256-260), so the boot cause is structurally gone. It is now ledgered: DEFERRED.md:3936-3945 (D-WORLD-8) names BOTH pre-arc reds, gives the one cause, and adds the standing rule "a red gate is ledgered with a RED row and an owner decision"; the opposite claims are struck in place (DEFERRED.md:2976-2980, :3404). The unnamed second red is resolved (rlm_proc_spawn_smoke; p1_parity's "unexpected class" cause is now handled at process_parity.rs:102). Residue: (1) "is green" (DEFERRED.md:2980) is an assertion — UNMEASURED here (read-only, no cargo); (2) `just gate`'s coverage step is still red on 18 regions "pending the owner's call" (step5_sl7_lane_deletion.md:198-202) with no DEFERRED row — the gate stays un-passable, breaking D-WORLD-8's own rule.

## [10] STILL_OPEN
Untouched. justfile:39 is still `tier_b_floor := "90"`, and the recipes consuming it (justfile:41 `coverage-io-prod`, justfile:49 `coverage-io-prod-hooks`) are byte-unchanged — `git diff -U0 justfile` shows batch-1 only added `-p vd-physics` to `tier_a` (justfile:12), rewrote the render-crossing/walk comments, and retired `rlm-demand-login-flight` (justfile:253-260). The ratchet policy comment it fails to deliver is still there verbatim (justfile:36-37). `git log -S tier_b_floor -- justfile` still returns only ca2ddb7 and 7e9d126 — the value has never moved. The measurement gap widened rather than closed: the newest recorded run is 95.09% (DEFERRED.md:2407), against a floor of 90. No new row covers it: D-WORLD-1..9 (DEFERRED.md:3861-3957) and D-PLACE-1..6 (:3958-4023) are world-roster and placement rows; LOW-11 (DEFERRED.md:2388) still ledgers only the regions-only/package-total weakness, not the numeric slack; D-40 (:393) still ledgers the %c merge.

## [11] STILL_OPEN
Untouched by batch-1. The region/function half is still package-scoped — justfile:22 `llvm-cov --branch {{tier_a}} … --fail-under-regions 100 --fail-under-functions 100` — while the branch half at justfile:26-31 is still `llvm-cov report --branch --ignore-filename-regex '(/bin/|/tests/)' --json --summary-only` with `{{tier_a}}` absent, so its denominator is not pinned to the Tier-A set (comment justfile:19-20 still claims "the report step enforces it from the same profdata"). `git diff -- justfile` touches only tier_a's membership (justfile:12 gained `-p vd-physics`), the crossing/walk recipes and their comments — the divergence now spans one more package. No ledger row covers it: DEFERRED.md D-WORLD-1..9 (3861-3957) and D-PLACE-1..6 (3958-4026) are world-roster/placement rows; grep for coverage-fast/report --branch/tier_a in DEFERRED.md returns only pre-arc lines 1974 and 2358.

## [13] DISSOLVED
Root cause removed and the wrong diagnosis retracted. The test no longer names Planet(7): it derives the spawn coord from THE world (crates/bins/tests/rlm_proc_spawn_smoke.rs:166 `vd_bins::world_roster(&DEV)`, :186 `coord: planet(galaxy_seed, home_seed, inner_seed)`), where `world_roster` reads the home/galaxy/inner names off the same boot the shard uses (crates/bins/src/lib.rs:2427-2452). The galaxy leg keeps `galaxy(2)` (:205) with a comment stating the System(1) collapse — matching the audit's own correction (a). The port-band guess is struck through and retracted with the true cause named (docs/design/rehome_one_mechanism.md:166), and it is now ledgered: DEFERRED.md:3936-3945 (D-WORLD-8 🟩, both pre-arc reds = one missing-realm boot fence, plus "a red gate is ledgered with a RED row"). Blast radius closed too (crates/bins/tests/node_per_realm_walk.rs:5-12). Non-blocking: proc_launch.rs:190-197 still volunteers the port-band guess, and no post-fix green run is recorded.

## [14] STILL_OPEN
Untouched by batch-1. The counter still exists with no production reader: crates/client/src/view.rs:105 (field), :155 (increment), :373-374 (accessor); siblings echo_rows_dropped (view.rs:92,:171,:367) and foreign_space_rows (view.rs:77,:160,:361; realm_view.rs:65,:92,:145). A whole-tree grep yields only those sites plus test assertions (view.rs:662/672/691/714/755/915/921, realm_view.rs:265/279, net.rs:1147 — inside `#[cfg(test)] mod tests` opening at net.rs:730). The DevState builder still omits any row-drop counter (crates/client/src/net.rs:576-616: stale_frames_dropped :602-603, decode_errors :610, nonfinite_poses :611), devproto/src/state.rs:140-166 still has no field, and WaitField still has the same six arms with no row-drop (crates/devproto/src/predicate.rs:14-33, ALL at :51-58). No ledger row covers it: grep of DEFERRED.md finds no "resurrect_rows_dropped"/row-drop entry, and D-WORLD-1..9 / D-PLACE-1..6 (DEFERRED.md:3861-4023) are cluster/placement scoped. Batch-1 touched crates/client only in Cargo.toml and realm_scene.rs.

## [15] DISSOLVED
The shipped path is gone. `write_seed_regions` and `emit-seed-fixtures` no longer exist (whole-tree grep returns no definition); `crates/bins/src/bin/vd-devcluster.rs:97-99` records them DELETED under SL5, leaving `emit-world-scene` (:92-95) as the one emitter. That emitter, `crates/bins/src/lib.rs:2562-2581`, calls `world_roster` + `boot_regions_and_movers`, which is parameterless on THE world (`crates/bins/src/lib.rs:2318-2336`, `UniverseConfig::world`) — the same call a shard boots — so the single-source claim is now true. `scripts/crossing-playground.sh:11-13,54` passes NO `--realm-boxes` (streamed scene); the two render smokes feed it `write_world_regions` output (`render_boxes_smoke.rs:143-146,171`; `render_crossing_smoke.rs:412`). Ledgered truthfully: D-WORLD-4 🟩 (DEFERRED.md:3891-3900) plus the in-place C-6b amendment (:3404), residue as D-WORLD-5 🟧 (:3911-3915). Residue is cosmetic only: `crates/core/src/geometry.rs:929-930` and `crates/client/src/realm_scene.rs:347-348` still name `realm_regions_for` as the source.

## [16] PARTIALLY_DISSOLVED
Both named artifacts are gone. `write_visual_regions`/`emit-visual-fixtures` no longer exist (grep: zero hits in crates/, only the tombstone at crates/bins/src/bin/vd-devcluster.rs:97-99); the ONE emitter is `write_world_regions` off `world_roster`+`boot_regions_and_movers` (crates/bins/src/lib.rs:2563-2582), exposed as `emit-world-scene` (vd-devcluster.rs:91-96). scripts/visual-run.sh:18-26 is rewritten to that emitter with no `VD_UNIVERSE_SCALE`/`resolve_universe_scale` narration. Ledgered truthfully by D-WORLD-4 🟩 (docs/design/DEFERRED.md:3891-3900, "the seed/visual scene emitters" deleted). The adjacent knob is deleted too (crates/bins/src/lib.rs:2240 tombstone). RESIDUE: the second preset itself survives — `UniverseConfig::visual_scale()` (crates/physics/src/worldgen.rs:1336) now has ZERO non-test callers (every use-site is ≥1693, inside `mod tests` opening at 1438-1439), and "visual_scale" appears 0 times in DEFERRED.md; plus stale `VD_UNIVERSE_SCALE=visual`/`resolve_universe_scale` narration in scripts/demand-visual-run.sh:66-68.

## [17] STILL_OPEN
Every step of the chain survives batch-1 unchanged. A child observer stands at its own placement and pushes its OWN box into its reflect set (crates/sim/src/stub.rs:7634, 7777-7782); the child stores that whole set verbatim (stub.rs:8441 `from_above.0 = cs.realms`); the next emit forwards the whole holding one level down — `merged.extend_from_slice(from_above)` (stub.rs:7891) — through the single subtraction that deliberately KEEPS the recipient's own outline (stub.rs:8001, 8011-8040). So an area still receives PARENT@ -own_placement. The gate still pins it: tests/tests/frame_conversion_e2e.rs:2367-2373 asserts `drawn[&PLANET] == -CHAIN_AREA_FROM_PLANET_M2` (constant at :2205). The only diff on this path is the D-PLACE signature change (`region.frame`/PlacementBook, stub.rs:7898). No ledger row names it: DEFERRED.md's single "SL1" hit is D-PLACE-2 (:3985, the stored-centre residual); D-PLACE-3 (:3996) would incidentally cure it but never states this leak.

## [18] PARTIALLY_DISSOLVED
Reduced and now truthfully ledgered, but the core violation stands. Residue: the field still exists (crates/core/src/geometry.rs:938, doc :935-937 "WHERE THIS REALM SITS INSIDE ITS PARENT"); the shard's OWN row is still in the planted set (crates/core/src/worldgen.rs:164 `let mut chain = vec![hosted_realm];`, :145-147; plant at crates/bins/src/bin/shard.rs:119-125, :153); static systems still carry real ring positions (crates/physics/src/worldgen.rs:580, :464, :199-203). Production STILL reads the own value at boot: crates/bins/src/lib.rs:2306-2310 builds `ChildReach::Fixed(r.center…)` for every parented region including own, consumed by shard.rs:291-294 → geometry.rs:1267-1288 → :1178. What changed: movers now use `Excursion(sma*(1+ecc))` (lib.rs:2299), killing the size-only fence (geometry.rs:1154-1160), and DEFERRED.md:3985-3993 (D-PLACE-2 🟥) names the residue exactly — "every shard is still planted with its own row's centre" — so it is 🟥 owed, not cured.

## [19] STILL_OPEN
Untouched by batch-1. The transient dest ingress is still the whole of `let pose = item.pose.sanitized();` then an unconditional insert (crates/sim/src/stub.rs:5738-5749), and the dispatch arm still discards `to_realm` via `..` (stub.rs:4548-4552) while the durable arm reads it and calls the refusing `place_arriving_pose` (stub.rs:4574, definition :4054). The in-code admission survives verbatim (stub.rs:5647-5652). The mis-framed number is still consumed raw: AoI folds it into the own-frame observer list (stub.rs:7663-7664) and containment anchors the book by the pose LABEL, `owning_realm(pose.frame, config.realm)` (stub.rs:4825, :4864) — the placement rework kept label anchoring. The gate is still blind: SRC_REALM=System(7)/DST_REALM=System(8) siblings, no FrameRef assertion (tests/tests/p3_transient.rs:100-101, :66-85). No new row covers it (D-PLACE-1..6, DEFERRED.md:3958-4035); the nearest entry (DEFERRED.md:3559-3562) is now worse — it cites deleted `rebind_pose_to_dest`/`IdentityFrames`.

## [20] STILL_OPEN
Untouched, and now worse. Both cited contract comments survive verbatim: crates/wire/src/intershard.rs:1094 ("the batch's `rebind_pose_to_dest` forms an Area frame") and :1113 ("`emit_transient_batch`'s `rebind_pose_to_dest`"), plus :1076, crates/wire/src/version.rs:24, crates/sim/src/stub.rs:745, crates/sim/src/saga.rs:58. Batch-1 DELETED the function itself — crates/core/src/frame.rs now defines only transfer_frame (:133); zero `fn rebind_pose_to_dest` in the tree — so the frozen wire names a nonexistent symbol. The transient path is unchanged: emit_transient_batch still calls flush_pose_for_dest (stub.rs:5652, verbatim upward arm at :4230-4232) and adopt stores `item.pose.sanitized()` raw (stub.rs:5737); `to_parent` stays write-only (stamped stub.rs:3357, dropped by `..` at :5630-5636). Ledger over-claims stand: DEFERRED.md:3043 and the 🟩 row at :3558-3561 (three fictional callers) plus a now-deleted test at :3577. D-PLACE-1 (:3963-3965) records the deletion but retracts neither row.

## [21] STILL_OPEN
Untouched by batch 1. The false pointer stands verbatim: docs/design/step5_sl7_lane_deletion.md:252-254 still says the sibling-outline debt is "ledgered in DEFERRED.md" (file not in `git status` modified set). No such entry exists at HEAD: `grep -ci "own look|draws itself|authors its own|how it looks|authors the child"` over docs/design/DEFERRED.md returns 0, and the only "SL3" hit is DEFERRED.md:3380 (the closed own-row drop). The new rows do not cover it — D-PLACE-3 (DEFERRED.md:3996-4005) is explicitly the CENTER half (owner Q2, "the outline carries no position", `RealmShape` minus `center`), i.e. finding 22; D-PLACE-2 only names `child_shape` as a `center` reader (:3990). The breach is live: crates/sim/src/stub.rs:8087 `shape: region.shape` inside `child_shape` (:8070), emitted up at :7068, to the client at :7762, down at :7781; wire field crates/wire/src/channels.rs:310.

## [22] PARTIALLY_DISSOLVED
The Q2 violation itself survives: `RealmShape` still packs `center` (crates/wire/src/channels.rs:309) and `shape` (:310) in one struct; `child_shape` still fills that centre from a placement read pinned at `SHAPE_LANE_TICK = UniverseTick(0)` (crates/sim/src/stub.rs:8070-8090, const at :7939); the client still draws it (crates/client/src/realm_scene.rs:330 `center: s.center`, :420 `None => *boot`).

Two of the finding's three legs are gone. The load-bearing leg — "a static child has no per-tick row" — is dissolved: the movers-only filter is deleted, `child_rows` (stub.rs:1322-1332) walks `direct_children` (:1339) with no motion test, so every direct child ships a row (ledgered D-FO-7, docs/design/DEFERRED.md:3818-3825). The "unledgered" leg is cured truthfully by D-PLACE-3 🟥 (DEFERRED.md:3996-4005), which quotes Q2, names exactly these three sites, and dates it to the next wire minor bump.

## [23] STILL_OPEN
Untouched. The render fold is now at crates/sim/src/stub.rs:7760 `if render_routes.contains_key(obs) & next_in {` → :7762 `drawn.push(child_shape(regions, region, tick_hz));` — still AoI band ∧ client route, no liveness term. `child_shape` (stub.rs:8070-8088) still takes the outline from the parent's seed `region.shape` (:8085). The batch-1 placement split only swapped the source to `regions.child_rows(config.realm, book)` (:7620); `child_rows` (:1320) joins `direct_children` (:1339), which filters on `parent == own` only — never liveness. `child_liveness` is read at :7589/:7631/:7884/:7916, never in the draw fold. No new ledger row covers it: D-PLACE-1..6 (DEFERRED.md:3958-4023) and D-WORLD-1..9 (:3861-3957) address placement/world shape; D-PLACE-3 is owner Q2, a different finding. The false citations survive verbatim: step5_sl7_lane_deletion.md:254 and rehome_one_mechanism.md:99. No test gates drawing on liveness (grep: zero hits).

## [25] DISSOLVED
The finding's two surviving sites are gone. `frame_context` no longer exists (grep "fn frame_context" over crates: zero hits; `LocalFrames`/`FrameContext` survive only in prose). The movers-only feed filter is deleted: `authored_realm_snaps` (crates/sim/src/stub.rs:1276-1279) now maps `child_rows` — every direct child, "static and moving alike (owner Q3 … the last of the rival has-orbit tests, D-FO-7)". vd-sim holds exactly ONE motion test, `placement_row` (crates/sim/src/stub.rs:1371), both arms writing one `FramePlacement`, and it cannot name the motion: `moving` holds opaque `MotionFn`s, vd-physics is dev-only (crates/sim/Cargo.toml:21) and the ban is executable (tests/tests/crate_isolation.rs:126 `sl4_the_crossing_path_cannot_name_a_motion`). Ledgered truthfully: D-PLACE-1 🟩 (DEFERRED.md:3958-3966) and D-FO-7 🟧 (:3818-3830, residue = realm-lane SCALE, not a motion test). The one remaining `moving.get` is boot-only `child_reaches` (crates/bins/src/lib.rs:2297), kind-blind downstream.

## [26] DISSOLVED
Finding 26 = the audit's "SL4 structural enforcement does not exist in any form" (docs/audit/stage_c_full_law_audit_2026-08-14.md:482); DEFERRED.md:3958 numbers the SL4 majors 25/26/27, confirming the mapping.

Both cited imports are gone: crates/core/src/lib.rs:21-37 no longer declares `celestial` (it now lives at crates/physics/src/celestial.rs, crates/physics/src/lib.rs:20); crates/core/src/frame.rs carries no orbit import; crates/sim/src/stub.rs:8501's `use vd_physics::celestial::{OrbitalElements, orbital_state}` sits inside `#[cfg(test)] mod tests` (stub.rs:8492) with vd-physics a dev-dependency only (crates/sim/Cargo.toml:20-21).

The demanded rule now exists and is executable: tests/tests/crate_isolation.rs `sl4_the_crossing_path_cannot_name_a_motion`, with an OBSERVED-FAILING record, plus the producer half in `the_dependency_law_holds...`. Motion reaches sim only as opaque `MotionFn` (crates/core/src/placement.rs:34; stub.rs:1021,1371). Deviations are ledgered honestly (D-PLACE-1/4).

## [27] PARTIALLY_DISSOLVED
Fence half CURED, measurably. `child_fits_in_parent` no longer reads `center`: it consumes a kind-blind `ChildReach` (crates/core/src/geometry.rs:1137-1143, :1172-1183); the boot states a mover's apoapsis `sma*(1+ecc)` (crates/bins/src/lib.rs:2298) and the shipped shard passes it (crates/bins/src/bin/shard.rs:291-294). THE world's numbers moved so no planet escapes: compression solved against apoapsis (crates/physics/src/worldgen.rs:345, ECC_CAP_SIGMAS :891), gated by tripwire crates/bins/tests/one_containment_answer.rs:338 plus unit arm geometry.rs:1789. Ledgered D-PLACE-1 🟩 (DEFERRED.md:3958-3983).

RESIDUE: the two-meanings field itself survives — `region_center_of` still branches Orbital⇒ZERO vs StaticOffset⇒offset (crates/physics/src/worldgen.rs:199-204). Truthfully ledgered open as D-PLACE-2 🟥 (DEFERRED.md:3985-3995), though that row names deleted `VD_REALM_BOUNDARIES` as a HEAD reader, and D-45(d) (DEFERRED.md:3412) still ledgers the now-live geometric check as P4/P5-deferred.

## [28] DISSOLVED
The SL4 placement split moved the generator to crates/physics/src/worldgen.rs; the cited defect is gone there. The `#[allow(dead_code)]` no longer exists (`grep allow(dead_code)` over crates/physics/src + crates/core/src returns only two prose mentions, worldgen.rs:176 and :2361 — no attribute). The variant's doc now states the truth: "PRODUCED IN PRODUCTION: `generate_system_forest` emits one per planet of THE world ... every shipped shard boots that forest ... This doc used to claim 'no production producer at P3' ... both false (audit finding 28)" (crates/physics/src/worldgen.rs:174-179), over the live producer at :588. The claim is now a measurement, not a comment: `the_world_produces_an_orbital_planet_in_every_system` (:2363-2384) asserts every star system of `UniverseConfig::world` holds an Orbital planet, in vd-physics, a Tier-A gate crate (justfile:12). Residual, outside this finding: DEFERRED.md:3423 and rehome_one_mechanism.md:288, :1081 still narrate the discharged deferral/stale comment.

## [29] DISSOLVED
Finding re-checked: the SL4 "speed-sized band" comment (audit line 846, Where: sim/src/stub.rs:5012). The false sentence is gone from the tree — `grep "sized from the occupant" crates/` matches only the audit report itself. At the same containment decision (`region.band.member`, crates/sim/src/stub.rs:5208) the replacement comment now states the truth: "STATIC widths: the shipped band is built at v_rel = 0, and the speed-sized widening is still OWED, D-WORLD-4b" (crates/sim/src/stub.rs:5211-5212). The residual mechanism gap is honestly ledgered: docs/design/DEFERRED.md:3902-3910 (D-WORLD-4b 🟧) records v_rel=0.0 in `BandConfig::build` (now crates/physics/src/worldgen.rs:1022-1029), states the comment was fixed, cites the audit MINOR ":846", and owes the widening at P5. One stale sentence survives elsewhere: DEFERRED.md:3411 still argues C-3 tunnel-safety from a "velocity-safe ContainmentBand", contradicting D-WORLD-4b — separate doc line, not this finding's Where.

## [32] PARTIALLY_DISSOLVED
The bin half is gone: shard.rs:119 and gateway.rs:105 both boot `boot_regions_and_movers`/`boot_world`, which have no scale arm and call `UniverseConfig::world(...)` (crates/bins/src/lib.rs:2329); VD_REALM_BOUNDARIES, write_seed_regions and crossing_playground are deleted (only a tombstone comment at crates/bins/src/bin/vd-devcluster.rs:97). It is now ledgered, truthfully but as OPEN: DEFERRED.md:3911-3915 D-WORLD-5 🟧 cites this finding (":512-542") and flips green only when `generate_walk_forest` has no caller outside its definition.

Residue: the seedless second generator still exists (crates/physics/src/worldgen.rs:696; `realm_regions_for_walk_config(_seed_universe,…)` :791-795; `hand_placed` "TEST WORLD" :652,:660) and still plants the whole scenario tier (tests/src/lib.rs:284,289,833,851,894; realm_lifecycle_e2e.rs:524,566). Unledgered residue: the false claim "the EXACT geometry the production shard.rs boot computes" survives at tests/src/lib.rs:823,841, and the production `impl Default for SeedInjectorConfig` still builds walk-scale (gateway.rs:162).

## [33] DISSOLVED
The whole override family is gone from live code. `crates/bins/src/bin/shard.rs:119-157` now plants `boot_regions_and_movers(universe_seed, ...)` unconditionally, with the explanatory note at :152-156 ("There is NO boundary-file override any more (SL5)"); `resolve_realm_boundaries`/`parse_realm_boundaries`/`guard_boundaries_in_realm`/`override_regions_for_boundaries`/`override_containment_band` return zero hits across `crates/`, `scripts/`, `tests/`, `justfile`. `crates/bins/src/lib.rs:1958-1964` states the deletion; `vd-devcluster.rs:97-99` deletes the fixture emitters, and the only `BOUNDARIES` strings left are two SL5 assertions that no shape's shard env carries any such key (`lib.rs:4173-4177`, `4442-4446`). `render_crossing_smoke.rs:8-9,378` now flies `up --dual` on THE world with no injection. Ledgered truthfully as D-WORLD-4 (`DEFERRED.md:3891-3900`), which cites this audit finding. Residue: one stale test comment naming the deleted fn (`crates/physics/src/worldgen.rs:3062`).

## [34] DISSOLVED
The slowdown machinery is gone from the tree. No code reads VD_VISUAL_ORBIT_SLOWDOWN/VD_TEST_ORBIT_SLOWDOWN and no `central_mass_kg /=` divisor exists (repo-wide grep: only the deletion note at crates/bins/src/lib.rs:2240-2246). The `OrbitSlowdown` guard is deleted from crates/bins/tests/rlm_demand_login.rs, and both named round-trip gates — `a_planet_to_system_return...` (:734) and `repeated_planet_system_roundtrips_do_not_freeze` (:946) — now fly the shared full-speed pattern `vd_bins::flight::rendezvous_into_planet` (:782, :1042; helper at crates/bins/src/flight.rs:41-56), as does the walk gate (crates/bins/tests/node_per_realm_walk.rs:309). justfile:253-255 retires the separate flight recipe: `rlm-demand-login` (justfile:251, in `gate:` justfile:260) IS the full-speed gate. Residue is cosmetic only: two stale comments still describe the deleted knob (rlm_demand_login.rs:731, :1101) — the audit's own stale-comment class, not this defect.

## [35] PARTIALLY_DISSOLVED
Both code halves are gone. scripts/crossing-playground.sh:25 now runs `dev-cluster.sh up --demand` and :51 launches the client with NO `--realm-boxes` (:12-13 "the scene STREAMS"); the walk-world hand-off is impossible because `emit-seed-fixtures`/`write_seed_regions`/the `crossing_playground` module are deleted (crates/bins/src/bin/vd-devcluster.rs:97-99; grep for them across crates/ returns nothing). The unbootable Forest is deleted too: ClusterShape is `Single|Dual|Chain|Demand` (crates/bins/src/lib.rs:223-228) and `--forest` now errors loudly (vd-devcluster.rs:143-149). Ledgered truthfully as D-WORLD-4 🟩 (DEFERRED.md:3891-3900) and D-WORLD-2 (:3875-3882, naming the --demand playground). RESIDUE: the ledger sentence the finding cited survives — DEFERRED.md:2999-3001 still calls "the live `--forest` window" the interim VISUAL proof and owes a re-base onto the six-shard forest; the blanket amendment at :2961-2967 disclaims only the "PROVEN" claims, not this Owed paragraph.

## [36] STILL_OPEN
Untouched — the batch-1 SL4 split only MOVED the code. Both presets are still public and callerless: `pub fn canonical()` crates/physics/src/worldgen.rs:1201 and `pub fn seed_derived(seed)` :1428 (the latter's only pre-test use is its own `canonical()` call at :1430). The `#[cfg(test)] mod tests` boundary is worldgen.rs:1438-1439, so every call site (:1760-1762, :1776-1778, :1797-1807, :1915, :2076, :2217, :2959) is a test. Tree-wide grep finds no caller outside that file. The self-repudiation survives verbatim at worldgen.rs:1223-1224 ("2.5x the real value … bodies do not nest correctly"). Tier-A cost survives and moved with it: justfile:13 now lists `-p vd-physics`. No ledger row covers it — D-WORLD-1..9 (DEFERRED.md:3861-3957) and D-PLACE-1..6 (:3958-4029) never name canonical/seed_derived/preset; D-WORLD-5 (:3911) ledgers `generate_walk_forest` only.

## [37] STILL_OPEN
Untouched. `redrive_stranded_crossings` still runs in the production schedule (crates/sim/src/stub.rs:2134, fn at :5438) and, whenever armed, still emits `push_demand(..., regions.coord_of(lc.to_realm), ..., DemandVerb::KeepAlive, ...)` (stub.rs:5474-5487) — only shifted from the audited :5280. `to_realm` is still `outward_dest` (stub.rs:5045-5057), i.e. the shard's own parent on an outward crossing, and the gate still asserts that upward case: `an_armed_shard_keepalive_demands_the_crossing_dest_full_lineage` (stub.rs:18635, comment :18641 "dest = the PARENT", filter on `coord_of(PARENT_REALM)`). Ingest remains source-blind (crates/node/src/rlm_runtime.rs:478-483; crates/io-prod/src/boot.rs:467), and the demand still stamps `last_demand_tick` (crates/sim/src/rlm.rs:357), falsifying `no_demand_since` (rlm.rs:495). Ledger: DEFERRED.md:3364 is still only descriptive inside D-RLM-14; D-WORLD-1..9 (:3861-3958) and D-PLACE-1..6 (:3958-4023) cover cluster/placement only. (The by-count index-37 section, the HR4 Ship panic, is likewise untouched: stub.rs:8176.)

## [38] STILL_OPEN
Untouched by batch-1. The arm still exists and still ships: `crates/wire/src/intershard.rs:393` declares `RealmShapeObservation(RealmShapeObservation)`; the payload `shapes: Vec<RealmShape>` is at `crates/wire/src/intershard.rs:865`; the producer still merges its own children's outlines with every set its live children lifted and pushes one hop up (`crates/sim/src/stub.rs:7070-7089`). The approval asymmetry the finding rests on is unchanged: `crates/wire/src/version.rs:96-99` (minor 10) still says "owner-approved 2026-08-13" while minor 11, this arm, `crates/wire/src/version.rs:100-105`, carries no approval citation. `docs/design/step5_sl7_lane_deletion.md` is unmodified in the working tree (git status shows only channels.rs, DEFERRED.md, rehome_one_mechanism.md), and DEFERRED.md contains zero occurrences of `RealmShapeObservation`. The nearest new row, D-PLACE-3 (`docs/design/DEFERRED.md:3996-4005`), covers a FUTURE `RealmShape` minus `center` bump that "owes an SL6 note" — it does not record an ask or approval for this arm.

## [39] STILL_OPEN
Re-checked the titled finding (audit line 606, SL6 up-observation relay bound). Batch-1 rewrote stub.rs heavily (SL4 placement split) but left the relay's gating untouched. crates/sim/src/stub.rs:7033 still relays every held interior batch upward on `if let Some(parent) = parent_node.0` alone, inside the loop over all observed entries (stub.rs:7009); the only prune is TTL (stub.rs:6999). The local fan filter is still authority-state, not visibility: `emits(&holds, d)` at stub.rs:7004 / stub.rs:6560-6563. The shape half's up-relay is gated only on parent + AoI cadence and still merges every live child's lifted set (stub.rs:7063-7071). Ingest `on_realm_observation` accepts on parent_ok + decode + placeability only (stub.rs:8254-8300). The frozen contract still promises "re-fans ... within visibility" (crates/wire/src/intershard.rs:374). No ledger row: DEFERRED.md has no RealmObservation entry; D-WORLD-1..9 / D-PLACE-1..6 (DEFERRED.md:3861-4030) cover other defects.

## [40] STILL_OPEN
Untouched by batch-1. The arm is still on the frozen enum (crates/wire/src/intershard.rs:356) with its unchanged doc block (:329-355) carrying no approval marker — unlike its immediate siblings ChildLive "owner-approved 2026-08-12/13" (:357) and the up-observation lane "owner-approved 2026-08-13, the SL6 ask" (:369). Version ledger minor 9 (crates/wire/src/version.rs:92-95) is likewise unchanged. Both wire files are clean in `git status`; only gateway.rs is modified, and it still consumes the arm at crates/connection-plane/src/gateway.rs:1796, produced at crates/node/src/orchestrator.rs:251. Trail still absent: `grep -rn ShardRoster docs/` hits only the audit report itself; DEFERRED.md has no roster entry, and none of the new rows cover it (D-WORLD-1..9 at DEFERRED.md:3861-3957 are world/cluster shape, D-PLACE-1..6 at :3958-4029 are placement).

## [41] STILL_OPEN
Untouched by batch-1. The wire contract still states "on the AoI cadence plus once at adopt" (crates/wire/src/intershard.rs:361-362; the file has no working-tree diff). Production still emits the bit ungated inside `aoi_decide` — crates/sim/src/stub.rs:7688-7694, guarded only by `if let Some(parent) = parent_node`, comment at :7682 still says "level-triggered per tick" — and `evaluate_realm_aoi` is still scheduled every tick with only `.run_if(has_synced)` (crates/sim/src/stub.rs:2149-2153). The two sibling lanes still DO gate on the cadence (`shape_ship_due` :7058-7064, `parent_headread_due` :7506-7516), so the phrase's meaning is unchanged. Grep shows 7689 remains the sole non-test constructor: no adopt-edge emit exists. No ledger cure: `ChildLive`/"adopt-edge" appear nowhere in docs/design/DEFERRED.md, and D-WORLD-1..9 (:3861-3957) / D-PLACE-1..6 (:3958-4023) cover clusters and placements only. Gate still prints instead of asserting (tests/tests/frame_conversion_e2e.rs:2104-2113).

## [42] STILL_OPEN
Checked the titled section (report line 886: SL6/SL3 debt has no DEFERRED entry, dup of 21). Untouched. The false cross-reference stands: docs/design/step5_sl7_lane_deletion.md:252-254 still says the parent-ships-sibling-OUTLINES debt is "ledgered in DEFERRED.md". The working-tree ledger (82 ### rows) still has no such row: the only "SL3" hit is docs/design/DEFERRED.md:3380 (closed D-RLM-17, own-row drop), and the new D-PLACE-3 (:3996-4005) ledgers only the outline's CENTRE (owner Q2, "RealmShape minus center"), explicitly leaving the extent on the wire. Breach still live: crates/sim/src/stub.rs:8070-8090 child_shape packs shape: region.shape (:8087) off the PARENT's roster into RealmShape (crates/wire/src/channels.rs:310), shipped as ChildSceneSet (crates/wire/src/intershard.rs:404,879). Disambiguation: the literal 43rd "## [" section is the SL4 Orbital dead-code finding, which IS dissolved (crates/physics/src/worldgen.rs:175, :2358).

## [43] STILL_OPEN
Checked the section matching the named title (audit :920). (Positional 44th is instead the SL4 band comment, which IS dissolved — fixed + ledgered D-WORLD-4b, DEFERRED.md:3902-3907, citing ":846".) The SL7 return-address defect is untouched: crates/sim/src/stub.rs:2506 still passes the raw frame sender `*from` into `retain_child_live` (stub.rs:8212-8218), which validates only the parent link (:8221-8225) and (fence,at) freshness (:8228-8233), then stores `home` verbatim (:8234-8240); grep `peers.contains|known_peer|allow_list|is_booked|authoriz` in stub.rs = 0 hits. That `home` still targets both down-lanes: `home_of_active_child` (:6811) → `push_cascade` (:7273) → `push_flow(home, RealmCascade)` (:6943), and `entry.home` → ChildSceneSet (:7900-7903). Ingress stays peer-blind (crates/io-prod/src/mesh.rs:1458; file unmodified). Unledgered: zero ChildLive/retain_child_live hits in DEFERRED.md; D-RLM-8 (:3315) remains scoped to `record_realm_demands`; no D-WORLD-*/D-PLACE-* row (:3861-4023) covers it.

## [44] STILL_OPEN
Untouched by batch-1. The capstone is byte-identical: tests/tests/p2_transfer_gates.rs:629-631 still states the "gone only once the dest is the sole render source" claim and :671-675 still asserts max_absent_run==0 (git diff HEAD shows no change to this file; HEAD is still the audit's eba296b). The mechanism that makes it inert is intact: crates/sim/src/stub.rs:6560-6562 still gates emits() on an open (entity, HoldRole::Source) hold, closed at the dest's promote by close_hold_at_fence in the SpawnV2 arm (stub.rs:3861), with the Despawn arm's comment "it stopped EMITTING at hold closure" (stub.rs:3877-3878). Holds are still armed in the fixture (crates/harness/src/topology.rs:1127). Client tracks remain sticky (crates/client/src/view.rs:294-298). The ledger claim is unamended: docs/design/DEFERRED.md:578-580 still says the capstone "proves NO vanish across it". No D-WORLD-*/D-PLACE-* row mentions band-exit or max_absent_run.

## [45] STILL_OPEN
Untouched by batch-1. The named defect (transient adopt verbatim pose + blind p3 gate) is intact: crates/sim/src/stub.rs:5737 still stores `item.pose.sanitized()` straight into OwnedTransients (:5738-5748) with no conversion and no refusal; the dispatch arm still discards `to_realm` via `..` (stub.rs:4548-4552) while both durable ingresses call the guard (stub.rs:3761, :4574, fn at :4054); the in-code admission survives verbatim (stub.rs:5647-5651). tests/tests/p3_transient.rs has ZERO uncommitted diff (`git diff --stat` empty), still asserts only `pos.offset()` (:66-85) with co-origin siblings SRC=System(7)/DST=System(8) (:100-101) and no FrameRef assertion. No new row covers it: D-PLACE-1..6 (DEFERRED.md:3958-4029) and D-WORLD-1..9 (:3861-3957) never mention transient/adopt, and the stale "one rebind, no per-path fork" claim still stands at DEFERRED.md:3561. (The count-based 46th section, canonical()/seed_derived, is also still open — merely relocated to crates/physics/src/worldgen.rs:1201,1430.)

## [46] DISSOLVED
Checked the title-matched section (audit line 255; dual-smoke red inside `just gate`, unledgered, ledger claiming the opposite). All three halves are cured. Root cause gone: `ClusterShape::Dual` now books `roster.galaxy` (crates/bins/src/lib.rs:259) derived from THE world by `world_roster` (lib.rs:2427-2436); the smoke is rewritten as `a_dot_re_homes_home_to_galaxy_over_the_process_dual_shard_tier` (crates/bins/tests/dual_cluster_crossing_smoke.rs:189-190), still ungated so `just gate`'s `test` step (justfile:260, :15-16) still runs it, with the C1 gate over `roster_realms(Dual)` (:268-270). Ledgered: DEFERRED.md:3936-3946 (D-WORLD-8 🟩, both pre-arc reds = one cause) plus the standing rule "a red gate is ledgered with a RED row … never explained away in commit prose"; the two false present-tense claims are struck through (DEFERRED.md:2976-2980, :3404). Residue, minor: rehome_one_mechanism.md:164 and :1337 still print the stale red uncorrected, and greenness is ledger-claimed, unmeasured here (read-only, no cargo).

## [47] DISSOLVED
The 300x knob is gone from code, not just ledgered. Repo-wide grep finds no `VD_TEST_ORBIT_SLOWDOWN`/`VD_VISUAL_ORBIT_SLOWDOWN` reader and no `OrbitSlowdown` type — only prose (crates/bins/src/lib.rs:2240 "THE ORBIT-SPEED KNOB IS GONE (Stage-C, SL5)"). Both named round-trip gates now fly the one full-speed rendezvous: crates/bins/tests/rlm_demand_login.rs:782 and :1042 call `rendezvous_into_planet` (crates/bins/src/flight.rs:50, documented at :41 as knob-deleted, aiming at where the planet will be via live `secs_since_epoch`, flight.rs:63-67). The separate slowdown-1 recipe is retired (justfile:253-255) and the whole binary — every crossing test, both return legs — runs in `just gate` at full speed (justfile:250, :260), so the unledgered debt has no subject. Residue is comment-only: rlm_demand_login.rs:731 and :1101 still describe the deleted guard (the stale-comment class, finding 51).

## [48] STILL_OPEN
Untouched. The 🟥 OWED bullet survives verbatim at docs/design/DEFERRED.md:3408 (was :3384; shifted only by insertions above), and `git diff -U0 docs/design/DEFERRED.md | grep send-once` returns nothing — batch-1 edited the sibling C-6b entry (D-WORLD-4 amendment, DEFERRED.md:3404) but not this child bullet. Every contradiction the audit named still holds: `emits()` still filters the retained ghost on an open Source hold (crates/sim/src/stub.rs:6560-6563, doc :6552-6557 "the leaver VANISHES ... at hold closure"); `push_entity_removed` (crates/sim/src/stub.rs:2994, called :2574/:3868/:3900/:6459) + `fan_entity_removed` (crates/connection-plane/src/gateway.rs:3222) are live; `grep -rn "MsgClass::Event" crates/` is empty; D-4 records the same landing LANDED (DEFERRED.md:817, :823). No D-WORLD-*/D-PLACE-* row (DEFERRED.md:3861-4023) covers it. Batch-1 added staleness: the bullet's named blocker gate `dual_cluster_crossing_smoke` now exists only as a comment (crates/bins/src/lib.rs:1667).

## [49] STILL_OPEN
Untouched by batch-1. All four stale production doc sites survive verbatim: crates/sim/src/stub.rs:488 ("`refresh_source_ghost` is its only writer"), crates/sim/src/stub.rs:310 and crates/sim/src/authority.rs:18 (both "strictly-DERIVED `simulates() | is_fed_ghost | is_retained_ghost`"), and crates/sim/src/stub.rs:6669-6672 (same three-term union plus the "fed ghost (the dest-driven collider feed)" gloss) — the last shifted from :6448 only by unrelated edits. The symbols remain dead: grep for refresh_source_ghost/is_fed_ghost/SourceGhostMirror returns only these comments plus the tombstone at stub.rs:3821; no definitions. The real two-term rule still reads `simulates() | (is_retained_ghost & Source hold open)` at crates/sim/src/stub.rs:6560-6562, contradicted 107 lines below by the correct slice-F comment at :6659-6661. The gate narration persists at tests/tests/frame_conversion_e2e.rs:2980. No covering ledger row: D-WORLD-1..9 and D-PLACE-1..6 (docs/design/DEFERRED.md:3861-4023) concern world clusters and placement only.

## [50] STILL_OPEN
Untouched. `git diff HEAD -- crates/bins/tests/process_parity.rs` is empty and no commit since the audit HEAD eba296b touches the file, so both swallowed lanes stand verbatim: `MsgClass::RealmSnapshot => self.realm_frames += 1,` at crates/bins/tests/process_parity.rs:102 sitting ahead of `other => panic!("unexpected class {other:?}")` (:103), and `ServerControlMsg::Event(_) => {}` (:150) ahead of `other => panic!("unexpected control message in P1: …")` (:151). `realm_frames` is still declared "counted, never asserted on" (:46, written at :60/:102, read nowhere), the unmeasured-tolerance comment survives (:96-101), and the false justification "tracks no bystander figures, so an eviction has nothing to evict" (:147-149) is still contradicted by `walker.poses.len() == 2 && idle.poses.len() == 2` (:295-296) and `idle.poses[&idle_own]` (:345). No ledger row covers it: D-WORLD-1..9 (DEFERRED.md:3861-3956) and D-PLACE-1..6 (:3958-4023) never mention the parity tolerance; grep for process_parity/realm_frames finds only pre-arc uses.


# Batch code review — confirmed findings

## [MAJOR] batch-code: Realm-aware re-home now targets the SOURCE shard on every upward crossing, where the retained Ghost for the same entity still lives — `re_home_apply` has no same-entity guard
**Where:** crates/sim/src/stub.rs:3777
`scan_deadlines` now resolves the forward-re-home target from the flushed pose's frame (crates/node/src/saga_runtime.rs:2230-2234):

```rust
let pose_realm_owner = live.flushed_pose
    .and_then(|p| p.frame.realm())
    .and_then(|realm| dir.head(DirectoryKey::Realm(realm)))
    .map(|r| r.authority.node());
```

For an UPWARD hand-off the flushed pose is shipped VERBATIM in the source realm's own frame (`build_rehome`, crates/node/src/saga_runtime.rs:983-984; `flush_pose_for_dest`'s upward arm, crates/sim/src/stub.rs:4196+), so `p.frame.realm()` is the SOURCE realm and `dir.head` is the live SOURCE node. The re-home therefore ships `ReHome` back to the shard the entity just left.

That shard still holds the entity. `self_fence_foreign_entity` explicitly retains it: "KEEP the dot (no remove) and KEEP `granted` == true — it survives as the retained Ghost" (crates/sim/src/stub.rs:3267-3269), and `GhostFlow::SpawnV2`'s comment confirms "The retained DOT stays: it is the return-crossing target until band-exit tears it down" (crates/sim/src/stub.rs:3855).

`re_home_apply` inserts unconditionally under a synthetic key with no check that this shard already holds a dot for `entity`:

```rust
let session = SessionId(entity.0);
dots.0.insert(session, Dot { ... authority: Authority::Owned { fence: cmd.new_fence }, ... });
```

The shard now holds TWO dots for one EntityId. The reachable consequence is on the NEXT crossing: `on_saga_demote` → `self_fence_foreign_entity` picks the FIRST match by BTreeMap key order (crates/sim/src/stub.rs:3235-3241) using `foreign_takeover_target = (dot.entity == entity) & dot.granted & !dot.departing` (crates/sim/src/stub.rs:3945-3947) — which the retained Ghost satisfies. When the Ghost sorts first, the guard at crates/sim/src/stub.rs:3247-3250 sees `!simulates()`, counts `self_fence_skipped`, and RETURNS — the Owned dot is never demoted, so the source keeps simulating and emitting an entity the directory has committed elsewhere. The comment at crates/sim/src/stub.rs:3775-3776 ("the oracle held-set sees exactly one Owned dot for this entity") only covers Owned dots and does not protect this.
**Verdict:** NOT REFUTED — the structural defect is real and unhandled, though the stated failure chain is the wrong (unlikely) one.

CONFIRMED PREMISES:
1. Target = source node on an upward crossing. crates/node/src/saga_runtime.rs:2230-2233 resolves the target from the flushed pose's frame realm via dir.head; select_rehome_target (saga_runtime.rs:2051-2060) returns it when alive+capable. build_rehome ships the pose "VERBATIM, in the source's own frame" (saga_runtime.rs:981). For a single-realm shard's upward hand-off conversion_path returns None — the authorship constraint (crates/sim/src/stub.rs:4482-4489) rejects converting in an ancestor — so the verbatim arm fires and the pose keeps the SOURCE realm's frame. Realm heads exist in the directory (dir.grant(DirectoryKey::Realm(..), Shard(node)) — crates/node/src/rlm_runtime.rs:748), so pose_realm_owner = the source node.
2. The adopt lands, it is not refused: place_arriving_pose short-circuits on pose.frame == own with Ok(pose) (stub.rs:4062-4081).
3. The source still holds the entity: self_fence_foreign_entity keeps the dot and keeps granted (stub.rs:3267-3269); the only teardown is the dest-driven band-exit Despawn, and in CELL 2 the dest is confirmed dead so it never arrives.
4. No guard: re_home_apply inserts unconditionally at SessionId(entity.0) (stub.rs:3777-3778); on_re_home (stub.rs:3672-3724) only gates on epoch + journal. The ghost is keyed by the gateway-minted random session (crates/connection-plane/src/gateway.rs:2169), a different key ⇒ TWO dots for one EntityId. The fn doc still asserts the now-false premise "the re-home target is FRESH (the killed dest never replicated a ghost here)" (stub.rs:3656-3659), and no test covers an occupied target (stub.rs:12128, stub.rs:12200 both start from empty Dots).

MISCHARACTERIZED PART (the cited consequence): foreign_takeover_target scans a BTreeMap<SessionId, Dot> ascending (stub.rs:3235-3241, 3945-3947). Production sessions are random u128 (gateway.rs:2169) while the synthetic key entity.0 = Player(0)<<120 | node<<88 | seq<<24 | rand24 (crates/core/src/ids.rs:164-170, entity_kind.rs:34) ≈ 2^88, so the re-homed Owned dot sorts FIRST with probability ~1-2^-39. The "Ghost sorts first ⇒ self_fence_skipped ⇒ source keeps simulating a committed-away entity" outcome (stub.rs:3247-3250) is therefore essentially unreachable outside small-session-id unit rigs.

THE REACHABLE HARM INSTEAD: because the target IS the source, cmd.source == self_node, so register_and_spawn_source_ghost returns early (stub.rs:3630-3634) and no SpawnV2 proof is sent; the source's hand-off hold never closes and runs to TTL (close_hold_at_fence doc, stub.rs:565-568). At expiry the eviction fan (stub.rs:2567-2582) sees is_retained_ghost true for the orphan ghost and calls push_entity_removed (stub.rs:2994-3024, no live-owner guard) — broadcasting EntityRemoved for an entity this very shard now Owns and emits, evicting a live figure from every bystander's client. A legitimate same-node crossing cannot do this (promote flips the SAME dot Ghost→Owned via crossing_target, leaving no retained ghost); it happens only because the re-home created a second dot. The orphan ghost is also never torn down, and remove_retained_ghost (stub.rs:3923-3934) would later remove the wrong one.

Verdict: real MAJOR — the missing same-entity guard at crates/sim/src/stub.rs:3777 stands; the finding's failure narrative should be restated around the orphan ghost / hold-expiry removal fan rather than the BTreeMap-order demote skip.

## [MINOR] batch-code: Stale world numbers survive the SOI/sma re-capture: the crossing gate's failure message and the frozen-sma comment still carry the pre-batch values
**Where:** crates/bins/src/flight.rs:172
The batch moved `FROZEN_PLANET_SOI_R_M` from 4.160705354131045 to 3.9541737529995578 and `FROZEN_ORBIT_SMA_M` from [16.98, 28.87, 49.08, 83.43, 141.84] to [15.18, 25.81, 43.88, 74.60, 126.83] (crates/physics/src/worldgen.rs:3181-3189 vs `git show HEAD:crates/core/src/worldgen.rs`, driven by `VISUAL_SOI_GAP_FRACTION` 0.35→0.372 at crates/physics/src/worldgen.rs:124).

Two NEW/edited sites still state the old numbers:

1. crates/bins/src/flight.rs:172 — the `rendezvous_into_planet` failure assert, the primary diagnostic every crossing gate prints on a red run:
```rust
"NO CROSSING: flew rendezvous legs at {planet:?} for {}s but location never flipped \
 (loc {loc:?}); closest approach {best:.2} m vs ~4.16 m SOI.",
```
A pilot debugging a 4.05 m closest approach reads "inside the SOI" when it is outside.

2. crates/physics/src/worldgen.rs:2405 — inside the deliberately re-captured pin block: `// The 5 planet orbit distances (semi-major axes, render m): ~17 / 29 / 49 / 83 / 142.` immediately above `assert_eq!(smas, FROZEN_ORBIT_SMA_M.to_vec());`, whose actual values are ~15 / 26 / 44 / 75 / 127.
**Verdict:** NOT REFUTED — both cited sites are real, exact, and correctly characterized. Verified read-only against the working tree and `git show HEAD`.

SITE 1 — crates/bins/src/flight.rs:172 (file is UNTRACKED, i.e. new in this batch):
  "NO CROSSING: flew rendezvous legs at {planet:?} for {}s but location never flipped (loc {loc:?}); closest approach {best:.2} m vs ~4.16 m SOI."
The demand world's planet SOI is now FROZEN_PLANET_SOI_R_M = 3.9541737529995578 (crates/physics/src/worldgen.rs:3181). Provenance confirmed: the sentence was copied verbatim from HEAD's crates/bins/tests/rlm_demand_login.rs:823.

SITE 2 — crates/physics/src/worldgen.rs:2405: "// The 5 planet orbit distances (semi-major axes, render m): ~17 / 29 / 49 / 83 / 142." sits immediately above assert_eq!(smas, FROZEN_ORBIT_SMA_M.to_vec()) at line 2413, whose actual pinned values are 15.18 / 25.81 / 43.88 / 74.60 / 126.83 (worldgen.rs:3184-3189). The finding's quoted baseline array is exact: git show HEAD:crates/core/src/worldgen.rs lines 2816-2824 hold 4.160705354131045 and [16.98247083318794, 28.870200416419497, 49.07934070791314, 83.43487920345233, 141.83929464586896]. VISUAL_SOI_GAP_FRACTION = 0.372 confirmed at crates/physics/src/worldgen.rs:124.

REFUTATION ATTEMPTS THAT FAILED:
1. "4.16 names some other radius, not the moved planet SOI." No. rendezvous_into_planet is called only by the demand-world gates — crates/bins/tests/rlm_demand_login.rs:651/782/1042 and crates/bins/tests/node_per_realm_walk.rs:309 — and the crossing it waits on is containment into FrameRef::PlanetCentered (flight.rs:78), i.e. the planet authority sphere = planet_soi_r_m. That is exactly the constant the batch moved.
2. "The tilde makes it approximate." 3.954 rounds to ~3.95, not ~4.16 — 5.2% off. The misleading band 3.954-4.16 m is real: a 4.05 m closest approach reads as "inside the SOI, so why no crossing?" when it is outside, which is the finding's stated failure mode.
3. "Site 2 is harmless because the assert self-checks." True — no test can pass on the stale prose, which is why MINOR is the right severity for both. It does not make the claim (stale prose in code the batch touched) false.

CORROBORATING, NOT REFUTING: the batch DID refresh the twin summary prose in the const block — crates/physics/src/worldgen.rs:3175-3178 now reads "planet SOI 3.95 / orbit semi-major axes 15,26,44,75,127" where HEAD read "4.16 / 17,29,49,83,142". So the re-capture consciously updated the numbers in one comment and missed the other. DEFERRED.md:3977 independently records "planet SOI 4.1607->3.954 m".

TWO SITES THE FINDING MISSED (same class):
a) crates/bins/src/flight.rs:76 — "// One full sweep of the planet's shell (~8.3 m at ~6.3 m/s = 66 ticks) plus pipeline margin." 8.3 m is the OLD diameter (2 x 4.1607 = 8.32); the new shell is 7.91 m (~63 ticks). No functional impact (SWEEP_GRACE_TICKS = 120 carries margin), but it is a third stale-derived number in the same new file, justifying the same one-pass fix.
b) MORE SERIOUS, worth its own finding: tests/tests/frame_conversion_e2e.rs:576 — const DEMAND_PLANET_SOI_M: f64 = 4.16; is a LIVE constant, not prose, consumed at lines 637, 672, 701, 705-706, next to DEMAND_PLANET_ORBIT_M = 17.9 (line 578) and DEMAND_ORBIT_PERIOD_S = 25.4 (line 581). The file is modified in this batch and hand-replicates THE world's geometry with pre-batch numbers, so its assertions now measure a world that no longer exists — an SL5 "one world, no variant" concern rather than a comment nit.

Severity MINOR is correct for the finding as filed. Absolute paths: /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/crates/bins/src/flight.rs, /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/crates/physics/src/worldgen.rs, /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/tests/tests/frame_conversion_e2e.rs

## [MINOR] batch-code: `placement_skew_max_ticks` conflates forward skew with backward staleness, destroying the `span_ahead` measurement it is documented to be — and D-PLACE-6 states the opposite of the shipped code
**Where:** crates/sim/src/stub.rs:1854
The field is documented as one-directional: `/// The widest forward skew clamped, in ticks — the `span_ahead` measurement the placement arc owed.` (crates/sim/src/stub.rs:1854-1855).

`book_at_or_head` feeds it forward-only, correctly (`stamp.0.saturating_sub(head.at().0)`, crates/sim/src/stub.rs:4910-4912). But `arrival_book` feeds the SAME gauge with an absolute difference, so a hand-off stale by 345 ticks behind the head writes 345 into a field whose only stated purpose is sizing a forward window:

```rust
stats.placement_skew_max_ticks = stats
    .placement_skew_max_ticks
    .max(stamp.0.abs_diff(head.at().0));   // crates/sim/src/stub.rs:4939-4941
```

Since the arrival lane is the one the code says wedges in production ("refused at `head−345` ticks, once per redelivery, forever", crates/sim/src/stub.rs:1850-1851), backward staleness will dominate the gauge and no operator can read a forward bound off it.

The binding ledger then asserts the opposite of the shipped behaviour: D-PLACE-6 says "no lane asks for a FUTURE instant (the flush reads head), so no clock-skew `span_ahead` exists — UNMEASURED machinery stays unbuilt" (docs/design/DEFERRED.md:4023-4025), while `book_at_or_head` exists solely to serve `stamp > head.at()` on the relay lane (crates/sim/src/stub.rs:4907-4913). Per CLAUDE.md the ledger is binding, so this row is now false at HEAD.
**Verdict:** NOT REFUTED — both halves check out against the working tree; only the impact wording is overstated.

HALF 1 (doc/code mismatch) — CONFIRMED.
- `crates/sim/src/stub.rs:1854-1855`: `/// The widest forward skew clamped, in ticks — the `span_ahead` measurement the placement arc owed.` / `pub placement_skew_max_ticks: u64,` — one-directional by its own words.
- `crates/sim/src/stub.rs:4907-4913` (`book_at_or_head`) feeds it forward-only, gated on `stamp > head.at()` with `stamp.0.saturating_sub(head.at().0)`.
- `crates/sim/src/stub.rs:4937-4941` (`arrival_book`) feeds the SAME field with `stamp.0.abs_diff(head.at().0)` under no direction guard.
- It is deliberate and pinned, not a slip: the test `a_forward_skewed_instant_clamps_to_the_head_counted_and_measured` (`crates/sim/src/stub.rs:16444`) asserts `placement_skew_max_ticks == 3` for a forward stamp (`:16460`) and `== 92` for a stamp 92 ticks BEHIND the head (`stale.universe_tick = UniverseTick(10)` vs head 102, `:16526`). So the gauge is a two-directional clamp MAGNITUDE while its doc names it a forward bound.

HALF 2 (ledger contradicts HEAD) — CONFIRMED for the row's rationale.
- `docs/design/DEFERRED.md:4023-4025` D-PLACE-6: "no lane asks for a FUTURE instant (the flush reads head), so no clock-skew `span_ahead` exists — UNMEASURED machinery stays unbuilt."
- At HEAD lanes DO present future instants and the forward ask IS measured: `book_at_or_head`'s forward arm (`crates/sim/src/stub.rs:4908`) exists for "a source whose ClockSync arrival phase leads this receiver's" (`:4893-4896`), it serves the realm-snapshot relay (`:7371`) and the up-observation lift (`:8281`), it is covered by a test whose name is literally "…clamps_to_the_head_counted_and_measured" (`:16444`), and the field doc (`:1854`) calls itself "the `span_ahead` measurement". The same stale rationale is repeated as a code comment at `crates/core/src/placement.rs:336` ("no lane asks for the future; span_ahead is a ledgered non-feature until one does").
- Both the code and the row are UNCOMMITTED in this batch (`git diff` shows D-PLACE-6 and `book_at_or_head`/`arrival_book` as additions), so the row was written stale in the same batch; no later row covers the clamp (only D-PLACE-1..6 exist, `grep -n "^### D-PLACE"`).

WHERE THE FINDING OVERSTATES (does not refute it, but should be trimmed):
1. "destroying the measurement" / "no operator can read a forward bound off it" — nothing outside `crates/sim/src/stub.rs` reads this field at all: `grep -rn placement_skew_max_ticks --include="*.rs"` hits only `:1852,:1855,:4910,:4939,:16460,:16526`; the harness scrape takes `flush_stamp_gap_ticks_max` and `placement_book_miss` but not this one (`crates/harness/src/topology.rs:141,145`). Impact today is documentation/observability design, not a live operator defect — consistent with MINOR.
2. "the binding ledger asserts the opposite of the shipped behaviour" — the row's DECISION is still true at HEAD: `crates/core/src/placement.rs` has `span_back` only (`:144,:159`), no `span_ahead`, no `PlacementBook::project`, and `at()` still errs ahead of the head. It is the row's stated JUSTIFICATION that is now false, not its conclusion.
3. Partial defence the finding omits: the sibling counter's doc one line above (`crates/sim/src/stub.rs:1843-1852`) does document BOTH directions and says "the widest skew rides `placement_skew_max_ticks`" with no "forward" qualifier — so the two-directional behaviour is documented adjacently; it is the field's own one-liner at `:1854` that is stale and contradicts it. That makes it a stale-wording defect rather than an undocumented behaviour, but the field-level doc still says the wrong thing.

Fix is two words plus one ledger clause: drop "forward" from `crates/sim/src/stub.rs:1854` (or split the gauge into ahead/behind maxima), and rewrite D-PLACE-6's premise at `docs/design/DEFERRED.md:4024-4025` to say the forward ask exists and is answered by a head CLAMP (counted + measured) rather than by a forward retention window — the conclusion (span_ahead + `project` unbuilt) survives unchanged.

## [MINOR] batch-code: The flight-law margins `world_roster` asserts are derived from the HOME system's movers only, but the chain gate flies a polar corridor into the SIBLING system, whose orbits are unasserted
**Where:** crates/bins/src/lib.rs:2461
`world_roster` computes I-AXIS, I-POLE and I-RADIAL over one mover set:
```rust
let (_, movers) = boot_regions_and_movers(p.universe_seed, &held_home, home, ...);  // lib.rs:2441-2442
let axis_clearance_m = movers.values().map(|e| e.sma * (1.0 - e.ecc) * e.inclination.cos()) ... // lib.rs:2461-2464
let pole_altitude_m = movers.values().map(|e| e.sma * (1.0 + e.ecc) * e.inclination.sin().abs() + release_reach) ... // lib.rs:2475-2478
```
`held_home` is `{home}`, so `movers` is the HOME system's planets and nothing else.

The module doc for the shared pilot states these asserts license every leg: "every static-cluster leg flies the ±Z POLAR corridor ... clear of both by the I-AXIS / I-POLE / I-RADIAL margins `world_roster` asserts" (crates/bins/src/flight.rs:7-9). But leg E of the chain gate flies the polar corridor into the SIBLING star: `let approach = roster.sibling_centre + DVec3::new(0.0, 0.0, -300.0);` then `creep_into(..., creep_axes_plus_z(), &sibling_label, ...)` (crates/bins/tests/node_per_realm_walk.rs:352-361). The sibling's planets are generated from a different system seed (crates/physics/src/worldgen.rs:582-589 draws per-system elements from `realm_stream(seed_universe, &[UNIVERSE_SEED, GALAXY_SEED, seed])`), so their inclinations, eccentricities and semi-major axes are not the home system's and are covered by no assert.

The in-test comment states the clearance as a bare claim rather than a measurement — "staying ≥140 m under every sibling planet (J2: the ring is XZ, orbits are near-XY)" (node_per_realm_walk.rs:346-347). A seed or world-number change that widened the sibling's inclinations would make the creep thread a sibling planet's shell and strand the dot on an unhosted realm (the exact J-0 outcome the I-AXIS assert text names at lib.rs:2459-2460), with every assert still green.
**Verdict:** CONFIRMED — not refuted. Every load-bearing claim checks out. (1) The margins are home-only: crates/bins/src/lib.rs:2440-2442 builds `held_home = {home}` and `visual_regions_and_movers` (lib.rs:2247-2263) takes movers as `moving_children_for_config(seed,cfg,hosted)` = `bodies.filter(|b| b.parent == Some(hosted))` (crates/physics/src/worldgen.rs:256-265), so I-AXIS (lib.rs:2461-2464), I-POLE (lib.rs:2475-2478) and I-RADIAL (lib.rs:2492-2497) fold over the HOME system's planets only; the sibling is touched solely for cell()==ZERO (lib.rs:2530-2534) and centre!=ZERO (lib.rs:2543-2549). (2) The over-claim is real and appears in four places, not one: crates/bins/src/flight.rs:7-9, the field doc at crates/bins/src/lib.rs:2408-2410 ("what licenses EVERY ±Z corridor leg"), the gate module doc crates/bins/tests/node_per_realm_walk.rs:15-25, and — strongest — the ledger row D-WORLD-2 at docs/design/DEFERRED.md:3879-3880, which justifies shipping the permanent-strand risk with "world_roster ASSERTS the I-AXIS/I-POLE/I-RADIAL margins that make that corridor PROVABLY unable to reach an unhosted realm". (3) Leg E flies the polar corridor into the sibling at node_per_realm_walk.rs:347-360, and the "≥140 m under every sibling planet (J2)" clearance is prose only — it lives at node_per_realm_walk.rs:340-341 and 22-24 (the finding cites 346-347: minor line drift, text verbatim) and a repo-wide grep for "J2" returns nothing else: no assert, no measurement. (4) Sibling elements are a different draw: worldgen.rs:582 keys the stream on the sibling's own seed; sma is deterministic per index but ecc/inclination are per-system draws and inclination is explicitly UN-clamped (worldgen.rs:400,407). (5) The harm is reachable in principle: ClusterShape::Chain books only galaxy+inner+sibling (lib.rs:256-262), so sibling planets are unhosted, and D-WORLD-2 (DEFERRED.md:3875-3883) documents an unhosted dest as a permanent "CROSSING UNRESOLVED" strand. ADVERSARIAL CAVEATS (they narrow the practical risk, consistent with MINOR, but do not refute): leg E's corridor sits outside the 150 m shell until the flip, creep_into cuts the throttle on the flip (flight.rs:305-322) and leg F flies further out, so the dot never traverses the sibling's orbital plane; and the AU→render compression is solved so EVERY system's outer planet's worst-instant face (apoapsis at the ecc cap + SOI) sits VISUAL_SYSTEM_MARGIN_M = 4.0 m inside its system shell (worldgen.rs:103-106, 326-345), a seed-independent radial bound. But that 4.0 m headroom is the same order as the documented post-commit creep drift (~3.75 m = CREEP_AXES_MAG 0.05 × 500 m/s × 150 ms poll, flight.rs:277-281,292) plus the containment inset, so it is not a proof either — the comfortable part of the margin rests entirely on the sibling's inclinations staying small, which is exactly what nothing asserts. Under the project's standing "measure it, never argue it" rule, a bare "≥140 m" comment gating a permanent-strand failure mode is the defect the finding names.

## [MAJOR] law-conformance: SL4 — D-PLACE-1 is ledgered 🟩 claiming the fifth rival has-orbit test "became Motion::closing_speed_mps"; the test is still in production and the accessor is dead code
**Where:** crates/physics/src/worldgen.rs:229
`to_regions` (production — every shard boot goes through `realm_regions_for_config`) still runs the has-orbit test verbatim:

    let v_child = orbital_of(b.placement).map_or(0.0, |e| e.v_peri());

Meanwhile `Motion::closing_speed_mps` (crates/physics/src/motion.rs:86) and its sibling `Motion::max_excursion_m` (crates/physics/src/motion.rs:74) have ZERO callers anywhere in the tree — `grep -rn --include="*.rs" "closing_speed_mps\|max_excursion_m" .` matches only crates/physics/src/motion.rs (the definitions plus their own unit tests; exit 1 outside that file).

DEFERRED.md:3966 states, inside a 🟩 LANDED row: "The four rival has-orbit tests collapsed into the one writer; the fifth (the `v_peri` AoI width) became `Motion::closing_speed_mps` — a scalar handed to boot." No boot hands it anything. This is the same defect class the Stage-C audit itself raised at MAJOR ("A 🟩 LANDED ledger entry claims two sim unit tests that no longer exist"), re-created by the fix batch, and it violates the standing NEVER-ASSUME rule: the collapse is argued, never measured. Either the accessor becomes the single writer of that scalar, or the ledger row must drop the claim and re-open the fifth rival.
**Verdict:** NOT REFUTED — all four load-bearing claims verified. (1) The has-orbit test is live in production: crates/physics/src/worldgen.rs:229 `let v_child = orbital_of(b.placement).map_or(0.0, |e| e.v_peri());` inside `to_regions` (:219), which is the sole lowering behind BOTH production entry points, `realm_regions_for_config` (:598-599) and `realm_regions_for_walk` (:795) — every shard boot. (2) It was moved, not collapsed: `git show HEAD:crates/core/src/worldgen.rs` line 242 is the character-for-character identical statement (same comment at :241), so the fix batch relocated the file into vd-physics and wrote a new accessor beside it without rewiring the call site. (3) Both new accessors are dead: `grep -rn --include="*.rs" "closing_speed_mps\|max_excursion_m" .` matches ONLY crates/physics/src/motion.rs — definitions at :74 and :86 plus their own unit tests at :178/:182/:191/:199/:203/:211; zero callers elsewhere. Outside motion.rs, `Motion` is reached only via `kepler_motion_fns`/`motion_fn` (crates/bins/src/bin/shard.rs:302, crates/bins/tests/one_containment_answer.rs:113, crates/sim/src/stub.rs, tests/src/lib.rs), i.e. only `state_at` is ever exercised. (4) The ledger text is as quoted inside a green row: docs/design/DEFERRED.md:3958 `### D-PLACE-1 🟩 THE PLACEMENT ARC … landed 2026-08-14`, with :3965-3966 stating "the fifth (the `v_peri` AoI width) became `Motion::closing_speed_mps` — a scalar handed to boot." No boot hands it anything. The cited precedent is real: docs/audit/stage_c_full_law_audit_2026-08-14.md:161 is a MAJOR for the same defect class (a 🟩 row claiming two sim unit tests that no longer exist). NUANCE that softens the title but not the finding: the surviving test now sits INSIDE vd-physics, the one crate licensed to name a motion (crates/physics/src/lib.rs:11-17), and tests/tests/crate_isolation.rs:139-144 proves the crossing path carries no Cargo edge to it — so SL4's structural fence is intact and this is not a live rival on the crossing path. The real defect is ledger honesty plus two public accessors whose only exercise is their own tests (HR5-green while measuring nothing — the NEVER-ASSUME pattern). MAJOR is defensible on the ledger-honesty half alone; fix is to wire `to_regions` through `Motion::closing_speed_mps` (and the boot fence through `max_excursion_m`, or delete it) or amend the row to re-open the fifth rival.

## [MAJOR] law-conformance: SL4 — the new worst-instant boot fence goes size-only for a mover on every shard that does not host the mover's parent: child_reaches hands it a defaulted zero wearing a Fixed label
**Where:** crates/bins/src/lib.rs:2297
`child_reaches` keys the reach on the `moving` roster:

    let reach = match moving.get(&r.realm) {
        Some(e) => ChildReach::Excursion(e.sma * (1.0 + e.ecc)),
        None => { ... ChildReach::Fixed(r.center.delta_m(LatticePos::local(DVec3::ZERO), tier)) }

but `moving` comes from `boot_regions_and_movers` → `moving_children_for_config(seed, config, hosted)`, and `moving_children` (crates/physics/src/worldgen.rs:256-264) filters `b.parent == Some(hosted_realm)` — the hosted realm's DIRECT children only.

Failure scenario, reachable today on the Chain cluster: the inner-planet shard boots with `held = {Planet(inner)}`, `hosted = Planet(inner)`. Its neighbourhood contains its own region (parent = the home System, an ancestor also present) but `moving` holds only the planet's own children. So the planet — an Orbital body whose stored `center` is zero by construction (`to_regions_gives_an_orbital_body_a_zero_center_position_authored_by_the_frame`, worldgen.rs:1897) — falls to the `None` arm and is judged at `Fixed(0,0,0)` against its parent's 150 m shell. That is exactly the "THE FENCE USED TO GO SIZE-ONLY FOR EVERY MOVER" defect the fence was rebuilt to cure (crates/core/src/geometry.rs:1156-1160), and `RegionNestError::NoReachForChild`'s own message says "a defaulted zero is banned". The result is a fence whose verdict on one forest differs per shard, silently lenient wherever the mover's parent is hosted elsewhere.
**Verdict:** CONFIRMED (real, unhandled at HEAD) — every mechanical claim verified by reading the code; only the severity framing needs qualifying.\n\nVERIFIED CHAIN:\n1. crates/bins/src/lib.rs:2293-2311 — child_reaches maps EVERY region with parent.is_some(); the reach is keyed on `moving.get(&r.realm)` (line 2297) and the None arm (2299-2310) emits ChildReach::Fixed(r.center.delta_m(local(ZERO), tier)).\n2. crates/physics/src/worldgen.rs:256-265 — moving_children filters `b.parent == Some(hosted_realm)` (direct children of the hosted realm ONLY), reached via moving_children_for_config (606-612) ← visual_regions_and_movers/boot_regions_and_movers (crates/bins/src/lib.rs:2247-2263). So a mover whose parent is NOT `hosted` is absent from `moving`.\n3. crates/physics/src/worldgen.rs:199-204 — region_center_of(Placement::Orbital(_)) = LatticePos::local(DVec3::ZERO); planets are minted Placement::Orbital at line 588. delta_m(local(ZERO), tier) is therefore exactly 0 at any tier, so the Fixed label carries a zero.\n4. crates/core/src/worldgen.rs:140-156 (neighbourhood_scope) + 158-175 (ancestor_realms) — the neighbourhood keeps the shard's OWN row verbatim with `parent` intact plus the whole ancestor chain, so on a planet shard both the Planet row (parent = System) and the System row are present; guard_single_root is satisfied by the Universe root.\n5. crates/core/src/geometry.rs:1267-1298 — guard_children_fit_parents judges EVERY non-root region (no authored-only scoping); child_fits_in_parent (1172-1183) turns Fixed(0) into child.shape.max_reach_from(ZERO) = the planet's own SOI (~3.95 m) vs the system's 150 m inscribed extent → passes trivially. That is precisely the size-only verdict lines 1154-1160 say the rebuild removed, and the zero that RegionNestError::NoReachForChild's text (1117-1119) calls banned.\n6. Reachable today: crates/bins/src/bin/shard.rs:105-108 (held = {own_realm}; no launcher emits VD_HELD_REALMS), 116-125 (hosted = own_realm), 291-296 (the guard_regions_nest call). ClusterShape::Chain spawns the inner-planet realm-shard (crates/bins/src/lib.rs:260; env builder exercised at 4423-4427) and the demand spawner is node-per-realm. Same class also hits a Station/Area shard, whose ANCESTOR Planet row is a mover judged Fixed(0).\n7. Not ledgered: docs/design/DEFERRED.md:3958-3983 (D-PLACE-1) claims the fence "no longer reads a mover's zeroed centre"; D-PLACE-2 (3985-3993) covers only the surviving field, not this per-shard hole.\n\nQUALIFICATIONS (lower severity, do not refute):\n(a) "silently lenient" overstates the blast radius — the shard that AUTHORS the mover still runs the strict Excursion check at its own boot, a child realm cannot be live without its parent live (demand AoI) or co-launched (Chain), and crates/bins/tests/one_containment_answer.rs:338-388 boots as EVERY anchor of THE world and checks apoapsis+soi vs shell exhaustively. A regression still fails, just on a different shard/test. The genuine defect is one forest yielding two different verdicts depending on which shard boots it, plus the re-entry of the zeroed-centre read the placement arc claims to have eliminated.\n(b) the implied cure conflicts with SL1 — giving the planet shard its own a(1+e) is a realm learning its own placement. The law-conformant fix is to SCOPE the geometric check (child_reaches / guard_children_fit_parents) to the rows this shard authors and refuse otherwise, rather than defaulting a zero for rows it may not know.

## [MAJOR] law-conformance: SL4 — the batch's own structural crate fence is carved open for vd-connection-plane, a Tier-A crate on the crossing/routing path
**Where:** tests/tests/crate_isolation.rs:75
`MAY_NAME_MOTION` allowlists `vd-connection-plane` ("the gateway resolves logins against THE world"), and crates/connection-plane/Cargo.toml:10 adds `vd-physics = { workspace = true }` as a NORMAL dependency. `vd-connection-plane` is simultaneously listed in `TIER_A` (crate_isolation.rs:59).

SL4 says: "Enforce structurally (a module/crate dependency rule), not by care." The gateway is on the crossing path — it consumes `TransferControl`, swaps the session route on `CommitAuthority`, and holds the directory-facing session state — yet `use vd_physics::celestial::OrbitalElements` now compiles there. The gate's own doc claims "an orbit symbol on the crossing path is an unresolved-crate compile error, not a review finding"; for the routing plane it is back to being a review finding.

The dependency is only needed for `vd_physics::worldgen::{UniverseConfig, WorldView}` (crates/connection-plane/src/gateway.rs:40) — a read-only world view, not motion — so the exemption is wider than the stated reason requires.
**Verdict:** CONFIRMED — the finding survives adversarial reading; two sub-claims are imprecise but do not dissolve it.

VERIFIED FACTS. tests/tests/crate_isolation.rs:75 lists "vd-connection-plane" as the second MAY_NAME_MOTION entry (reason at :76-77). crates/connection-plane/Cargo.toml:10 carries `vd-physics = { workspace = true }` under `[dependencies]` (section opens :8) — a NORMAL edge, exactly what the gate's parser would otherwise catch (it drops only kind=="dev", crate_isolation.rs:29-31). crate_isolation.rs:59 also lists the crate in TIER_A. The carve-out is against a UNIVERSAL assert: sl4_the_crossing_path_cannot_name_a_motion (:138-148) iterates every vd-* package; only the allowlist and vd-physics skip. `use vd_physics::celestial::OrbitalElements` compiles in the gateway today (celestial is `pub mod`, crates/physics/src/lib.rs:20).

THE CONTRAST IS DELIBERATE ELSEWHERE, which strengthens the finding: crates/sim/Cargo.toml:19-21 and crates/node/Cargo.toml:19-21 both put vd-physics under [dev-dependencies] with an explicit SL4 comment ("DEV-ONLY, deliberately (SL4): fixtures plant real motion; the shipped crate cannot name one"). vd-connection-plane is the ONLY Tier-A crate that took the normal edge.

THE GATEWAY IS ON THE HAND-OFF PATH: gateway.rs:2367 `fn on_transfer_control`; the route.store swap driven by CommitAuthority (:8-9, :2263); cut buffer + commit drain (:1348-1355).

NOT ALREADY HANDLED: D-PLACE-1 (docs/design/DEFERRED.md:3958-3981) asserts flatly "the crossing path carries NO edge to it"; no D-PLACE-*/D-WORLD-* row and no connection-plane row in DEFERRED.md records this exemption. It exists only as an allowlist comment.

TWO IMPRECISIONS IN THE FINDING (neither refutes it):
(1) "The gate's own doc claims..." overstates. crates/physics/src/lib.rs:11-14 parenthetically scopes the fence to "(vd-core, vd-wire, vd-sim, the client)" — the gateway is not in that enumeration, so the doc is not literally self-contradicting. But the ASSERT is universal, so the carve-out is still a hole in a blanket rule.
(2) "a read-only world view, not motion" is wrong on the types: UniverseConfig is motion-parameterised (crates/physics/src/worldgen.rs:1157 central_mass_kg; :1163-1170 orbital_a0_au/orbital_ratio/ecc_sigma/incl_sigma/ecc_cap), and WorldView::generated (:645) runs generate_system_forest, which mints Placement::Orbital — the gateway calls it at gateway.rs:8913. What IS true (and better supports the remediation): the accessors the gateway uses — regions()/neighbourhood()/contains_realm() (:672-687) — return lowered RealmRegions only, `bodies` is private, and :665 states "an orbit's elements do not survive lowering"; gateway consumption is names/lineage only (realm_registry_for_home :2020-2036, render_pin :2044-2052 yield RealmId root + pin).

RESIDUAL JUDGEMENT. No live SL4 violation: gateway.rs:40 is the crate's SOLE vd_physics import and names no orbit. The defect is that for this one crate SL4 reverts to enforcement "by care" — the exact failure mode the law names. Narrowing is not a one-line Cargo change: WorldView/UniverseConfig live in vd_physics::worldgen by the batch's own split rule (crates/core/src/worldgen.rs:3-5), so the fix is either splitting the world builder from celestial/motion or shrinking the gateway to only RECEIVE a pre-built WorldView (production already passes one; only Default at gateway.rs:162 and tests construct one). Severity MAJOR is arguable — a documented, reason-carrying exemption with no live violation reads closer to MINOR — but the substance holds.

## [MINOR] law-conformance: HR5 — the new Tier-A crate's only public generic has no in-crate test; its coverage rides another crate's dev-dependency
**Where:** crates/physics/src/motion.rs:111
`pub fn kepler_motion_fns<K: Ord>(...)` is the only `pub fn …<…>` in vd-physics (`grep -n "pub fn [a-z_]*<" crates/physics/src/*.rs` matches this line and nothing else). `grep -rn kepler_motion_fns crates/physics/` returns ONLY the definition — the crate's own `mod tests` covers `motion_fn` and `kepler_motion_fn` but never the map form.

Every instantiation lives outside the crate: crates/sim/src/stub.rs (12 sites, dev-dep), crates/bins/src/bin/shard.rs:302, tests/src/lib.rs, tests/src/frame_fixture.rs. `coverage-fast` only reports it as covered because the single llvm-cov session also builds `-p vd-sim`. CLAUDE.md HR5 discipline (b) is explicit: "cover a crate's generics completely in its own unit tests" — an uninstantiated generic emits no region at all, so removing vd-sim's dev-dep would make this function silently vanish from the 100% gate rather than fail it.
**Verdict:** CONFIRMED — every factual claim verified by reading the code; not exempted, not ledgered, not handled.\n\n(1) Sole generic: `grep -n \"fn .*<\" crates/physics/src/*.rs` across all four modules (celestial.rs 664L, motion.rs 228L, taxonomy.rs 824L, worldgen.rs 3191L) returns exactly one type-parameterized function — crates/physics/src/motion.rs:111 `pub fn kepler_motion_fns<K: Ord>(...)`. All other `<` hits are Result/Vec return types.\n\n(2) No in-crate coverage: within crates/physics/ the name occurs only at motion.rs:111. The crate's `mod tests` (motion.rs:120-227) has four tests; `a_motion_fn_is_the_motion_made_opaque` (motion.rs:218-226) exercises `motion_fn` and the singular `kepler_motion_fn` and never the map form. The crate's only integration test, crates/physics/tests/celestial_ephemeris_pin.rs, touches `orbital_state` only.\n\n(3) All instantiations external: crates/sim/src/stub.rs (import at 8502; 13 call sites — 12682, 12721, 13055, 13160, 13255, 13391, 14953, 15037, 15509, 15723, 19241, 19251, 20780), crates/bins/src/bin/shard.rs:302, crates/bins/tests/one_containment_answer.rs:113, tests/src/lib.rs:299/883/898, tests/src/frame_fixture.rs:375.\n\n(4) Coverage rides a dev-dep edge: crates/sim/Cargo.toml lists vd-physics under [dev-dependencies] (\"DEV-ONLY, deliberately (SL4)\") and stub.rs:8502 is inside `#[cfg(test)] mod tests`. The justfile `tier_a` is `-p vd-core -p vd-physics -p vd-devproto -p vd-wire -p vd-sim …` — vd-bins and the `tests` crate are absent, and `--ignore-filename-regex '(/bin/|/tests/)'` excludes them regardless. So of 19 instantiation sites, only vd-sim's unit tests run inside the coverage session.\n\n(5) The law is stated twice: CLAUDE.md HR5 discipline (b), and the justfile's own tier_a comment — \"Each crate's OWN tests must cover its full surface (llvm counts regions per compiled instance, so leaning on the vd-tests binary would double-instance every crate — learned in P1.7).\"\n\nRefutation attempts that failed: coverage-exemptions.toml is `[exemptions] # (none yet)` and no `coverage(off)` exists in motion.rs; vd-physics IS Tier-A (justfile tier_a; crates/physics/src/lib.rs:19 \"Coverage: Tier-A — 100% region + branch (HR5)\"); no DEFERRED row covers it (D-PLACE-1..6, DEFERRED.md:3958-4030, are silent), while motion.rs:42 explicitly claims in-crate coverage for the monomorphic `state_at` arms — the generic directly below it has none.\n\nEXTRA EVIDENCE the finding did not cite, which makes the risk concrete rather than hypothetical: D-PLACE-4 (DEFERRED.md:4007-4013) ledgers the owed rework as moving `author_placements` into vd-node reading a `vd_physics::MotionRoster` \"so vd-sim holds NO motion data at all\" — that is exactly the change that drops vd-sim's dev-dep on vd-physics, i.e. planned future work deletes the only edge carrying this function's coverage, and (per llvm-cov's per-monomorphization region emission) the gate would stay green with the function absent from the report rather than fail.\n\nTwo cosmetic imprecisions, neither material: \"12 sites\" in stub.rs is actually 13, and crates/bins/tests/one_containment_answer.rs:113 is omitted from the instantiation list (outside Tier-A either way). Severity MINOR is correct — the gate passes today.\n\nFix note: `with_moving_children` (crates/sim/src/stub.rs:1116) takes `BTreeMap<RealmId, MotionFn>`, so K is `RealmId` at all 19 sites — the type parameter buys nothing; monomorphizing it, or adding a three-line in-crate test, closes this.

## [MINOR] law-conformance: SL4 / DRY — the apoapsis bound is now written twice: the boot fence reimplements the Motion accessor documented as existing for it
**Where:** crates/bins/src/lib.rs:2298
crates/bins/src/lib.rs:2298 computes `ChildReach::Excursion(e.sma * (1.0 + e.ecc))`. crates/physics/src/motion.rs:77 computes `Motion::Kepler(elements) => elements.sma * (1.0 + elements.ecc)` inside `max_excursion_m`, whose doc says: "The WORST instant, in CLOSED FORM, for the boot fence".

The boot fence never calls it (finding 1's grep: zero external callers). Two independent expressions of the same rule, one of them the declared single writer and dead — so a future change to the Integrated/thruster-budget arm lands in `Motion` and never reaches the fence that is supposed to consume it.
**Verdict:** CONFIRMED — the finding survives adversarial checking on every axis.

REAL: crates/bins/src/lib.rs:2298 computes `ChildReach::Excursion(e.sma * (1.0 + e.ecc))`; crates/physics/src/motion.rs:77 computes `Motion::Kepler(elements) => elements.sma * (1.0 + elements.ecc)`. Identical f64 expression, same operand order, so numerically bit-identical today.

THE ACCESSOR IS DEAD: `grep -rn max_excursion_m` over the whole tree returns exactly four hits — crates/physics/src/motion.rs:74 (definition) and :178, :182, :191, all inside that file's own `#[cfg(test)]` module. Zero product callers. Its doc at crates/physics/src/motion.rs:65 explicitly claims it exists "for the boot fence".

THE FENCE BYPASSES IT: crates/bins/src/bin/shard.rs:294 feeds `vd_bins::child_reaches(&regions, &moving)` into `vd_core::geometry::guard_regions_nest` (crates/core/src/geometry.rs:1269), and child_reaches derives the bound from raw elements.

NO BARRIER TO DEDUPLICATION (so "it can't call it" fails as a defence): vd-bins already depends on vd-physics — crates/bins/src/lib.rs:2289 names `vd_physics::celestial::OrbitalElements`, crates/bins/src/bin/shard.rs:302 names `vd_physics::motion::kepler_motion_fns`. And crates/physics/src/motion.rs:19-20 declares Motion "PRIVATE knowledge of this crate's callers (boot composition, the demand spawner)" — the boot is exactly the licensed party. `Motion::Kepler(*e).max_excursion_m()` would compile; the SL4 crate_isolation gate binds the crossing path, not the boot.

NOT ALREADY HANDLED: docs/design/DEFERRED.md:3958-4030 (D-PLACE-1..6) covers ChildReach and the apoapsis fence at length and never ledgers the duplicate writer.

THE FUTURE-RISK MECHANISM CHECKS OUT AND IS WORSE THAN STATED: child_reaches's roster is Kepler-only — `moving: &BTreeMap<RealmId, vd_physics::celestial::OrbitalElements>` (crates/bins/src/lib.rs:2287-2290) — so an `Integrated` child cannot even be expressed to the fence; it falls through to the `None`/`ChildReach::Fixed` arm. The P8 thruster-budget bound promised at crates/physics/src/motion.rs:67-69 will land in `Motion::max_excursion_m` and provably never reach the fence.

SEVERITY MINOR IS CORRECT: no live defect, the two expressions agree exactly; this is a DRY/maintenance hazard on a not-yet-exercised arm.

TWO SMALL CORRECTIONS, NEITHER MATERIAL: (a) strictly, child_reaches is the boot's reach-map BUILDER in vd-bins, not the fence itself (the fence, guard_regions_nest in vd-core, consumes the map kind-blind) — wording only; (b) the finding understates the spread — the apoapsis subexpression appears in FOUR places: crates/bins/src/lib.rs:2298, crates/bins/src/lib.rs:2477 (the I-POLE flight-law assert), crates/bins/tests/one_containment_answer.rs:369 (the apoapsis tripwire), and the dead crates/physics/src/motion.rs:77. The declared single writer is the only one nobody calls.

## [MINOR] law-conformance: §4u stale-symbol class — the batch deleted LocalFrames/FrameContext/rebind_pose_to_dest and left production docs naming them, including a broken rustdoc intra-doc link
**Where:** crates/physics/src/worldgen.rs:184
DEFERRED.md D-PLACE-1 records: "The `FrameContext` trait, `LocalFrames`, `IdentityFrames`, `rebind_pose_to_dest`, the `_resolved` twins and `ChildFrame` are DELETED." Surviving references to those symbols:

- crates/physics/src/worldgen.rs:184 — a resolvable-link form ``[`LocalFrames::with_moving_child`](vd_core::frame::LocalFrames::with_moving_child)``, i.e. a broken intra-doc link, not just prose
- crates/physics/src/worldgen.rs:251, :1899, :2741 — `LocalFrames::with_moving_child`
- crates/physics/src/worldgen.rs:218 — `to_regions`' doc still says it keeps "the output-side `rebind_pose_to_dest` in agreement"
- crates/sim/src/stub.rs:5160, :7975 — `LocalFrames`
- crates/bins/tests/rlm_demand_login.rs:609 — `on_flush_source` via `LocalFrames`

The Stage-C audit closed with exactly this finding (MINOR, §4u); the batch re-opens it with new instances.
**Verdict:** NOT REFUTED — confirmed real, newly introduced by the batch, and unledgered. (1) BROKEN LINK IS LITERAL, NOT PROSE: crates/physics/src/worldgen.rs:184 reads `///   ([`LocalFrames::with_moving_child`](vd_core::frame::LocalFrames::with_moving_child)), so its boundary is` — a path-form link destination, which rustdoc resolves as an intra-doc link. The target has no definition: `grep -rn "struct LocalFrames\|trait FrameContext\|fn rebind_pose_to_dest\|struct ChildFrame\|struct IdentityFrames" --include="*.rs" crates/ tests/` returns ZERO definitions (crates/core/src/frame.rs now holds only FramePlacement + transfer_frame, and its own :10 and :119 speak of FrameContext in past tense). (2) THE BATCH CREATED IT — measured against HEAD, not assumed: `git show HEAD:crates/core/src/worldgen.rs` line 203 had the same sentence with `(crate::frame::LocalFrames::with_moving_child)`, which RESOLVED — `git show HEAD:crates/core/src/frame.rs` has `pub struct LocalFrames` at :263 and `pub fn with_moving_child` at :285. The batch moved the file into the new vd-physics crate (crates/physics/src/worldgen.rs is untracked/`??`), rewrote the path to `vd_core::frame::…`, and deleted the target. Newly broken, not carried-over rot. (3) PRODUCTION, NOT TEST, for the headline sites: `region_center_of` (doc :182-196) and `to_regions` (doc :218) are on the shipped boot path — worldgen.rs:599 `realm_neighbourhood_for_config` → `to_regions` ← crates/bins/src/lib.rs:2259; the `#[cfg(test)]` at worldgen.rs:93/:509 are single items, and the test module opens at :1438. (4) NOT ALREADY HANDLED: docs/design/DEFERRED.md:3963-3964 (D-PLACE-1, 🟩) is the row that DECLARES these symbols deleted; no row covers the survivors — `grep -n "stale comment\|stale-comment\|stale doc\|§4u" docs/design/DEFERRED.md` returns only :569, an unrelated slice-F entry. THREE CORRECTIONS that do not change the verdict: (a) the finding UNDERSTATES — crates/sim/src/stub.rs:7975 is ALSO a resolvable-link form (``because [`LocalFrames`] re-derives``, a shortcut intra-doc link), so there are TWO broken links, not one; (b) two cited sites are TEST docs, not "production docs" — crates/physics/src/worldgen.rs:1899 sits under `#[test] fn to_regions_gives_an_orbital_body_a_zero_center_position_authored_by_the_frame` (:1896) and :2741 is a test doc, both inside the `#[cfg(test)]` module opening at :1438 (the production claim holds for :184, :218, :251, crates/sim/src/stub.rs:5160 and :7975; crates/bins/tests/rlm_demand_login.rs:609 is listed separately as a test); (c) MINOR severity is correct because it is NOT gate-caught — Cargo.toml `[workspace.lints]` has a clippy table only with no rustdoc table, and `grep -n doc justfile` shows no `cargo doc` step, so `rustdoc::broken_intra_doc_links` (warn-by-default) fires only on a manual `cargo doc`. HONESTY NOTE: the worktree is read-only with no cargo, so the warning EMISSION is UNMEASURED — inferred from rustdoc's documented behavior; the load-bearing fact (the link target has no definition anywhere in the tree) is a grep measurement that could have failed and did not. Adjacent same-class sites the finding did not list: crates/core/src/geometry.rs:1053, crates/wire/src/version.rs:24, crates/wire/src/intershard.rs:1076/1094/1113, crates/sim/src/saga.rs:58, crates/node/src/saga_runtime.rs:917/1636/2090/2118/6764, crates/bins/src/bin/shard.rs:130 — though several of those are deliberate past-tense tombstones ("used to call"), which the two bracketed links are not.

## [MINOR] law-conformance: Gate honesty — the new PlacementLedger::publish clippy ban is unmeasured and covers only vd-sim, while the ledger's write API is fully public
**Where:** crates/sim/clippy.toml:10
The new entry is `{ path = "vd_core::placement::PlacementLedger::publish", reason = "only the ONE placement writer (author_placements) publishes books…" }`. The block's own pre-existing header three lines above states "Empirically checked: injecting a banned call here produces a clippy diagnostic (SEAL-1)" — the new path carries no such recorded control, so under the standing NEVER-ASSUME rule its enforcement is argued rather than measured (an unresolvable path in clippy.toml is a silent no-op).

Scope is also narrower than the claim "ONE writer": `Placements(pub PlacementLedger)` (crates/sim/src/stub.rs:946) exposes the ledger through a public field and `PlacementLedger::publish` is `pub`, but the ban exists only in crates/sim/clippy.toml — vd-node, vd-bins and vd-tests can mint a rival placement history with no diagnostic at all.
**Verdict:** CONFIRMED on both halves. (1) Unmeasured: the new ban is crates/sim/clippy.toml:10; the "Empirically checked… (SEAL-1)" line at crates/sim/clippy.toml:4-5 is byte-identical boilerplate shared by six files (crates/{core,wire,harness,node,connection-plane,sim}/clippy.toml:1-5), five of which list only std clock/hasher/socket paths — it is a control for the SEAL-1 std bans, not for a path added later. The same batch shows what a recorded control looks like: tests/tests/crate_isolation.rs:133-135 "OBSERVED FAILING … failed with the message below (2026-08-14), and passed again once the edge was removed." Nothing equivalent exists for publish. (2) Scope: the entry exists only in crates/sim/clippy.toml — crates/node/clippy.toml:6-10 lacks it, and crates/bins/clippy.toml, tests/clippy.toml and a workspace-root clippy.toml do not exist (corroborated by scripts/dormant_world_simulation_design.md:600). The write API is open end to end: crates/core/src/placement.rs:168 `pub fn publish`, crates/sim/src/lib.rs:34 `pub mod stub;`, crates/sim/src/stub.rs:946 `pub struct Placements(pub PlacementLedger)`. Reach is an existing ordinary pattern, not theoretical: crates/bins/src/bin/shard.rs:301 and tests/src/lib.rs:297,751,817 already call `.resource_mut::<vd_sim::stub::…>()`, so `resource_mut::<vd_sim::stub::Placements>().0.publish(..)` from vd-bins/vd-tests draws no diagnostic under `just clippy` (justfile:63). NOT already handled: tests/tests/crate_isolation.rs enforces only the vd-physics crate edge; no per-crate ban-parity gate exists (scripts/player_driven_economy_comparison.md:437); and docs/design/DEFERRED.md:4010-4011 (D-PLACE-4) repeats the overstatement "the publish mint is clippy-banned outside the one writer" instead of ledgering the gap. Two trims that do not break it: the aside "an unresolvable path is a silent no-op" is itself unverified for the pinned clippy (rust-toolchain.toml channel 1.94.1) and the path is almost certainly resolvable, being the same crate::module::Type::method shape as the working std::time::Instant::now on line 12 — the load-bearing "unmeasured" claim stands regardless; and clippy.toml's own reason text is honestly sim-scoped ("every other sim system"), the unscoped "ONE writer" claim living in crates/sim/src/stub.rs:939-941 and D-PLACE-4. MINOR severity is right: every publish call site is inside vd-sim (crates/sim/src/stub.rs:979 writer plus test twins 12760/12779/12793/15741/16175/16501, each with #[allow(clippy::disallowed_methods)]), and Placements appears in no crate outside vd-sim — a gate-honesty/defence-depth gap, not a live defect.

## [MINOR] law-conformance: Test honesty — the movers-only deletion left a realm-feed test whose name and comment assert a property it no longer measures
**Where:** crates/sim/src/stub.rs:12674
`fn authored_realm_snaps_computes_a_moving_child_pose_in_the_own_frame_and_is_empty_when_static()` still ends with:

    // A static forest (no moving roster) authors NO realm snap — the byte-identity case.
    let bare = RealmRegions::new(vec![root_region(), own_region()]);
    assert!(bare.authored_realm_snaps(...).is_empty());

After D-FO-7 ("the feed ships EVERY direct child's authored row per tick, static and moving alike") that statement is false: `bare` is empty because its forest has NO direct child, not because the children are static. Add a static direct child to `bare` and the assertion fails. The name and comment now assert the deleted behaviour, so a future re-introduction of a movers-only filter would not turn this test red.
**Verdict:** CONFIRMED — not refuted. (1) crates/sim/src/stub.rs:1276 `authored_realm_snaps` maps `child_rows` with no filter; its doc at :1261 states "one per child, static and moving alike (… D-FO-7)". `direct_children` (:1339/:1348) keeps every region with `parent == Some(own_realm)`, and `placement_row` (:1367) has an explicit `None` arm that gives a STATIC child a row from `r.center`. So the feed does ship static children. (2) In the test at crates/sim/src/stub.rs:12674, `bare` (:12702) = `RealmRegions::new(vec![root_region(), own_region()])`; `root_region()` (:15221) has `parent: None` and `own_region()` (:15227) has `parent: Some(ROOT_REALM)` — neither is a direct child of OWN_REALM, and `RealmRegions::new` (:1071) drops nothing, so the `is_empty()` at :12703 passes because the forest is CHILDLESS, not because its children are static. Adding `child_region()` (:15233, `parent: Some(OWN_REALM)`) with no moving roster yields one row and the assertion fails — exactly as the finding predicts (code-read inference; cargo is unavailable in this read-only checkout, so the failure is UNMEASURED as an executed run). (3) The staleness came from this uncommitted batch: `git show HEAD:crates/sim/src/stub.rs` has `.filter(|(r, _)| self.moving.contains_key(&r.realm))` inside `authored_realm_snaps` plus the "movers-only … byte-identical" comment, and under that code the assertion genuinely measured the property; the diff hunk `@@ -12449,9 +12699,9 @@` rewrote only the call to the new `author_book` signature, leaving the function name (:12674) and the comment (:12701) asserting the deleted behaviour. (4) The property is not guarded elsewhere: `child_placements_unifies_movers_and_static` (:19419) and `a_static_child_placement_carries_its_whole_cell_anchored_center` (:19442) exercise `child_placements`, the layer BELOW the filter's former site, so re-adding the movers-only filter inside `authored_realm_snaps` would leave them green; the other `authored_realm_snaps` tests (:12710, :13203) and the emit gate at :7105 are all reached only through moving fixtures. Severity MINOR is right — DEFERRED.md:3818 and the production doc comment already state the correct behaviour, so this is a test name/comment honesty defect (crates/sim/src/stub.rs:12674 and :12701-12706), not a behavioural one.

## [MINOR] law-conformance: SL5 — D-WORLD-5 attributes the surviving walk-forest callers to tests, but the shipped gateway library's Default still builds a second complete world
**Where:** crates/connection-plane/src/gateway.rs:162
`impl Default for SeedInjectorConfig` — production library code in a Tier-A crate, outside any `#[cfg(test)]` — reads:

    let world = WorldView::hand_placed(&UniverseConfig::walk_scale());

DEFERRED.md D-WORLD-5 (🟧) says the hand-placed walk forest "no longer reaches any bin … but `tests/src/lib.rs` (+ the gateway's test fixtures) still build on it", and sets its flip condition as "`grep generate_walk_forest crates/` returns nothing outside its definition". This call site is not a test fixture. The shipped gateway bin does construct every field explicitly (crates/bins/src/bin/gateway.rs:109-118), so no live cluster reaches it today — but the second world remains constructible from the shipped library, and `UniverseConfig::{walk_scale, visual_scale, walk_demand, canonical, seed_derived}` all stay `pub` beside `world()`. The SL5 sweep's own row understates what survives.
**Verdict:** NOT REFUTED — every cited line reads exactly as the finding says, and the ledger row's wording is genuinely imprecise. But the finding is a documentation-precision nit whose underlying debt is already ledgered and blocking, and it under-scopes what actually survives.

CODE CLAIM — CONFIRMED VERBATIM.
- /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/crates/connection-plane/src/gateway.rs:157-181 is `impl Default for SeedInjectorConfig`, and :162 reads `let world = WorldView::hand_placed(&UniverseConfig::walk_scale());`. It is NOT under `#[cfg(test)]`: the only `#[cfg(test)]` in the file starts at gateway.rs:3941, far below. So this is shipped library code in Tier-A vd-connection-plane, exactly as claimed.
- The shipped bin does construct all five fields explicitly: crates/bins/src/bin/gateway.rs:109-118 (`world: universe` from `vd_bins::boot_world` at :105), so no live cluster reaches the Default. Line range in the finding is exact.
- The preset constructors are all `pub` beside `world()`: crates/physics/src/worldgen.rs:1136 `walk_scale`, :1201 `canonical`, :1336 `visual_scale`, :1399 `world`, :1411 `walk_demand`, :1428 `seed_derived`. Accurate.
- D-WORLD-5 reads as quoted at docs/design/DEFERRED.md:3911-3915, including "`tests/src/lib.rs` (+ the gateway's test fixtures)" and the flip condition "`grep generate_walk_forest crates/` returns nothing outside its definition". The `Default` impl is not a test fixture, so the row's attribution is an omission — the substantive part of the row ("no longer reaches any bin") stays true.

WHAT WEAKENS IT (does not overturn it).
1. The debt is ledgered OPEN (🟧) and the flip condition transitively binds this very site: gateway.rs:162 calls `WorldView::hand_placed`, whose body is `WorldView::of(generate_walk_forest(config), config)` (crates/physics/src/worldgen.rs:660-662), so `grep generate_walk_forest crates/` cannot return nothing while this call site stands. The row cannot go green with the gateway Default alive — the imprecise wording cannot launder the debt.
2. The finding singles out the LEAST exposed survivor. Two more non-test `pub` constructors of the same walk world stand in vd-physics: `WorldView::hand_placed` itself (crates/physics/src/worldgen.rs:660, doc-labelled "TEST WORLD" at :651), and `pub fn moving_children_for` (crates/physics/src/worldgen.rs:298-304) which calls `UniverseConfig::walk_scale()` + `generate_walk_forest` directly with only test callers (:1987-1988, :2989). So "the row understates what survives" is true and the finding itself understates it.
3. The substance was already written down before this row: docs/design/rehome_one_mechanism.md:1086-1087 — "`SeedInjectorConfig::default()` constructs a hand-placed walk-scale world (gateway.rs:157-176) — an SL5 foot-gun with no production caller found." So this is a re-statement of a recorded observation, not a new discovery.
4. Prior adversarial verdict on the SAME site, in the audit being reviewed: docs/audit/stage_c_full_law_audit_2026-08-14.md (MAJOR SL5 verification) concluded "The finding's attribution of this site to the test path is accurate, not overstated." That judgement was about REACHABILITY (only test callers), which the new finding concedes; it does not contradict the new finding's point about the item's `#[cfg(test)]`-free placement, so it is not a refutation.

CALLER CENSUS (confirms "no live cluster reaches it"): every `SeedInjectorConfig::default()` call is a test — gateway.rs:3989, :8769, :10840, :10905 (all below the `#[cfg(test)]` at :3941) and tests/src/lib.rs:500 in the `publish = false` vd-tests scenario crate (tests/Cargo.toml:7).

Net: real, correctly severity-rated as MINOR (arguably below it) — a wording fix to DEFERRED.md:3913 ("+ the gateway's test fixtures" → "+ the gateway library's own `SeedInjectorConfig::default()` and its test fixtures"), ideally also naming `WorldView::hand_placed` and `moving_children_for` as the remaining non-test constructors. No code defect and no green-flip risk.

## [MAJOR] gate-honesty: render_crossing_smoke: all three pixel verdicts are insensitive to the dot — the OUTSIDE one passes on an EMPTY dot region, and the dot is beyond the capture camera's far plane there
**Where:** crates/bins/tests/render_crossing_smoke.rs:497
The gate's headline is "the round trip is captured in actual pixels" (:1-6), carried by three calls to `dot_pixels_within_box_region` (:448 INSIDE, :497 OUTSIDE, :528 RETURNED). None of them can fail for the dot.

(a) NEGATIVE ASSERT IS SATISFIED BY NOTHING BEING DRAWN. `dot_pixels_within_box_region` returns `saw_dot_pixel` (crates/client-harness/src/verdict.rs:135) — a scan region with zero non-clear pixels returns `false`. The OUTSIDE assert is `!dot_pixels_within_box_region(...)` (:497-510), so "the dot drew outside the home shell" and "the dot drew nothing at all" are the same pass. That is the exact capture the middle of the round trip rests on.

(b) AND THE DOT PROVABLY CANNOT DRAW THERE (code-derived, not measured — I cannot run the GPU gate). The offscreen capture camera is `Camera3d::default()` with no `Projection` override (crates/client-render/src/lib.rs:1020); only the WINDOWED camera gets `far: STAR_FAR_PLANE` (:335-336), so the capture camera keeps Bevy's default 1000 m far. `fit_camera_to_bounds` (crates/client-harness/src/camera.rs:205-209) puts the eye `radius/sin(22.5°)*1.15` back: the drawn scene is the 150 m home shell + its five planets ⇒ union radius ≈ 259.8 m ⇒ eye ≈ 781 m from the origin, along FIT_VIEW_DIR ≈ (0.30,−0.35,−0.887). `OUTSIDE_PARK = (0,0,−2000)` (:75) is then ≈ 2717 m from the eye — past the 1000 m far plane. The analytic `CaptureCamera` used to size the rect has `FAR_PLANE = 1.0e12` (camera.rs:146), so the test places a rectangle on screen where the GPU renders nothing.

(c) THE POSITIVE ASSERTS DO NOT ISOLATE THE DOT EITHER. The verdict treats ANY non-clear pixel in the dot rect as a dot pixel (verdict.rs:120-123, no color test). `DOT_RADIUS = 0.5` m (client-render/src/lib.rs:67) at 781 m is ≈ 0.6 px radius, while the translucent, double-sided, unlit home shell (spawn_realm_box, client-render/src/lib.rs:~795) covers the image centre where the dot rect sits. Shell pixels alone satisfy INSIDE and RETURNED.

What still measures the crossing in this file is the state tier — the `location` label (:443, :476, :523), the directory CAS row on the galaxy shard (:484-488), and `expected_box(...) == None` (:491-496). The pixel tier is inert. Related: `OUTSIDE_PARK` and `RETURN_PARK` are bare literals whose "still deep inside the galaxy's own 12331 m shell" claim (:74) is never asserted from the world, unlike the sibling gate's computed bound (dual_cluster_crossing_smoke.rs:213-231).
**Verdict:** NOT REFUTED — the core claim holds, but one of its three legs (the far-plane argument) is wrong and I correct it below.

CONFIRMED (a) — the negative assert cannot fail. `dot_pixels_within_box_region` ends with `saw_dot_pixel` (crates/client-harness/src/verdict.rs:135), so an all-clear scan region returns false; and any non-clear pixel not inside the box rect early-returns false (verdict.rs:129-131). The OUTSIDE call is negated (crates/bins/tests/render_crossing_smoke.rs:497-510). Stronger than the finding states: at OUTSIDE the two rectangles are DISJOINT BY CONSTRUCTION — the file itself says the 2 km park exists so the dot's projection "clears the rect" with "~50 px of margin per axis" (render_crossing_smoke.rs:70-74). I reconstructed the camera and reproduce those numbers (home rect radius 167 px at image centre ~(642,360); dot rect ~(421,134) at ±0.7 px), so BOTH branches of the function return false and `!…` passes unconditionally, whatever the GPU drew. The OUTSIDE pixel assert carries zero pixel information.

REFUTED (b) — the far-plane mechanism is false. The capture camera is indeed `Camera3d::default()` with no `Projection` override (crates/client-render/src/lib.rs:1019-1027) and inherits `far: 1000.0` (bevy_camera-0.18.1/src/projection.rs:422). But that field clips nothing: `PerspectiveProjection::get_clip_from_view` builds `Mat4::perspective_infinite_reverse_rh(fov, aspect, near)` (projection.rs:339 — `far` unused), and CPU frustum culling explicitly skips the far plane, passing `intersect_far = false` for both the sphere and OBB tests (bevy_camera-0.18.1/src/visibility/mod.rs:667-673; semantics at primitives.rs:346-384). So the dot at ~2555 m view depth is NOT culled and does render (whether it covers a sample is a separate, unmeasured question — 0.5 m at 2555 m is ~0.17 px radius). Leg (b) should be struck; it is not what makes the assert inert.

CONFIRMED (c), and understated. The verdict has no colour test — anything not byte-equal to the corner-sampled clear counts as a dot pixel (verdict.rs:117-123), and clear is pure black (client-render/src/lib.rs:66). The home shell is translucent (BOX_ALPHA 0.25, crates/client/src/realm_scene.rs:158), double-sided and unlit (client-render/src/lib.rs:786-800), so its disc fills the image centre with non-clear pixels. On top of that the capture app never registers `despawn_reference_scaffold` (windowed schedule lib.rs:306-319 vs capture schedule lib.rs:978-989; the comment at lib.rs:357-358 says the capture path deliberately keeps it), so the ±250 m ground plate and eight landmark pillars (lib.rs:390-417) are in every capture frame over the origin region. The INSIDE dot rect sits at the image centre and RETURNED at ~(622,339) — both inside the 167 px home rect and both painted by plate+shell regardless of the dot, whose own footprint is ~0.5-0.6 px radius (DOT_RADIUS 0.5 m at the fitted eye distance ~781 m; camera.rs:205-209, FIT_VIEW_DIR camera.rs:166). Shell/plate pixels alone satisfy both positive asserts.

So all three pixel calls are insensitive to the dot, exactly as the headline says; what still measures the crossing is the state tier (render_crossing_smoke.rs:443-447, 476-488, 491-496, 523-527). The related note also holds: OUTSIDE_PARK/RETURN_PARK are bare literals (render_crossing_smoke.rs:75, 78) and nothing in the file ties them to the galaxy shell, unlike the sibling gate which computes `park_m` and asserts it against the world-read `galaxy_shell_m` (crates/bins/tests/dual_cluster_crossing_smoke.rs:213-231). MAJOR is appropriate; my geometry is code-derived (no GPU run here) but matches the file's own measured 167 px / ~50 px figures.

## [MAJOR] gate-honesty: frame_conversion_e2e's "REAL WORLD'S PROPORTIONS" constants are the PRE-S4 world, transcribed — the S4 world-number move failed nothing here and the test now runs an easier case than production
**Where:** tests/tests/frame_conversion_e2e.rs:576
This is the concrete answer to "does anything else silently depend on the old numbers". `const DEMAND_PLANET_SOI_M: f64 = 4.16;` (:576), `const DEMAND_PLANET_ORBIT_M: f64 = 17.9;` (:578), `const DEMAND_ORBIT_PERIOD_S: f64 = 25.4;` (:581) feed `demand_forest()` (:604-643) and `demand_orbit()` (:584-596), the fixture behind `entering_a_real_sized_planet_lands_inside_it_and_does_not_trade_the_player_back` (:654).

All three are the pre-S4 numbers, and the drift is exactly the S4 lever:
- planet SOI: THE world now ships 3.9541737529995578 m (crates/physics/src/worldgen.rs:3182 FROZEN_PLANET_SOI_R_M; VISUAL_SOI_GAP_FRACTION 0.35→0.372 per DEFERRED.md:3977). 4.16 is the retired 4.1607.
- inner orbit: the regenerated golden puts the home system's innermost planet at ≈16.03 m at tick 0 (crates/bins/tests/goldens/placement_rows.golden, `System(7) Planet(7701581858760374086) tick=0 pos=3feb720f07fe47b8,4030015c63e2ce0b,...`). 17.9 / 16.03 = 1.117 ≈ (1 + ecc_cap) = 1.12 — precisely the factor the apoapsis re-solve removed (ECC_SIGMA·ECC_CAP_SIGMAS = 0.12, worldgen.rs:884-891).
- period: 25.4 s is derived in its own doc comment from the 17.9 m radius, so the fixture also sweeps the planet over the waiting occupant at the wrong speed.

The test's own thesis (:565-570) is that the fixed 1.0 m entry margin against a small body is what turns an arrival error into a re-home loop. It is therefore measuring a planet 5.2 % LARGER than production ships — a strictly easier case — and nothing links these literals to the generator, so the S4 change could not have failed them. Contrast the anti-drift discipline the same suite applies elsewhere (tests/src/frame_fixture.rs:527-530, "the story's three distances are asserted against what the PRODUCTION generator actually planted"), which does not cover these constants.
**Verdict:** NOT REFUTED — every load-bearing claim verified by reading; nothing found that undermines it.

1) The constants are real, load-bearing, and untouched by the batch. tests/tests/frame_conversion_e2e.rs:576 `const DEMAND_PLANET_SOI_M: f64 = 4.16;`, :578 `DEMAND_PLANET_ORBIT_M = 17.9`, :581 `DEMAND_ORBIT_PERIOD_S = 25.4`. `git diff tests/tests/frame_conversion_e2e.rs` contains no DEMAND_* line, so the uncommitted S4 world-number move left them at their HEAD values. They feed demand_forest() (:604-643; used at :625 and :637 as the `Boundary::Shell { r }` of the system and the planet) and demand_orbit() (:584-596), the fixture behind entering_a_real_sized_planet_lands_inside_it_and_does_not_trade_the_player_back (:657). demand_forest hand-builds RealmRegions from the literals with no generator call — while the SAME file calls vd_physics::worldgen::realm_regions_for at :1511 and realm_neighbourhood_for at :740. So the S4 change structurally could not have failed this test.

2) Planet-SOI drift confirmed, and 4.16 is provably the retired number. `git show HEAD:crates/core/src/worldgen.rs` → FROZEN_PLANET_SOI_R_M = 4.160705354131045 (old :2816) and VISUAL_SOI_GAP_FRACTION = 0.35 (old :144). Working tree crates/physics/src/worldgen.rs:3182 → 3.9541737529995578, :124 → VISUAL_SOI_GAP_FRACTION = 0.372. docs/design/DEFERRED.md:3976-3977 states it in words: "planet SOI 4.1607→3.954 m with VISUAL_SOI_GAP_FRACTION 0.35→0.372". 4.16/3.9542 = 1.052, i.e. the fixture's planet is 5.2% larger — exactly as the finding says.

3) Orbit drift confirmed on the SAME planet seed and the SAME measured quantity. I hex-decoded the (untracked, regenerated) golden: crates/bins/tests/goldens/placement_rows.golden:13 `System(7) Planet(7701581858760374086) tick=0` → |pos| = 16.028420 m. The pre-S4 live cluster logged that identical seed at 17.9: docs/design/rehome_one_mechanism.md:418 "inner planet Planet(7701581858760374086) epoch_len=17.9" and :676 "placement … |p| = 17.90". Same planet, same quantity (live placement magnitude), different world numbers. 17.9/16.0284 = 1.1168 against ecc_cap = ECC_SIGMA·ECC_CAP_SIGMAS = 0.12 (crates/physics/src/worldgen.rs:882, :884, :891, applied at :1171) — the apoapsis-re-solve attribution is a plausible, consistent inference (I could not execute to prove the exact factor), but the drift itself is measured, not argued.

4) The impact framing holds. The margin really is fixed at 1.0 m: ContainmentBand::for_containment_velocity_safe(1.0, 2.0, 0.0, 1.0, 0.0) at :609; the gate asserts `from_centre < DEMAND_PLANET_SOI_M` at :705. A 4.16 m shell with a 1 m inset leaves a 3.16 m acceptance radius vs production's 2.954 m (+7%) — strictly easier, in exactly the direction the test's own thesis (:565-570) says decides the loop. The period compounds it: 25.4 s is derived in its own doc comment from the 17.9 m radius, so the sweep speed is the old world's.

5) Honesty defect is the substance and it is verbatim: :558 heads the section "THE REAL WORLD'S PROPORTIONS" and :564 asserts "The numbers below are that world's, taken from its own generator and from a live cluster run." That sentence is now false.

6) Not already handled. No DEFERRED.md row covers these constants (its only frame_conversion_e2e hit, :3391, is about EntityInterest). The Stage-C audit's nearby findings (docs/audit/stage_c_full_law_audit_2026-08-14.md:516, :526) indict the WALK-world fixtures — this section was the countermeasure to that and has now drifted itself. The contrast the finding draws is real and sharper than stated: the other surviving 17.9 sites (crates/physics/src/worldgen.rs:2743, :2801; crates/bins/tests/one_containment_answer.rs:153) are prose over assertions that derive from the_world()/realm_regions_for_config/moving_children_for_config and self-update; only frame_conversion_e2e's copies are load-bearing geometry.

Minor citation nits, no bearing on the verdict: the anti-drift contrast is at tests/src/frame_fixture.rs:529-533 (test fn at :534), not :527-530; the ecc derivation spans :882-891, not :884-891.

Honest limit: whether the gate would actually go red at production numbers is UNMEASURED — this tree is read-only with no cargo. The finding does not claim it would flip, only that it runs an easier case than production and that nothing links it to the generator; both are confirmed by reading. MAJOR severity is appropriate.

## [MAJOR] gate-honesty: render_boxes_smoke: the H2 "the shell drew WHERE it should" assert no longer isolates the shell — five nested boxes and the avatar sit inside the region it counts
**Where:** crates/bins/tests/render_boxes_smoke.rs:287
`nonclear_in_region` (:98-115) counts ANY pixel != clear inside a rectangle, with no color test, and the region is `projected_point_aabb(&camera, home_centre, radius)` for `roster.home` (:244-249) — the OUTERMOST box of the scene. The gate's own load-path assert (:229-232) states the drawn set is "the home shell + its five planets", i.e. all five planet boxes are, by construction, inside the home shell's projected rectangle, as is the avatar dot and any HUD pixel that falls there (the offscreen egui pass composites into the readback, client-render/src/lib.rs:1032-1034). So `box_pixels > 0` (:287) is satisfied by a single 3.95 m planet sphere even if the home shell's mesh or material never drew.

This is a regression introduced by the rewrite, not pre-existing: at HEAD the gate framed exactly ONE authored box (`git show HEAD:crates/bins/tests/render_boxes_smoke.rs` header, "a boot-loaded boxes.json (ONE translucent colored realm box)"), where the same counter was discriminating. The surviving anti-vacuity is the label-set equality at :229, which proves the client REPORTS drawing the shell, not that it rasterized.
**Verdict:** CONFIRMED — not refuted; the finding is real and understated. (1) The counted region is non-discriminating: nonclear_in_region (crates/bins/tests/render_boxes_smoke.rs:98-115) has no color test, and the region is projected_point_aabb over roster.home's r=150 shell (:236-249). THE world's home shell is Shell{r=150} (crates/physics/src/worldgen.rs:102) with five Shell{r=3.954} planets at sma 15.18/25.81/43.88/74.60/126.83 (worldgen.rs:3182-3190), asserted strictly inside the shell (worldgen.rs:2942). The camera is bounds_union over the drawn boxes (crates/bins/src/scene_camera.rs:53-65) => radius 259.8 m, eye ~780 m (crates/client-harness/src/camera.rs:203-206), so the home rectangle is ~334x334 px centred in a 1284x720 frame and encloses the whole system; the inner planet projects ~17 px off centre as a ~9 px disc, so one planet alone satisfies box_pixels > 0 (:287). (2) Scene contents confirmed: the emitted neighbourhood is Universe+Galaxy+System+5 planets (crates/core/src/worldgen.rs:140-156) with the two ambient shells dropped at MAX_RENDERABLE_EXTENT_M=200 (crates/client/src/realm_scene.rs:276-278) => exactly the six nested boxes. (3) No other assert isolates rasterization: DevState.realm_boxes is built from the client's own scene model (crates/client/src/net.rs:557-560), the same iteration sync_realm_boxes spawns from, so the :229 label equality proves REPORTING, not drawing; a degenerate mesh / zero-alpha material / spawn_realm_box returning None (crates/client-render/src/lib.rs:788) yields no magenta either, and render_crossing_smoke.rs:449 checks the dot, not the shell. (4) Regression confirmed: at HEAD the region was box_screen_aabb over the ONE authored Station(4242) box at x=500 (git show HEAD:crates/bins/tests/render_boxes_smoke.rs, header + :285), deliberately placed far from the origin so the scaffold fell off-screen. STRONGER THAN REPORTED: despawn_reference_scaffold is registered only in the windowed schedule (crates/client-render/src/lib.rs:313), NOT in the capture schedule (:978-987), and its own comment (:356-358) justifies that by "render_boxes_smoke checks the box, not these" — stale after the rewrite. The reference ground plate is a 500x500 m slab centred at the ORIGIN (:390-399, GROUND_HALF=250), exactly where the new scene is centred; the frame centre is the projection of a point on the slab, so the counted rectangle is filled with scaffold pixels even if NO realm box draws. The corner clear-calibration (:263-269) is unaffected (the top-right ray tilts ~+2 deg above horizontal and misses the slab), so slab pixels count as non-clear. Two minor evidence corrections, neither load-bearing: the egui-composite comment is at crates/client-render/src/lib.rs:1024-1025 (not 1032-1034), and the HUD is an egui::Area anchored LEFT_TOP+(10,10) (:860-861) which at this camera falls OUTSIDE the 475..809 x 193..527 rectangle, so the HUD strand does not apply; the avatar dot is marginal too (DOT_RADIUS=0.5 at 780 m => ~0.56 px). The planets and the kept ground scaffold are the decisive vacuity sources.

## [MINOR] gate-honesty: The placement golden was created inside the slice that moved the world's numbers, so it could not have caught S4 — and the file records no provenance
**Where:** crates/bins/tests/one_containment_answer.rs:280
`every_anchors_child_rows_match_the_golden_vector` (:280) and `crates/bins/tests/goldens/placement_rows.golden` are both NEW in the working tree: `git show HEAD:crates/bins/tests/one_containment_answer.rs` contains neither the test nor any `golden` reference, and `crates/bins/tests/goldens/` is untracked (`git status` shows `?? crates/bins/tests/goldens/`). The 57-line golden therefore records the POST-S4 numbers as its first content — it is a baseline going forward, but it cannot be evidence that the S4 change was deliberate, and any "byte-identical" claim for the placement arc itself is UNMEASURED by it.

In-file justification: absent. The doc states the rule — "Regenerate DELIBERATELY (a slice that changes the world's numbers says so) with VD_UPDATE_GOLDEN=1" (:277-278) — and the failure message repeats it (:325-327), but neither the test nor the golden records WHICH world these bits pin (seed, VISUAL_SOI_GAP_FRACTION 0.372, ECC_CAP_SIGMAS 4.0) or that this content is the S4 re-solve. The golden is 57 bare data lines with no header. The ledger DOES carry it (docs/design/DEFERRED.md:3977 "THE WORLD'S NUMBERS MOVED (owner lever 1 …)" and :3981-3983 "regenerate ONLY with a stated world-numbers change"), so the arc is honest at the ledger tier — but a future silent `VD_UPDATE_GOLDEN=1` is indistinguishable from a stated one from inside the repo's test tree.
**Verdict:** NOT REFUTED — the finding is real and every factual claim verifies, though its closing sting overstates by one step.

VERIFIED TRUE:
1. Both artifacts are new in the working tree. `git show HEAD:crates/bins/tests/one_containment_answer.rs | grep -i golden` returns nothing (HEAD file is 309 lines, last commit b719c12); the test `every_anchors_child_rows_match_the_golden_vector` is at crates/bins/tests/one_containment_answer.rs:280. `git status --porcelain` shows `?? crates/bins/tests/goldens/placement_rows.golden`, and `git ls-files | grep -i golden` shows NO tracked golden data file anywhere in the repo.
2. The world's numbers did move in this same uncommitted batch, so the golden's first content is post-S4 by construction: HEAD crates/core/src/worldgen.rs:144 = `VISUAL_SOI_GAP_FRACTION: f64 = 0.35` with no `ECC_CAP_SIGMAS`; working tree crates/physics/src/worldgen.rs:124 = `0.372` and :891 = `ECC_CAP_SIGMAS: f64 = 4.0`. The golden therefore could not have caught S4.
3. No provenance, and structurally cannot have any: the test writes `let got = lines.join("\n") + "\n";` (:314) — 57 bare data lines, no header. The rule text is at :277-278 and the failure message at :324-327 exactly as quoted; neither names the seed or the two constants. Ledger rows confirmed at docs/design/DEFERRED.md:3977 and :3981-3983.
4. No auto-regeneration foot-gun: `VD_UPDATE_GOLDEN` appears nowhere in justfile, scripts/, or CI (no .github/workflows exists), so the gate never silently rewrites itself.
5. D-PLACE-1 makes no byte-identity claim for the placement arc (it states the opposite), so the finding's conditional "any byte-identical claim ... is UNMEASURED by it" does not misattribute a claim.

ONE OVERSTATEMENT (qualification, not a refutation): the closing "a future silent VD_UPDATE_GOLDEN=1 is indistinguishable from a stated one from inside the repo's test tree" is false for world-numbers changes. A pre-existing TRACKED hand-literal pin did catch S4 and does carry in-code deliberateness: HEAD crates/core/src/worldgen.rs:2816 pinned `FROZEN_PLANET_SOI_R_M: f64 = 4.160705354131045` (with FROZEN_AU_TO_RENDER_M, FROZEN_CENTRAL_MASS_KG, FROZEN_ORBIT_SMA_M); the working tree moved all five and annotated them at crates/physics/src/worldgen.rs:3176-3178 — "RE-CAPTURED at the placement arc S4 (the apoapsis-solved compression + the 0.372 gap fraction) — THE WORLD'S NUMBERS MOVED, deliberately" — plus the full 0.35→0.372 reasoning at :114-124. Those literals are non-self-referential (no env var rewrites them). The residual concern is narrower than stated: a silent regeneration hiding a PLACEMENT-PATH-ONLY ulp change (conversion math the FROZEN_* worldgen pins do not reach) would still be invisible in the test tree. MINOR severity is correct; the fix is a provenance header emitted by the writer at :314 and/or naming the world's numbers in the doc at :277-278.

## [MINOR] gate-honesty: The apoapsis tripwire re-checks the generator's own solved constraint (with the generator's own 4 m headroom) and omits the release outset; its doc still calls it a known-red
**Where:** crates/bins/tests/one_containment_answer.rs:338
`every_planets_apoapsis_plus_its_soi_stays_inside_its_systems_shell` (:338) tests `apoapsis + soi > shell` (:369-370). That is exactly what the generator solves for, by name: VISUAL_SYSTEM_MARGIN_M = 4.0 is "Headroom (render m) between the OUTER planet's WORST-INSTANT face (apoapsis at the eccentricity cap + its SOI) and the System SOI surface — visual_au_to_render_m solves the compression so the outer body renders strictly inside its System box at EVERY point of its orbit" (crates/physics/src/worldgen.rs:103-106). And it solves at the eccentricity CAP (worldgen.rs:1170-1171), so every actual planet clears it a fortiori. The tripwire can only fire if the compression solve itself regresses — it is not an independent property.

It also tests the wrong edge for containment: containment releases at `soi + band.outset_m` (CONTAINMENT_OUTSET_M = 2.0, worldgen.rs:39; the same `release_reach` world_roster uses at crates/bins/src/lib.rs:2455), not at the bare SOI face. With 4 m of generator headroom that gap is currently harmless, but the number the test names is not the number containment uses.

Separately, its doc comment still says "Red until the world's numbers move (the compression re-solved against apoapsis)" (:335-336) — that move has landed (DEFERRED.md:3977), so the file describes a known-red test that should now be green. Same class in tests/tests/crossing_e2e.rs:286: "Red until the flush reads the CURRENT placement book (S2)", two lines after "This pins the gap at ZERO", while D-PLACE-1 records the B-1 fix as landed.
**Verdict:** CONFIRMED on all three claims; only cosmetic line drift. (1) Non-independence is real and stronger than stated: crates/physics/src/worldgen.rs:345 solves `(system_soi - margin) / (outer_axis_au*(1+ecc_cap) + soi_au)`, so outer apoapsis+soi == 150-4 == 146 exactly (VISUAL_SYSTEM_SOI_R_M=150.0 at :102, VISUAL_SYSTEM_MARGIN_M=4.0 at :106); every planet is bounded below that because ecc is `.min(config.planet.ecc_cap)` at :405 with the cap set at :1171 and sma = orbital_axis_au(n<=N-1). Both shapes are `shell(r)` spheres (:579, :587) so the test's circumscribed_extent is exactly 150 / exactly the solved SOI. The comparison at one_containment_answer.rs:369-370 is therefore structurally unable to fire unless line 345 itself regresses. Only planets are ever movers (Placement::Orbital emitted at exactly one site, worldgen.rs:588), so the loop over all anchors collapses to the one level the solve covers. (2) The doc's PREMISE is also false now, not just its verdict: one_containment_answer.rs:332-334 says the shipped boot fence cannot see this, but crates/bins/src/lib.rs:2298 hands `ChildReach::Excursion(sma*(1+ecc))`, consumed at crates/core/src/geometry.rs:1179 and run at boot from crates/bins/src/bin/shard.rs:291-294; geometry.rs:1155-1158 records the pre-cure measurement (152.57 m, 155.05 m vs a 150 m shell) in the past tense. The tripwire is dominated by, and weaker than, the shipped fence (fence uses inscribed_extent at geometry.rs:1180; test uses circumscribed_extent at :356-361). (3) Wrong-edge claim is exact, not approximate: CONTAINMENT_OUTSET_M = 2.0 at worldgen.rs:39 and BandConfig::build (worldgen.rs:1021-1029) passes v_rel=0.0, dt=1.0 so the velocity widening at crates/core/src/geometry.rs:784 is inert and the resolved release outset is exactly 2.0; world_roster (crates/bins/src/lib.rs:2427) names `release_reach = planet_soi + config.band.outset_m` at :2457 (finding said 2455). 146+2=148 < 150, so harmless today as stated. This is a different edge from the one geometry.rs:1164-1171 deliberately excuses (that note excuses the PARENT's inset, not the CHILD's release outset), so the gap is substantive. (4) Stale known-red docs confirmed at both sites: one_containment_answer.rs:335-336 still says "Red until the world's numbers move", while docs/design/DEFERRED.md:3975-3978 records "THE WORLD'S NUMBERS MOVED (owner lever 1: ECC_CAP_SIGMAS=4, compression solved against apoapsis; planet SOI 4.1607->3.954 m)"; and tests/tests/crossing_e2e.rs:287 "Red until the flush reads the CURRENT placement book (S2)" sits directly above `assert_eq!(src.flush_stamp_gap_ticks_max, 0)` at :288-290 while DEFERRED.md:3972-3974 records the B-1 fix as landed and "pinned to 0 by crossing_e2e". Line-number corrections only: crates/bins/src/lib.rs 2455 -> 2457; tests/tests/crossing_e2e.rs 286 -> 287. MINOR severity is right: no runtime defect, but a test advertising a red that is green, guarding a property its own generator makes unfalsifiable, at an edge 2 m short of the one containment uses.

## [MINOR] gate-honesty: vd_bins::flight — THE one pilot every crossing gate now flies — still sizes and reports its rendezvous against the pre-S4 planet SOI
**Where:** crates/bins/src/flight.rs:172
`rendezvous_into_planet`'s deadline panic prints "closest approach {best:.2} m vs ~4.16 m SOI." (:171-172) — the retired 4.1607 m. THE world now ships 3.9541737529995578 m (crates/physics/src/worldgen.rs:3182). That string is the one number a developer compares `best` against when leg C of node_per_realm_walk or the demand round-trips fails, so a stale SOI there mis-diagnoses exactly the near-miss case the message exists for.

Same file, same class: `SWEEP_GRACE_TICKS`'s derivation comment is "One full sweep of the planet's shell (~8.3 m at ~6.3 m/s ≈ 66 ticks)" (:76) — 8.3 m is 2 × 4.1607; the post-S4 diameter is 7.908 m. The 120-tick value has enough slack that nothing breaks, but the stated arithmetic no longer describes the world.

Both are in the module the batch-1 rework promoted to shared status ("ONE pilot for every cluster test", :1-5), so the stale numbers now reach every crossing gate.
**Verdict:** NOT REFUTED — verified against the working tree. (1) The panic string is live and stale: crates/bins/src/flight.rs:169-174 prints \"closest approach {best:.2} m vs ~4.16 m SOI.\" while THE world now ships FROZEN_PLANET_SOI_R_M = 3.9541737529995578 (crates/physics/src/worldgen.rs:3182; doc comment at :120-121 states \"solved SOI is 3.954 m\"; the change is recorded at docs/design/DEFERRED.md:3977 \"planet SOI 4.1607->3.954 m with VISUAL_SOI_GAP_FRACTION 0.35->0.372\"). The gates hitting that panic do fly that world: world_roster reads UniverseConfig::world(...) and config.planet.planet_soi_r_m (crates/bins/src/lib.rs:2455-2457), and a planet's boundary is shell(pl.planet_soi_r_m) (crates/physics/src/worldgen.rs:587). The mis-diagnosis window is real: a best in 3.95-4.16 reads as INSIDE against the printed number but is OUTSIDE the shell, and this exact message has been used that way before (docs/design/rehome_one_mechanism.md:573,583 \"reached 5.29 m from a boundary at 4.16 m\"). Two facts STRENGTHEN the finding: the acquire threshold is signed_distance <= -inset (crates/core/src/geometry.rs:820-826) with CONTAINMENT_INSET_M = 1.0 (crates/physics/src/worldgen.rs:37), so the number best must beat to enter is ~2.95 m — the printed reference was already the wrong quantity even before it went stale. (2) SWEEP_GRACE_TICKS comment confirmed stale: crates/bins/src/flight.rs:76-77 says \"~8.3 m at ~6.3 m/s ≈ 66 ticks\"; 8.3 = 2 x 4.1607, post-S4 diameter is 7.908 m; 120 ticks still has slack, as the finding itself concedes. (3) Not already handled: crates/bins/src/flight.rs is UNTRACKED (git status ??), line 172 carries 4.16 on disk, and no DEFERRED row covers it (the only 'flight' hit is DEFERRED.md:3952, about cross_leg's galaxy label). THREE FRAMING CORRECTIONS, none changing the verdict or the MINOR severity: (a) \"every crossing gate\" overstates reach — rendezvous_into_planet has 4 call sites (crates/bins/tests/node_per_realm_walk.rs:309 leg C; crates/bins/tests/rlm_demand_login.rs:651, :782, :1042); render_crossing_smoke.rs:41 imports only cross_leg (whose panic names no SOI) and dual_cluster_crossing_smoke.rs does not use the module; (b) batch-1 did not author the staleness, it broadened it — git show HEAD:crates/bins/tests/rlm_demand_login.rs carries both strings verbatim at :716 and :823, so the promotion carried a pre-existing stale comment into shared status; (c) the \"~6.3 m/s\" half was never right for either world (inner-planet circular speed ~7.7 m/s now, ~8.6 m/s pre-S4 from FROZEN_ORBIT_SMA_M[0]=15.185 and FROZEN_CENTRAL_MASS_KG=1.340795e13) — only the 8.3 m figure traces to 2 x 4.1607. ADDITIONAL SAME-CLASS INSTANCE the finding missed and worth its own row: tests/tests/frame_conversion_e2e.rs:576 `const DEMAND_PLANET_SOI_M: f64 = 4.16;` is not a message but a load-bearing live constant — it builds the fixture planet shell (:637) and is the assertion threshold at :705 (`from_centre &lt; DEMAND_PLANET_SOI_M`) — inside a fixture whose own doc (:560-575) claims to reproduce the shipped demand world, so it now proves an arrival margin against a planet 5% larger than the one the game boots.

## [MINOR] gate-honesty: node_per_realm_walk's thrash guard is one-sided: it bounds the entity fence from above only, so "one clean CAS commit per crossing" is never observed
**Where:** crates/bins/tests/node_per_realm_walk.rs:379
The gate asserts `entity_fence <= MAX_ENTITY_FENCE` (:379-385) with the message "a clean node-per-realm chain commits ONE fence per crossing". There is no lower bound and no other read of the directory for the subject anywhere in the test: `max_entity_fence` (:83-100) is called once, and `admin_directory` (:104-127) is used only in failure messages. A run in which the directory recorded ZERO entity commits satisfies the guard. The doc already discloses the upper bound as UNMEASURED (:60-61, :373-374), but the count is not merely unmeasured — it is unasserted, and the chain makes five crossings (:305-369). All crossing evidence is therefore carried by the `location` label alone (crates/bins/src/flight.rs:212-275, 296-325), which is the client's delivered `own_location_frame` (crates/client/src/view.rs:335-338). The sibling gate does check the directory for the moved row (crates/bins/tests/dual_cluster_crossing_smoke.rs:329-345); this one does not.

Second, narrower point: `Cluster::first_exited` is polled only through bring-up (:278-283). D-WORLD-7's new standing requirement is satisfied for bring-up, but a shard that dies during the flight (e.g. the sibling shard before leg E) surfaces only as "the location label never flipped" after a 60 s leg deadline, with no named dead child — the same silence D-WORLD-7 was written against, moved one phase later.
**Verdict:** NOT REFUTED — both points verified against the working tree; the finding is accurate, unhandled, and if anything understated.

POINT 1 (one-sided thrash guard) — CONFIRMED.
- /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/crates/bins/tests/node_per_realm_walk.rs:379-385 is `assert!(entity_fence <= MAX_ENTITY_FENCE, "... a clean node-per-realm chain commits ONE fence per crossing ...")`. Upper bound only; there is no `>=`, no equality, no per-crossing count anywhere.
- Exhaustive check of every assertion site in the file (:143, :287, :294, :379) plus every directory read (`admin_get_body` reached only via `max_entity_fence` :83-100 and `admin_directory` :104-127): `max_entity_fence` is called exactly once (:375) and `admin_directory` appears only inside panic/assert messages (:281, :298, :384). So no other read constrains the subject's commit count. The finding's "called once / failure-messages only" claim is exact.
- The zero-commit-passes claim is stronger than stated: `max_entity_fence` returns `0` on THREE silent paths — unreachable/non-200 admin endpoint (:84-86, backed by `admin_get_body` returning `None` on connect/read/status failure, crates/bins/src/lib.rs:1899-1912), unparseable JSON (:87-89), and no `ent-` rows (`.max().unwrap_or(0)` :96-99 / `.unwrap_or(0)` :99). A 2 s admin timeout blink at :375 therefore makes even a genuinely thrashing run pass. The guard cannot fail on infrastructure failure — it self-disables.
- The doc's disclosure (:29-31, :58-62, :371-374) covers only that the UPPER bound is UNMEASURED. It does not disclose that the count is unasserted. So "already handled by disclosure" does not hold.
- Crossing evidence really does rest on the label alone, and I confirmed the chain of custody the finding cites: `cross_leg` asserts only `loc == want` (crates/bins/src/flight.rs:221-274, esp. :259-263 and :274), `creep_into` likewise (:296-325), `rendezvous_into_planet` likewise (:169-183); none of the three ever reads the directory. The label is `DevState.location`, which is `own_location_frame().map(FrameRef::label)` (crates/client/src/render_snapshot.rs:180) off `crates/client/src/view.rs:335-338`. In fairness to the code, that label IS server-delivered and fence-validated (view.rs:330-334 doc), so it is real crossing evidence — the finding does not dispute that; its claim is narrower and correct: the fence guard adds no evidence about HOW MANY commits a crossing took.
- The sibling-gate contrast is real: crates/bins/tests/dual_cluster_crossing_smoke.rs:313, :328-341 polls and then re-asserts `dest_owns_an_entity` on the moved `ent-` row plus `dest_entity_rows >= 1` — a positive directory assertion the chain gate has no analogue of.

POINT 2 (first_exited polled only through bring-up) — CONFIRMED, correctly scoped by the finding itself.
- The only `first_exited` call in the whole tree outside the dev-cluster binary is node_per_realm_walk.rs:278-283, inside the bring-up loop; after `let _nodes = nodes;` (:302) the cluster is never polled again (grep: crates/bins/src/bin/vd-devcluster.rs:375 and crates/bins/src/lib.rs:2785 are the only other hits). `_nodes` is also bound non-`mut`, so it could not be polled as written without a change.
- docs/design/DEFERRED.md:3933-3934 words the new standing requirement as "every in-test cluster BRING-UP polls Cluster::first_exited", so the mid-flight gap is not a ledger violation — exactly as the finding concedes. But it is uncovered: a shard dying before leg E surfaces only as flight.rs:259-263's "the location label never flipped" after the 60 s LEG_DEADLINE (node_per_realm_walk.rs:57), with no dead child named — the same silence D-WORLD-7 (DEFERRED.md:3923-3934) was written against, moved one phase later.

MINOR severity is right: nothing here makes the gate green on a broken product today (five label flips still gate the fence check, and on the Chain shape every leg is cross-node so each flip does imply a directory CAS). The defect is that the assertion's stated claim — "ONE fence per crossing" — is not the assertion's content, and the guard silently degrades to a no-op on an admin blink.

## [MINOR] gate-honesty: dual_cluster_crossing_smoke never establishes the source side — any ent- row authored by the galaxy shard ends the poll
**Where:** crates/bins/tests/dual_cluster_crossing_smoke.rs:313
The proof loop breaks on `dest_owns_an_entity(&directory_rows(admin), &dest_authority)` (:313), i.e. any `ent-` key whose authority is the galaxy shard, and the final asserts (:329-345) restate the same predicate. `home_entity_rows` is computed and then deliberately discarded (`let _ = home_entity_rows;`, :352). There is no precondition that the dot ever logged in on the HOME shard, and no observation that the home shard held that row first — so a login that resolved onto the galaxy shard, or an entity row authored there for any other reason, satisfies the headline with zero crossing. The J3 flight bound (:208-231) computes where the dot is COMMANDED to park, but the gate has no dev-control client and never reads the dot's actual pose, so "provably still inside the galaxy's own shell" (:20-22) is an arithmetic claim about intended ticks, not a measurement.

This shape is carried over from HEAD (`git show HEAD:...` has the same break condition and the same discarded `source_entity_rows`), so it is inherited rather than introduced — but the rewrite changed the login geometry from "spawns inside the planted shell on the SOURCE" to "spawns at the home star's centre", which makes the missing source-side precondition load-bearing on the gateway's home resolution rather than on an injected fixture.
**Verdict:** PARTIALLY UPHELD — every cited structural fact checks out, but the finding's stated failure mechanism ("satisfies the headline with zero crossing") is REFUTED by the code and must be struck; the residual is a proof-strength / comment-vs-code gap, MINOR at most.

VERIFIED (all cites exact, /Users/maxim/Projects/my/voxeldust/.claude/worktrees/new-system/):
- crates/bins/tests/dual_cluster_crossing_smoke.rs:313 — the poll breaks on `dest_owns_an_entity(&directory_rows(admin), &dest_authority)`; :184-187 the predicate is `key.starts_with("ent-") && authority == dest_authority`, one-sided, no home-side term.
- :329-345 restate the same predicate twice (dest_owns + `dest_entity_rows >= 1`).
- :333-336 computes `home_entity_rows`, :352 discards it (`let _ = home_entity_rows;`).
- :115-131 the LoginClient decodes only `ServerControlMsg` on `MsgClass::Control`; snapshots are never decoded, so no pose is ever read. :208-231 the J3 bound is pure arithmetic (`park_m = exit_ticks * step_m` vs the galaxy shell). So the header's "provably still inside the galaxy's own shell" (:20-22) is indeed a claim about COMMANDED ticks, not a measurement.
- Inheritance claim confirmed: `git show HEAD:crates/bins/tests/dual_cluster_crossing_smoke.rs` has the same break at its :254 and the same discarded `source_entity_rows` at its :274/:292.

REFUTED (the part that gave the finding its teeth):
1. "a login that resolved onto the galaxy shard" is unreachable in `--dual`. crates/bins/src/lib.rs:1294-1296 routes Dual to the STATIC gateway env, which sets `VD_SHARD = SHARD` (lib.rs:1319) and emits NO `VD_DEMAND` (`VD_DEMAND=1` exists only in the demand builders, lib.rs:1141 and :1256). crates/bins/src/bin/gateway.rs:78/110 therefore leave `seed_injector.armed = false`, so crates/connection-plane/src/gateway.rs:348-350 `dynamic_home_mode` is false and crates/connection-plane/src/gateway.rs:454 `session_target` = `config.shard` = SHARD. The login CANNOT land on the galaxy shard.
2. "an entity row authored there for any other reason" is unreachable. The only `ent-` rows in a process cluster are player avatars minted at admit (crates/sim/src/stub.rs:2684 `mint_entity`), granted by the admitting shard (crates/sim/src/stub.rs:2331 `LeaseGrant{Entity, owner: AuthorityRef::Shard(node)}`); an ADOPTING dot never LeaseGrants (stub.rs:2317-2325, guarded by the unit test at stub.rs:11741-11748). No NPC/debris/prop entity exists. So galaxy authority on an `ent-` key is obtainable ONLY through the transfer CAS.
3. The "rewrite changed the login geometry, making the missing precondition load-bearing" worry is also closed: crates/bins/src/lib.rs:2144-2155 (`homes_from_offsets`) spawns at the home realm's OWN CENTRE with a ZERO offset and no `VD_SPAWN_POSES` in the dual env — inside the home shell by construction, not by luck.

WHAT SURVIVES (worth keeping, re-scoped):
(a) Comment-vs-code: :326-327 says the block exists "to pin that the home shard no longer holds THAT entity under its own authority (the CAS moved it, not copied)" — but :352 discards the only value that could pin it. The gate proves dest-owns, never moved-not-copied. Either assert it (with the transient-Ghost tolerance the :352 comment describes) or delete the claim from the comment.
(b) Header overstatement at :20-22 vs the standing "NEVER ASSUME — measure it" rule: nothing measures where the dot actually parked. Note the failure mode is benign — a server integrating fewer steps than commanded shortens the leg and the gate times out LOUDLY at :316-322; overshoot past `park_m` is not producible since movement zeroes at :141.
(c) Coverage of the missing properties already exists in a sibling process gate: crates/bins/tests/render_crossing_smoke.rs:444-460 asserts the session is IN the home system and the dot's pixels fall inside the home shell BEFORE the leg, then :484-487 asserts the same dest-owns row — i.e. the source side and the real pose ARE measured elsewhere in the suite, which is why this gate's weaker predicate is a redundancy gap rather than a hole.

## [MINOR] gate-honesty: rlm_demand_login still documents the deleted orbit-slowdown knob and an epoch-fixed aim that only holds at tick 0
**Where:** crates/bins/tests/rlm_demand_login.rs:731
Batch 1 deleted the orbit-slowdown knob (crates/bins/src/lib.rs:2240 "THE ORBIT-SPEED KNOB IS GONE (Stage-C, SL5)"; no `OrbitSlowdown` / `VD_TEST_ORBIT_SLOWDOWN` symbol survives anywhere — grep over crates, tests, justfile, scripts returns only comments). Two comments still describe it as live machinery: :731-732 "A large epoch-invariant orbit slowdown keeps the fly targets fixed; the knob is reset at the END (a leak would quasi-freeze a later full-speed test)" on `a_planet_to_system_return_commits_both_rehomes_and_the_player_rides`, and :1101 "(The orbit-slowdown knob clears at the `OrbitSlowdown` guard's drop — panic-safe.)" — naming a type that no longer exists.

The fly-OUT legs also still aim at the tick-0 epoch (`-epoch * 1.5` at :862 and :1045, `epoch.to_array()` at :1057, from `orbital_state(&elements, 0.0)` at :763 and :975) under the comment "FLY OUT INWARD toward the star (≈ -epoch in the planet frame)" (:850). At full orbit speed that direction is only "toward the star" at tick 0; the legs still work because any 25 m displacement leaves a 3.95 m SOI, but the stated reason is no longer the operating one. This is the §4u stale-comment class the arc committed to fixing, re-introduced in the files batch 1 rewrote.
**Verdict:** NOT REFUTED — every element verified against the uncommitted working tree, and the git diff proves the leftovers were unintentional.

HALF 1 (stale orbit-slowdown comments) — CONFIRMED. Batch 1 deleted `struct OrbitSlowdown` / `impl OrbitSlowdown::engage` / `impl Drop for OrbitSlowdown` and both `let _slowdown = OrbitSlowdown::engage();` call sites (visible in `git diff -- crates/bins/tests/rlm_demand_login.rs`). `grep -rn "OrbitSlowdown" crates/ tests/ justfile scripts/` now returns EXACTLY ONE hit — the comment itself at crates/bins/tests/rlm_demand_login.rs:1101 (`// (The orbit-slowdown knob clears at the \`OrbitSlowdown\` guard's drop — panic-safe.)`). No type, no `VD_TEST_ORBIT_SLOWDOWN` reader, no env write survives anywhere.

The diff reveals a CROSSED PAIR that strengthens the finding: for `a_planet_to_system_return_commits_both_rehomes_and_the_player_rides` (fn at :734) batch 1 DELETED its trailing "(The orbit-slowdown knob clears…)" line (hunk `@@ -1162,21 +934,19 @@`) but LEFT the doc sentence at :731 ("A large epoch-invariant orbit slowdown keeps the fly targets fixed; the knob is reset at the END (a leak would quasi-freeze a later full-speed test)"); for `repeated_planet_system_roundtrips_do_not_freeze` (fn at :946) batch 1 REWROTE the doc comment (deleted "epoch-fixed fly targets (300× orbit slowdown) … Knob reset at the end", replaced with "full orbit speed — the slowdown knob is deleted") but LEFT the trailing :1101 comment. Each test got exactly one of its two stale comments cured — a leftover, not a decision. The :731 sentence is present-tense live-machinery description and is directly contradicted 47 lines later in the same body at :778-779 ("full orbit speed; the slowdown knob is deleted"). The finding correctly does NOT cite :509 ("the freeze the human saw at full speed but not under the orbit-slowdown flag"), which is legitimate history — no overreach. justfile:253 was correctly updated; DEFERRED.md carries no covering entry.

HALF 2 (epoch-fixed fly-out aim) — CONFIRMED. :763 and :975 `let epoch = vd_physics::celestial::orbital_state(&elements, 0.0).position;`; :850 comment "── FLY OUT INWARD toward the star (≈ -epoch in the planet frame)" with the :851-853 "no sibling confound (siblings orbit farther out)" rationale; :862 `target: (-epoch * 1.5).to_array()`; :1045 `cross_to("System 7", -epoch * 1.5, "fly-out", cycle)`. At full orbit speed `-epoch` is the star direction only at tick 0 — a half-orbit later it points OUTWARD, past sibling orbits, so both the "inward" claim and the sibling rationale are inoperative. The contrast is internal to the same change: the fly-IN leg now uses the LIVE tick-aware aim (crates/bins/src/flight.rs:62-68, `planet_at(tick)` via `secs_since_epoch`), i.e. batch 1 replaced this exact pattern on the in-leg and left it on the out-leg. A THIRD instance the finding only partly covers: :1057 steers at `target: epoch.to_array()` under the comment at :1048-1049 "The steer target is the PLANET (the next cycle's fly-in)" — `epoch` is the planet's tick-0 position, not the planet.

SEVERITY MINOR IS CORRECT and the finding is not mischaracterized: behaviour is unaffected (both loops break on `location == "System 7"` and the planet SOI is metres, so any sustained displacement exits regardless of heading), and the substantive gate-honesty defect (audit stage_c_full_law_audit_2026-08-14.md:219 — both round-trips gating only at 300×) IS genuinely discharged by batch 1. What remains is documentation that understates the gate's strength at :731, names a deleted type at :1101, and states three no-longer-operating reasons at :850, :1048-1049 and the `-epoch` targets at :862/:1045 — the §4u stale-comment class, in files batch 1 rewrote.

Load-bearing citations: crates/bins/tests/rlm_demand_login.rs:731, :763, :778-779, :850-853, :862, :975, :1045, :1048-1049, :1057, :1101; crates/bins/src/flight.rs:62-68; crates/bins/src/lib.rs:2240; justfile:253; docs/design/DEFERRED.md (no covering entry).
