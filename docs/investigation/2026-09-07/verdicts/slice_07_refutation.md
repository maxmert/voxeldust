# Slice 7 refutation — the client links the recipe (2026-09-08)

The refuter (Opus 5, read-only) read the slice document, rulings V9–V12, the laws, and every changed
file, and reported 19 findings. Every one is answered here. "FIXED" means the code changed and a test
or a measurement shows it; "KEPT" means the code stands, with the reason; "OWED" names the slice that
takes it.

| # | Severity | Finding | Answer |
|---|---|---|---|
| 1 | defect | The client compiled the universe seed in (`DEV.universe_seed`), so a cluster on another `VD_UNIVERSE_SEED` refused every login with "update your build". The document's §4 diagram claimed `Welcome` carries the seed; it does not. | **FIXED** (the dev half) and **OWED** (the shipped half). The client binary now reads the seed exactly as the gateway does (`env.parse_or("VD_UNIVERSE_SEED", HOME_SEED)`) and builds its identity through the same `vd_bins::world_identity` the gateway uses; the render handles' declared tag comes from the same seed. The seed ON THE WIRE at `Welcome` is a new field on the connection plane's own lane: an ASK to the owner (§12.6), not done quietly. §4's diagram is corrected. |
| 2 | defect | The fallback work light STOOD below the observer (`-up`), so its light travelled upward and lit the undersides of hills, which back-face culling never draws; only the 8 % fill lit the ground. | **FIXED**: the light stands at `overhead` (`-centre`, the direction from the body's centre to the eye), so `dir = -star` points down. The comment states which way `centre` points. Not seen on the pictures because every picture had the star's row in the window (the `brightest` arm). |
| 3 | defect | The fill light was aimed once and never again; `TerrainFill` was a dead marker. On the night side the sun and the fill came from one side and the shadows went black. | **FIXED**: the fill's entity is kept on the `Terrain` resource and re-aimed opposite the sun every frame through the same light query. |
| 4 | defect | HR5: branches in Tier-A crates without a reaching test — `InlineWorkers::submit`'s `None` arm, `release`'s "realm still holds another chunk" arm, `len.is_nan()`, the `ChunkJob`/`ChunkReady` derives, `field_value`'s `TerrainChunksDrawn` arm. | **FIXED**: `submit` is branchless (`extend(Option::map)`); the lane test releases one of two chunks and asserts the rung is kept; a NaN point is asserted empty; the derives are exercised (`clone`, `Debug`, `PartialEq`); the wait-field test reads both terrain fields. The coverage gate is the proof (§12.5). |
| 5 | defect | `chunks_around` took the surface band from ONE height (the column's middle cell) ±1 chunk; a cliff of more than 62 cells inside a column, or a cave mouth deeper than a chunk, left a hole. | **FIXED** for slopes, **OWED** for caves: `vd_terrain::digest::surface_chunk_span` reads FIVE heights (the centre and the four corners) and returns the lowest-to-highest chunk with a chunk of margin each way, clamped to the band; tested on the golden columns and at the top rung. A cave deeper than the margin is slice 8's residency band (`D-TERRAIN-3`), as is a picture at a cave mouth (§12.4). |
| 6 | defect | A refused surface statement was re-counted and its body re-derived on every frame. | **FIXED**: the lane remembers refused realms (`refused`); a refusal counts once until the realm is forgotten. Tested. |
| 7 | weakness | `place_chunks` cloned the window scene and walked every row even with nothing drawn. | **FIXED**: it returns before the clone when no chunk entity exists. `sync_terrain` still clones once per frame while a rung is named (it must read every row's surface statement); that is the one-rung interim's cost. |
| 8 | weakness | The realm's outline is hidden whenever any chunk exists, even from outside the look shell, so the aloft picture shows black beyond the patch where the globe should be. | **KEPT**, registered: the black beyond the patch IS `D-TERRAIN-3` (one rung; slice 8's coarse rungs replace the outline). Showing the outline from just outside the shell would put a translucent blue veil over the terrain instead — the other wrong picture. The comment now says so. |
| 9 | weakness | The picture gate "settled" by sampling the drawn count twice 500 ms apart — a sleep, which its own doc forbids. | **FIXED**: the lane exposes `pending_count`, the renderer writes it to `DevState::terrain_chunks_pending`, `WaitField::TerrainChunksPending` exists, and the gate waits `le 0` after `terrain_chunks_drawn ge 1`. No sleep remains in the settle. |
| 10 | weakness | Two claims stated as MEASURED (the clock at genesis; the planet's frame equals the system's) were not measured by the gate. | **FIXED** for the clock (the login's universe tick is asserted under one day); **REWORDED** for the facing: the dev state does not carry a row's facing, so the gate states the assumption and what goes red when it breaks. Adding the facing to the dev state is a small owed item (§12.4). |
| 11 | weakness | `chunks_around` had no bound on `radius` (an overflow panic at `i32::MAX`; a frozen render thread at 1 000); a rung past the ladder gave a black screen with no line. | **FIXED**: `MAX_RADIUS = 64`, clamped inside `chunks_around` (tested at `i32::MAX` and negative) and at `TerrainConfig::from_env` with a warning; a rung past a body's ladder warns once per realm. |
| 12 | weakness | No cancellation: a released chunk was still built (4 ms, 400 KB) and thrown away at `poll`. | **FIXED**: `ChunkWorkers::cancel`; the threaded workers keep a withdrawn set a worker checks before it starts; the inline workers drop the finished chunk. `release` and `forget` withdraw. |
| 13 | weakness | `release` scanned every held chunk; the outline loop scanned every chunk per realm box. | **FIXED**: a per-realm count (`held`) makes a release a lookup; the outline loop reads a set of realms with chunks built once per frame. `resident()` (a test accessor) still filters. |
| 14 | weakness | When a planet spins and the observer does not ride it, the wanted set changes every tick. | **KEPT** by design: the ground under such an observer really moves (465 m/s at an Earth-like equator), so the columns under the eye really change. Slice 8's residency band with hysteresis sizes how much is held around the eye; the planet does not spin today. |
| 15 | weakness | A corrupt surface tag decodes to "no surface" with no counter. | **KEPT**, noted: the window lane's bag decoder treats every malformed tag as absent (skip-unknown), and the surface is decoded where the other tags are; a per-tag refusal counter is the lane's (slice 10) when the statement gets its producer. |
| 16 | note | `to_f64` took `to_bits`'s doc comment. | **FIXED**. |
| 17 | note | Fourteen worker threads started even with no rung flag. | **FIXED**: the threads start only when a rung is named; otherwise the inline workers (never asked) stand in. |
| 18 | note | The ground eye's 1.8 m clearance is measured against the recipe's height, not the drawn mesh. | **KEPT**: the mesh sits within half a cell (0.5 m) of the height along the radial (M6-4's snap error, p50 0.004 cells), so 1.8 m clears it; a seed change that does not keeps the gate red with the paint message, which is acceptable for a picture gate. |
| 19 | note | The paint classifier accepts any warm pixel; nothing ties it to a chunk. | **KEPT**: it is a smoke-level proof (chunks exist AND the lower frame is warm), strong today because nothing else in frame is warm; a projected-rectangle verdict per chunk is slice 8's pictures' when the LOD ladder makes the frame busier. |

## Checked and found sound (the refuter's list, kept for the record)

The two measured fixes hold (the inverse bend before the cell index; the eye lifted along the
avatar's own up). The placement is consistent (`Fᵀ·(0 − centre)` and `centre + F·origin` are exact
inverses; f64 before the single narrowing; the trim gate at 1e-4 m). No law violation: no pose,
velocity or placement is derived; no absolute folded; no new data crosses a realm boundary; no walk
of a realm's children. The isolation gate is real. The lane's bookkeeping is right. The world hello
goes out once after the welcome, is retried on a failed send, and a refusal is terminal. The key
light's retirement is safe for every flight without the rung flag. The spawn parser is loud on every
malformed input. The documents state what is owed.
