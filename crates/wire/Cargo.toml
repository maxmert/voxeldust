[package]
name = "vd-wire"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "THE frozen wire contract: closed channel enums, framing, postcard codecs, version negotiation, seam contracts (TransferControl / Directory API / tickets), and the closed InterShardFlow taxonomy (HR1)."

[dependencies]
vd-core = { workspace = true }
serde = { workspace = true }
postcard = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
