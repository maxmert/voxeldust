//! ★ THE FACE BEND — MOVED to the `vd-seed` leaf in the voxel foundation's slice 5 (ruling V9 S5-1),
//! because the generator must turn an address into a direction with the SAME arithmetic the grid
//! uses, and one implementation is the only way two hosts agree. Re-exported here at the path the
//! grid always used.

pub use vd_seed::bend::*;
