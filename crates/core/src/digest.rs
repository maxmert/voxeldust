//! THE ONE CONTENT DIGEST — MOVED to the `vd-seed` leaf in the voxel foundation's slice 5 (ruling V9
//! S5-1) so the generator and the core share ONE implementation; re-exported here at the path every
//! caller always used.

pub use vd_seed::digest::*;
