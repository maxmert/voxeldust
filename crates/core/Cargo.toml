[package]
name = "vd-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Pure shard-agnostic domain: ids, Fence, kind registry, TLV blobs, closed-form celestial math, band geometry. NO I/O dependencies."

[dependencies]
# THE LEAF (slice 5, ruling V9 S5-1): the hash, the digest, the face bend and the ladder live there and
# are re-exported here at their old paths.
vd-seed = { workspace = true }
serde = { workspace = true }
postcard = { workspace = true }
thiserror = { workspace = true }
glam = { workspace = true }
rstar = { workspace = true }
# The registry's identity digest (ruling V7 S3-4: SHA-256 over the in-digest columns). Already a
# workspace dependency (the connection plane's tickets); new to this crate only.
sha2 = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
