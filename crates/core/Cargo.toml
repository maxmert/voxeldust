[package]
name = "vd-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Pure shard-agnostic domain: ids, Fence, kind registry, TLV blobs, closed-form celestial math, band geometry. NO I/O dependencies."

[dependencies]
serde = { workspace = true }
postcard = { workspace = true }
thiserror = { workspace = true }
glam = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
