[package]
name = "noisebench"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "noisebench"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

# Standalone: its own workspace root, so the repo workspace never builds or lints it.
[workspace]
