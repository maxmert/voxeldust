# S5b vetted design (WalkTo/LookAt + DevState orient)

## DESIGN
{
  "devstate_change": "Add ONE Tier-A field to `DevEntityRow` (crates/devproto/src/state.rs:22), plus a symmetric orient copy in the client builder. This field is LOAD-BEARING, not optional: the serve_conn loop reads own pos+orient from the published `DevState` each tick, so LookAt cannot work without it.\n\nserde/JSON shape \u2014 add to `DevEntityRow` after `pos`:\n    /// Composited world orientation (x,y,z,w \u2014 DQuat component order). Always\n    /// finite (sanitized). The look-at driver reads the OWN row's orient as the\n    /// current facing; every row carries it (no per-row is_own flag).\n    pub orient: [f64; 4],\n\nSourcing (Reader A, exact): crates/client/src/net.rs:395-399, inside the `.map(|(entity, sub, pose)| DevEntityRow { .. })` where `pose: RenderPose` already holds a slerped, ingress-sanitized `orient: DQuat`:\n    orient: sanitize_quat(pose.orient),\nAdd a `sanitize_quat` next to `sanitize_vec3` (net.rs:448) to hold net.rs's finite-JSON invariant locally (do NOT rely on upstream sanitize alone \u2014 keep the same defensive posture as sanitize_vec3):\n    fn sanitize_quat(q: glam::DQuat) -> [f64; 4] { [sanitize_f64(q.x), sanitize_f64(q.y), sanitize_f64(q.z), sanitize_f64(q.w)] }\n\nNo wire/interp/view change (orient already flows to the build site, Reader A verdict). Consumer reconstructs `DQuat::from_xyzw(o[0],o[1],o[2],o[3])`.\n\nAlso update the two fixtures/tests that construct DevEntityRow: state.rs:99-103 `sample()` (add `orient: [0.0,0.0,0.0,1.0]`) and the state.rs:118 roundtrip assertion (assert a quat substring); and the client net.rs devstate tests (net.rs ~:1053/:1068 per Reader C) to assert own row orient equals the delivered slerped orient. This holds vd-devproto + vd-client at 100% Tier-A.",
  "helper_location": "Pure control-law math is NOT re-authored \u2014 it already exists and is 100% Tier-A covered in `crates/client-harness/src/nav.rs` (Reader C verdict). S5b reuses verbatim:\n- `vd_client_harness::nav::walk_to(own_pos: DVec3, own_orient: DQuat, target: DVec3, arrive_epsilon: f64) -> WalkStep { movement: [f32;3], arrived: bool }` (nav.rs:48)\n- `vd_client_harness::nav::look_at(own_pos: DVec3, own_orient: DQuat, target: DVec3, align_epsilon: f64) -> LookStep { look: [f32;2], aligned: bool }` (nav.rs:84)\n- `WalkStep::action() / LookStep::action() -> InputAction` (nav.rs:33/74)\nThese bottom out on `vd_core::kinematics::{movement_from_local, forward_to_yaw_pitch}` (already branchless with hoisted `clamp_unit`, kinematics.rs:96), and nav's own monomorphic helpers `clamp_step`/`wrap_pi`/`near_vertical` (nav.rs:118-130). No new helper fns are added anywhere.\n\nCRITICAL CARGO WIRING (the one non-obvious blocker, beyond all three readers): `vd-client-harness` is renderer-FREE (its only deps are vd-core, vd-devproto, glam, serde \u2014 confirmed crates/client-harness/Cargo.toml; the ~8.8GB Bevy lives in the separate vd-client-render crate). But today crates/bins/Cargo.toml:79 marks it `optional = true` and pulls it in ONLY via `render = [..., \"dep:vd-client-harness\"]` (Cargo.toml:62). The `dev-control` build does NOT have `nav` in scope. FIX: make the `dev-control` feature also enable the harness dep:\n    dev-control = [\"dep:vd-client-harness\"]\nThis adds ZERO Bevy weight to the dev-control build (harness is pure). The `render` feature keeps its own `dep:vd-client-harness` (Cargo unions them). Then `use vd_client_harness::nav::{walk_to, look_at};` under `#[cfg(feature = \"dev-control\")]`. This is the ONLY dependency-graph change; no new crate, no new lib.\n\nThe bin-side glue (per-tick reconstruct + convergence loop) lives inline in `crates/bins/src/bin/client.rs mod dev_control` as branchless Tier-B shims (see serve_conn_wiring) \u2014 kept a straight-line call into the Tier-A nav fns, NOT llvm-captured (HR5: coverage-bearing math is the nav crate; the bin arm is behavior-proven by the subprocess E2E).",
  "walkto_law": "Reuse `nav::walk_to` UNCHANGED (nav.rs:48). The law: `to_target = target - own_pos`; if `to_target.length() <= arrive_epsilon` \u2192 `{ movement:[0;3], arrived:true }`; else express the world direction in the entity's local frame `local = (own_orient.conjugate() * to_target.normalize()).normalize()` and invert the sim's axis map via `kinematics::movement_from_local(local)` \u2192 fixed unit-magnitude Move axes, `arrived:false`.\n\nWORLD-SPACE (Reader B verdict): pass the delivered `StampedPose.orient` and `pos.offset()` directly \u2014 NO `up`. Through P3 every live frame is SystemSpace (up == world +Y, frame_from_up(+Y)=identity), and the sim STORES bare `orient_from_yaw_pitch` (stub.rs:1234), so `own_orient` IS the world\u2192local rotation walk_to needs.\n\narrive_epsilon semantics \u2014 HARD CONTRACT (nav.rs:43-46, pinned by walk_to_oscillates_if_arrive_epsilon_is_below_the_step): walk_to emits FIXED unit-magnitude movement and does NOT know the sim step length (move_speed_mps\u00b7tick_dt_s), so `arrive_epsilon` MUST exceed one sim step or the dot overshoots and oscillates forever. The driver does NOT invent a taper. DEFAULT arrive_epsilon = 0.5 (matches the process_parity convergence gate's >0.5 band, process_parity.rs:253) \u2014 comfortably above one 20Hz step. vdctl exposes it with this default so an agent can't pick a sub-step tolerance by accident; document the \u2265one-step rule in the vdctl usage line.\n\nOscillation/overshoot under LAGGED delivered state (the no-prediction constraint): the ~100-150ms delivered-pose lag means the loop steers on a stale pos. Because arrive_epsilon \u2265 one step AND the loop uses a MONOTONE-progress termination (arrived flag from fresh-enough state) rather than a tight band, a lag of a few steps only widens the effective arrival band \u2014 it cannot induce sustained oscillation as long as arrive_epsilon covers (one step + max lag drift). Bump the DEFAULT to arrive_epsilon = 0.5 which already dominates a handful of sub-step lag deltas; if a caller sets it near the step, the pinned oscillation test documents the failure mode. No client prediction is introduced \u2014 the lagged pose is fed straight in.\n\nDegenerate cases: already there (`to_target.length() <= arrive_epsilon`) \u2192 zero movement, arrived. target == pos exactly \u2192 same arm (length 0 \u2264 eps). These are covered by walk_to_arrives_within_epsilon (nav.rs:207).",
  "lookat_law": "Reuse `nav::look_at` UNCHANGED (nav.rs:84). world-dir \u2192 Look delta given current orient:\n- `desired_dir = (target - own_pos).normalize()`; `current_dir = own_orient * DVec3::NEG_Z` (world-forward straight from the delivered orient \u2014 Reader B: world-space today, no up).\n- Decompose both via `kinematics::forward_to_yaw_pitch` (kinematics.rs:46).\n- SHORTEST-ROTATION in yaw: `yaw_err = wrap_pi(desired_yaw - current_yaw)` (nav.rs:123 wraps to [-\u03c0,\u03c0)); pitch_err = desired_pitch - current_pitch.\n- Per-tick clamp: `look = [clamp_step(yaw_err), clamp_step(pitch_err)]`, each clamped to \u00b1MAX_LOOK_STEP = 0.2 rad (nav.rs:13,128) \u2014 this IS the smooth turn-rate cap; the loop accumulates it (Look delta semantics).\n\nalign_epsilon AS AN ANGLE (decisive): `aligned = current_dir.angle_between(desired_dir) <= align_epsilon` (nav.rs:110) \u2014 the TRUE 3-D angle in RADIANS, not a yaw/pitch component test. This is honest at the pole. DEFAULT align_epsilon = 0.02 rad (~1.1\u00b0, matches the nav convergence tests, nav.rs:359). Expose in vdctl with this default.\n\nPitch clamp: implicit and correct \u2014 `forward_to_yaw_pitch` clamps `forward.y` into the asin domain (kinematics.rs:47), so pitch stays in [-\u03c0/2, \u03c0/2]; the accumulator can never exceed a valid pitch because it is re-derived from the (clamped) orient each tick, not free-integrated. No extra clamp needed in the driver.\n\nDegenerate cases (all pinned in nav tests):\n- target == pos: `to_target.length() <= f64::EPSILON` \u2192 `{ look:[0;2], aligned:true }` (nav.rs:86, test look_at_degenerate_target_is_aligned:260).\n- target overhead / straight up-down (gimbal pole): `near_vertical(dir)` guard (|forward.y| \u2265 0.9999, nav.rs:99 uses `|` not `||` for coverage) HOLDS yaw (yaw_err=0) and drives pitch only, so it cannot chase the degenerate atan2 or spin; `aligned` still uses the true 3-D angle so it never falsely reports aligned (nav.rs:106-110, tests look_at_at_the_pole_commands_zero_yaw...:371 and ...converges_at_the_vertical_pole...:384).\n- already facing target: yaw_err\u22480, pitch_err\u22480, angle_between\u22480 \u2264 eps \u2192 aligned, zero look (test look_at_aligned_when_already_facing_target:267).\n\nLAGGED-STATE convergence (no prediction): because look_at clamps every tick and steers on the delivered orient, a stale orient just delays the alignment by the lag; the clamped step guarantees no overshoot in the controller's own error space (drive_look_at monotone assertion, nav.rs:191-199). align_epsilon = 0.02 rad exceeds the per-tick residual so it settles.",
  "serve_conn_wiring": "Both loops go in `serve_conn` (crates/bins/src/bin/client.rs:718), NOT `dispatch_immediate` \u2014 same reason WaitUntil/Screenshot/Record are there: the per-tick poll is async and MUST cancel on socket EOF, and serve_conn owns the read half (the LineFramer). Add two new match arms in serve_conn's `match decode_request(&line)` (client.rs:725), MIRRORING the WaitUntil arm (client.rs:726-732):\n\n    Ok(DevRequest::WalkTo { target, arrive_epsilon, max_ticks }) =>\n        match drive_walk_to(&handles, &mut framer, target, arrive_epsilon, max_ticks).await {\n            Some(response) => response,\n            None => return Ok(()), // socket closed mid-drive\n        },\n    Ok(DevRequest::LookAt { target, align_epsilon, max_ticks }) =>\n        match drive_look_at(&handles, &mut framer, target, align_epsilon, max_ticks).await {\n            Some(response) => response,\n            None => return Ok(()),\n        },\n\nAnd DELETE `WalkTo`/`LookAt` from the `Unsupported` fan-in at dispatch_immediate (client.rs:786-791) \u2014 leave Screenshot/Record there when captures is None (they stay gated). Keep a defensive `WalkTo|LookAt => Error{Unsupported}` arm in dispatch_immediate ONLY as the unreachable-defensive fall-through mirroring the WaitUntil defensive arm (client.rs:779), since serve_conn now intercepts them.\n\nEach driver is a new `async fn` modeled cell-for-cell on `wait_until` (client.rs:832-864). Structure of `drive_walk_to` (drive_look_at is identical with look_at/aligned):\n\n    async fn drive_walk_to(handles, framer, target:[f64;3], arrive_epsilon:f64, max_ticks:u64) -> Option<DevResponse> {\n        let target = DVec3::from(target);\n        let start = handles.step_seq.load(Ordering::Relaxed);\n        loop {\n            let state = handles.published.load_full();                 // freshest DELIVERED state (lagged \u2014 no prediction)\n            // (a) resolve own pos+orient from the published DevState (branchless helper, below)\n            if let Some((pos, orient)) = own_pose(state.as_ref()) {     // None until own row has a delivered pose\n                let step = nav::walk_to(pos, orient, target, arrive_epsilon);\n                if step.arrived {\n                    return Some(DevResponse::State { state: state.as_ref().clone() });\n                }\n                // (b) apply the Move via the SAME gated mailbox as apply() \u2014 try_send, count a shed\n                match handles.commands.try_send(step.action()) {\n                    Ok(()) => {}\n                    Err(_) => { handles.dropped.fetch_add(1, Ordering::Relaxed); } // shed loud, keep looping\n                }\n            }\n            // (c) EOF-cancel + timeout/Closed, IDENTICAL to wait_until (client.rs:846-862)\n            let elapsed = handles.step_seq.load(Ordering::Relaxed).wrapping_sub(start);\n            if state.phase == DevPhase::Closed || elapsed >= max_ticks {\n                return Some(DevResponse::Timeout { state: state.as_ref().clone() });\n            }\n            tokio::select! {\n                _ = tokio::time::sleep(WAIT_POLL) => {}                 // reuse the 5ms WAIT_POLL (client.rs:585)\n                read = framer.fill() => { if matches!(read, Ok(true) | Err(_)) { return None; } }\n            }\n        }\n    }\n\n`own_pose(state: &DevState) -> Option<(DVec3, DQuat)>` is a tiny monomorphic bin helper: find the `entities` row whose `entity == *state.own_entity.as_ref()?`, return `(DVec3::from(row.pos), DQuat::from_xyzw(row.orient[0..4]))`. It reads the NEW orient field (devstate_change). Keep it branchless-shaped (an `iter().find(..).map(..)` chain, one `?`), Tier-B (bin) \u2014 its behavior is proven by the subprocess E2E, its underlying math by nav's Tier-A tests.\n\nDevResponse on arrive/align vs timeout: arrived/aligned \u2192 `DevResponse::State { state }` (mirrors WaitUntil's fired reply, client.rs:842); budget exhausted or Closed \u2192 `DevResponse::Timeout { state }` (mirrors client.rs:848). This is EXACTLY the WalkTo/LookAt reply contract already documented in dispatch.rs:53-66 (\"Reply: State (arrived) / Timeout\"). Both are non-mutating from the gate's view \u2014 is_mutating()==false (dispatch.rs:119) \u2014 because they decompose into ordinary Move/Look, so NO `--allow-dev-control` gate applies (the try_send goes straight to handles.commands like a Move would; NotAllowed is never returned for these).\n\nMutating-gate note: do NOT route these through `apply()` (which would call is_mutating/as_input_action and reject \u2014 WalkTo has as_input_action()==None). The driver sends `step.action()` (a real InputAction::Move/Look) DIRECTLY to handles.commands, bypassing apply's None-arm rejection. This is the whole point of the closed loop: one DevRequest \u2192 many InputActions.",
  "frame_decision": "VERDICT (Reader B, quote-backed): WORLD-SPACE, no separate `up`. Pass the delivered `StampedPose.orient` and `pos.offset()` DIRECTLY into nav::walk_to/look_at. Justification: the sim stores the bare `kinematics::orient_from_yaw_pitch(yaw,pitch)` with NO frame_from_up composition (stub.rs:1234), and every live frame through P3 is `FrameRef::SystemSpace` (star-at-origin flat space; grep for PlanetCentered/ShipLocal live spawns returns zero \u2014 shard.rs:72, saga_runtime.rs:2084 all SystemSpace). Under +Y-up, `forward_in_frame(+Y,...) == forward_from_yaw_pitch == orient\u00b7-Z` (proven by kinematics up_y_frame_is_identity, kinematics.rs:222), so world-forward = `orient * DVec3::NEG_Z` exactly. walk_to/look_at already take only (own_pos, own_orient, target) \u2014 no up parameter to thread.\n\nWHY THIS STAYS CORRECT WHEN PLANET RADIAL-UP LANDS (P5) \u2014 do NOT corner us: the correctness INVARIANT the driver relies on is \"the delivered StampedPose.orient is a FULL WORLD-SPACE rotation whose forward is orient\u00b7-Z.\" That invariant is a property of the SIM, not the driver. Reader B's caveat: today orient is world-space only BECAUSE up==+Y makes bare orient_from_yaw_pitch coincide with a true world rotation. When a real non-Y up lands, the FIX belongs in the sim's `orient_from_angles` (stub.rs:1234) \u2014 it will compose `frame_from_up(up)` so the STORED orient remains a true world rotation. The moment the sim does that (the ONE place, HR3), walk_to/look_at continue to work UNCHANGED because they still read `orient * -Z` as world-forward and `orient.conjugate()` as world\u2192local \u2014 both remain valid for ANY world-space orient. So the S5b driver is written against the durable invariant (orient is world-space), not against the +Y coincidence. The `forward_in_frame(up,...)` seam (kinematics.rs:68) stays INERT and is deliberately NOT wired into S5b \u2014 wiring it now would DOUBLE-apply the frame once the sim starts composing it. The safe default is: consume orient as world-space; let the sim own the up-composition when P5 arrives. Zero corner: no driver change is owed at P5.",
  "test_plan": "THREE tiers, matching the coverage/behavior split (Reader C):\n\n(1) Tier-A UNIT \u2014 the pure math (100% region+branch, already satisfied; EXTEND for the new field only):\n  - crates/client-harness/src/nav.rs `mod tests` \u2014 NO new tests owed; walk_to/look_at convergence, oscillation-below-step, pole-no-spin, budget-exhaustion are already exhaustive (nav.rs:206-442). Confirm still green.\n  - crates/devproto/src/state.rs `mod tests` \u2014 EXTEND: add `orient` to `sample()` (:99) and assert it roundtrips in devstate_roundtrips_through_json (:118) \u2014 e.g. assert the JSON contains the quat and `back == state`. Holds vd-devproto at 100%.\n  - crates/client/src/net.rs devstate tests (~:1053/:1068 per Reader C) \u2014 EXTEND: assert the own entity's row `orient` equals the delivered slerped orient (feed a known StampedPose.orient through the mock, sample devstate, assert the row's [x,y,z,w]). Add a sanitize_quat unit test (non-finite \u2192 0.0) to cover the new helper. Holds vd-client at 100%.\n\n(2) CLIENT-LEVEL LOOP test (Tier-A, in nav or a new bin unit is NOT possible \u2014 bin has no #[cfg(test)]): the closed-loop convergence is ALREADY a Tier-A harness inside nav.rs (drive_walk_to/drive_look_at, nav.rs:145-204) that steps the controller exactly as stub::integrate does. No new client-level loop crate is needed; this IS the loop test. If you want the own_pose reconstruction covered at Tier-A, add a `DQuat::from_xyzw(row.orient) round-trips the sampled orient` assertion in the net.rs devstate test (proves the [f64;4] \u2192 DQuat path the driver uses is faithful).\n\n(3) E2E SUBPROCESS leg (Tier-B, behavior/wiring proof, NOT llvm-captured) \u2014 extend crates/bins/tests/process_parity.rs (the existing move\u2192pos-advance leg, process_parity.rs:147/:253):\n  - Add a sibling test `p1_walk_to_converges_over_dev_control` that: spawns CARGO_BIN_EXE_client with `--features dev-control` (like render_smoke.rs:76 drives the TcpStream), connects a raw TcpStream to the dev-control listener, sends a JSON-line `{\"cmd\":\"walk_to\",\"target\":[X,Y,Z],\"arrive_epsilon\":0.5,\"max_ticks\":400}`, reads the single response line, asserts it is `{\"resp\":\"state\",...}` (arrived, NOT timeout) and that the own entity row's `pos` distance to target \u2264 0.5 while an idle client stays at DVec3::ZERO (mirrors the existing gate, process_parity.rs:253/:293).\n  - Add `p1_look_at_aligns_over_dev_control`: send `{\"cmd\":\"look_at\",\"target\":[...],\"align_epsilon\":0.02,\"max_ticks\":200}`, assert `resp:\"state\"` and that the own row's `orient * -Z` angle-to-target \u2264 0.02 rad (reconstruct DQuat::from_xyzw from the new orient field \u2014 this is ALSO the E2E proof the orient field is wired end-to-end).\n  - A negative leg: an unreachable target with a tiny max_ticks returns `{\"resp\":\"timeout\",...}` (proves the budget bound, mirrors nav.rs:395).\n  These live in process_parity.rs (or a new sibling crates/bins/tests/dev_control_nav.rs) and are the wiring proof. They are coverage-EXEMPT (subprocess, Tier-B) \u2014 role is behavior, not coverage.\n\nFROZEN-WIRE / no-prediction assertions to bake into the E2E: the walk-to leg must confirm the client sends ordinary InputDatagram Move (sent_input_count rises) and NO new wire message type appears; the loop steers on delivered state only (no local pose extrapolation) \u2014 assert convergence is monotone (distance never increases across polled states).",
  "risks": [
    "COVERAGE \u2014 the new bin loop arms are NOT llvm-captured (subprocess Tier-B), so keep them BRANCHLESS shims: the driver bodies must contain no un-exercised match arms beyond the WaitUntil-mirror shape, and ALL real math stays in nav (Tier-A). Mitigation: the `own_pose` helper is a single find().map() chain with one `?`; the drive_* fns copy wait_until's exact control structure (already the established Tier-B pattern). Do NOT add novel branching in the bin.",
    "LAGGED DELIVERED STATE (no client prediction, HR) \u2014 the loop steers on a ~100-150ms-stale pose/orient. If arrive_epsilon/align_epsilon are set below (one sim step + max lag drift) the controller oscillates/never settles. Mitigation: default arrive_epsilon=0.5 (>> one 20Hz step) and align_epsilon=0.02 rad; document the \u2265one-step contract in the vdctl usage line (it is already pinned by nav.rs:328 walk_to_oscillates_if_arrive_epsilon_is_below_the_step). Never introduce extrapolation to compensate \u2014 feed the lagged pose straight in.",
    "CARGO FEATURE GRAPH \u2014 `nav` lives in vd-client-harness which is optional and today ONLY reachable via `render` (pulling ~8.8GB Bevy is NOT wanted for dev-control). Mitigation: add `dep:vd-client-harness` to the `dev-control` feature (it is renderer-FREE \u2014 deps are only vd-core/vd-devproto/glam/serde, confirmed); this adds zero Bevy weight. Verify `cargo build -p vd-bins --features dev-control` stays Bevy-free (no vd-client-render in the tree).",
    "FROZEN-WIRE COMPLIANCE \u2014 StampedPose.orient already rides the wire; the ONLY new serialized field is DevEntityRow.orient on the DEV-CONTROL protocol (vd-devproto, NOT the frozen gateway wire). Mitigation: confirm no vd-wire type changes; the devproto change is a versionless local JSON protocol between client and vdctl (dispatch.rs / state.rs), explicitly out of the frozen-wire contract.",
    "NO-CLIENT-PREDICTION RULE \u2014 a reviewer may flag the closed loop as 'prediction'. It is NOT: the driver reads the delivered (lagged) state and emits ordinary Move/Look inputs; the SERVER remains sole authority over pose. Mitigation: assert in the E2E that the wire carries only InputDatagram (no new message), convergence is monotone over polled delivered states, and the client never writes a locally-advanced pose into DevState.",
    "OWN-ROW ABSENCE / STARTUP RACE \u2014 before the first snapshot anchors render_cursor, entities is empty and own_pose returns None; the loop must keep polling (not error) until the own row appears, then converge. Mitigation: the `if let Some((pos,orient)) = own_pose(..)` guard skips the tick when absent; the max_ticks budget + Closed check still terminate honestly (Timeout) if the row never arrives.",
    "MAILBOX SHED UNDER LOAD \u2014 the loop try_sends a Move/Look every WAIT_POLL (5ms); a full bounded mailbox sheds. Mitigation: reuse apply()'s exact shed-loud path (dropped.fetch_add on Err, keep looping) so a shed is counted in dev_commands_dropped, never silent; the loop tolerates transient sheds and re-sends next poll (idempotent \u2014 Move is a held axis, Look accumulates so a dropped Look just slows the turn, it does not corrupt).",
    "GATE MIS-ROUTING \u2014 routing WalkTo/LookAt through apply() would reject them (as_input_action()==None \u2192 BadRequest). Mitigation: the driver sends step.action() (a real InputAction) DIRECTLY to handles.commands, bypassing apply; and is_mutating()==false so no --allow-dev-control gate applies. Keep the defensive Unsupported arm in dispatch_immediate as unreachable (serve_conn intercepts), mirroring the WaitUntil defensive arm."
  ],
  "slice_order": "[\"STEP 1 (Tier-A, independently gate-able): Add `orient: [f64;4]` to DevEntityRow (devproto/state.rs:22) + `sanitize_quat` and the orient copy in net.rs:395/448; extend devproto state.rs roundtrip test and client net.rs devstate tests. Gate: `just coverage-fast` stays 100% for vd-devproto + vd-client; `cargo test -p vd-devproto -p vd-client` green. This lands the LookAt prerequisite with zero bin change.\",\"STEP 2 (Cargo, independently buildable): Add `dep:vd-client-harness` to the `dev-control` feature (bins/Cargo.toml:58). Gate: `cargo build -p vd-bins --features dev-control` succeeds AND is Bevy-free (`cargo tree -p vd-bins --features dev-control | grep -c bevy` == 0).\",\"STEP 3 (Tier-B bin, gate-able by the E2E): In client.rs mod dev_control add the `own_pose` helper + `drive_walk_to`/`drive_look_at` async fns (wait_until-mirror), add the two serve_conn arms, and remove WalkTo/LookAt from the dispatch_immediate Unsupported fan-in (keep a defensive unreachable arm). Gate: `cargo build -p vd-bins --features dev-control` + `cargo clippy -D warnings`.\",\"STEP 4 (vdctl CLI, gate-able by parse tests): Add `walk_to`/`look_at` subcommands to vdctl parse_command (vdctl.rs:84) building DevRequest::WalkTo/LookAt with 3 floats + optional epsilon/max_ticks (defaults 0.5/0.02, 400/200); update the usage string (vdctl.rs:82). Gate: a vdctl parse unit test (if present) / manual `vdctl walk_to 1 2 3`.\",\"STEP 5 (E2E, the wiring proof): Add p1_walk_to_converges_over_dev_control + p1_look_at_aligns_over_dev_control (+ a timeout negative leg) to process_parity.rs (or sibling dev_control_nav.rs). Gate: `cargo test -p vd-bins --features dev-control --test process_parity` (or the sibling) green \u2014 asserts own-pose convergence to target, own-facing alignment via the new orient field, idle stays put, and timeout on unreachable.\",\"STEP 6 (full gate): `just gate` \u2014 fmt + clippy(-D) + tests + coverage all green; confirm no vd-wire change in `git diff` and DEFERRED.md S5b entry flips to done.\"]"
}

## ADVERSARY
{
  "verdict": "SOUND-WITH-FIXES \u2014 the reused nav math is correct and the frozen-wire/frame/HR5 reasoning holds, but the bin-side loop cadence introduces a real look_at turn-rate multiplication bug that breaks the convergence proof; fix it before implementing.",
  "defects": [
    "CRITICAL \u2014 look_at turn-rate MULTIPLICATION (oscillation under lag). The driver polls every WAIT_POLL=5ms (client.rs:585) and try_sends step.action() each poll, but the sim assembles input at 20Hz=50ms (DEFAULT_STEP_HZ, client.rs:61) and drains ALL queued commands at the top of each tick (client.rs:410 `while let Ok(action) = commands.try_recv()`). Look actions ACCUMULATE via `add_look` (input.rs:29 `self.look[0] += delta[0]`), consumed once per assemble. So ~10 look_at steps (each clamped to \u00b10.2 rad, nav.rs:112/128) sum into ONE datagram carrying look\u22482.0 rad. The sim then applies `dot.yaw += input.look[0]` UNBOUNDED (stub.rs:1212 \u2014 only pitch is clamp_pitch'd at stub.rs:1216, yaw has no per-tick cap). Result: ~10x the intended per-tick turn. This DEFEATS MAX_LOOK_STEP's smoothness cap AND breaks nav's convergence/monotonicity proof \u2014 nav.rs:174-204 `drive_look_at` applies exactly ONE clamped step per tick, matching the sim's one-datagram-per-tick model; the real loop applies ~10 per tick. A 10x yaw jump overshoots the target; because the delivered orient lags ~2-3 ticks (100-150ms), the controller reverses on the next fresh state and OSCILLATES around alignment rather than converging. align_epsilon does not save it (the error is in the yaw COMMAND magnitude, not the arrival band).",
    "walk_to is SAFE from the multiplication bug: Move = set_movement (input.rs:19, idempotent REPLACE), so ~10 identical Moves per tick collapse to one datagram. Re-sending the same Move across the 2-3 ticks a delivered pose stays stale is correct (held axis). Only look_at (accumulating add_look) is affected \u2014 the design's risk register never distinguishes these.",
    "sanitize_quat produces a NON-ROTATION on all-NaN input, diverging from the wire invariant. The proposed `sanitize_quat(q) = [sanitize_f64(q.x)..q.w]` (component-wise) maps [NaN,NaN,NaN,NaN] \u2192 [0,0,0,0], a ZERO quaternion (length 0) \u2014 NOT a valid rotation. `DQuat::from_xyzw(0,0,0,0)` in the consumer yields garbage for `orient*-Z` and `orient.conjugate()`. This DIVERGES from `StampedPose::sanitized` (pose.rs:183-187), which maps non-finite orient \u2192 DQuat::IDENTITY (a valid rotation). In practice the input to sanitize_quat is the already-ingress-sanitized + slerped RenderPose.orient (finite), so the zero-quat path is unreachable \u2014 but the proposed `sanitize_quat` unit test (\u2018non-finite \u2192 0.0\u2019) would DOCUMENT emitting a non-rotation, and the client-side defensive posture no longer matches the wire's own guarantee.",
    "E2E template citations are imprecise/wrong. process_parity.rs (cited as the extension site) drives IN-PROCESS `ProcessClient` mesh handles (process_parity.rs:36/231) \u2014 it spawns orchestrator/gateway/shard bins but NOT the vd-client bin, and has no dev-control TCP listener. render_smoke.rs:76 (cited as the TcpStream driver) is not the right pattern either. The ACTUAL correct template is client_load.rs (#![cfg(feature=\"dev-control\")], spawns CARGO_BIN_EXE_client with --dev-control, drives via the shared `vd_bins::dev_roundtrip`, lib.rs:763). The design's own sibling-crate suggestion (dev_control_nav.rs) is right; the process_parity-extension option is not feasible as written.",
    "Cargo comment drift (HR6 doc-honesty). bins/Cargo.toml:77-78 states vd-client-harness is \u2018Pulled in only with render\u2019 \u2014 after adding `dev-control = [\"dep:vd-client-harness\"]` this comment is false. Must be updated or it silently misleads (the project treats stale prose as a drift hazard, per kinematics.rs:7-11 precedent).",
    "MINOR \u2014 the design bumps arrive_epsilon DEFAULT to 0.5 and cites process_parity.rs:253 as a >0.5 band; verified that gate uses `distance(DVec3::ZERO) > 0.5` (process_parity.rs:259), so 0.5 is exactly at the boundary. A walk_to E2E asserting `distance \u2264 0.5` on arrival while an idle client is at ZERO is consistent, but arrive_epsilon=0.5 means the walker may STOP at up to 0.5 from target \u2014 the E2E must assert `\u2264 0.5` (not `< 0.5`) and choose a target far enough that `> 0.5` unambiguously distinguishes moved-from-idle."
  ],
  "must_fix": [
    "FIX THE LOOK_AT (and, defensively, WALK_TO) CADENCE: gate the driver to emit at most ONE action per SIM STEP, not one per 5ms poll. Read handles.step_seq at loop top; after a send, wait until step_seq ADVANCES before computing/sending the next step (step_seq.fetch_add is once-per-tick, client.rs:438). Concretely: track `last_sent_seq`; only `nav::look_at(...).action()` + try_send when `current_seq != last_sent_seq`, else just poll/select. This makes the real loop apply exactly one clamped \u00b10.2 rad Look per assembled datagram \u2014 matching nav.rs:174-204 drive_look_at's model \u2014 restoring the convergence/monotonicity guarantee. Without this, look_at oscillates under lag. (Walk_to is idempotent so unaffected, but the same gate keeps the two drivers symmetric and avoids spamming the mailbox \u2192 fewer spurious dev_commands_dropped.)",
    "Make sanitize_quat preserve a VALID rotation, matching StampedPose::sanitized: `fn sanitize_quat(q: DQuat) -> [f64;4] { if q.is_finite() { [q.x,q.y,q.z,q.w] } else { [0.0,0.0,0.0,1.0] } }` (identity on non-finite, not component-wise zero). Keep the whole-quat `is_finite` check in a monomorphic helper so HR5 covers both arms with one finite and one non-finite input; update the proposed unit test to assert the non-finite case yields identity [0,0,0,1], NOT [0,0,0,0].",
    "Retarget the E2E to the correct template: extend crates/bins/tests/client_load.rs (or a sibling gated `#![cfg(feature=\"dev-control\")]`), spawn CARGO_BIN_EXE_client with --dev-control/--allow-dev-control, and drive one blocking `vd_bins::dev_roundtrip` per closed loop (its 300s DEVCTL_READ_TIMEOUT, lib.rs:752, comfortably covers a ~20s max_ticks:400 drive). Drop the process_parity.rs / render_smoke.rs citations.",
    "Update bins/Cargo.toml:77-78 comment when adding `dev-control = [\"dep:vd-client-harness\"]` so it no longer claims the harness is render-only."
  ],
  "coverage_traps": [
    "No per-monomorphization trap: nav::walk_to/look_at and sanitize_quat take CONCRETE types (DVec3/DQuat/f64), not generics \u2014 the HR5 \u2018llvm counts regions PER MONOMORPHIZATION\u2019 gotcha (CLAUDE.md HR5) does not bite. Keep it that way: do not make the new helpers generic.",
    "Cross-binary instantiation is a non-issue: vd-bins is NOT in the tier_a set (justfile:12 lists vd-client-harness but not vd-bins), so the dev-control test binary linking nav.rs is coverage-EXEMPT; nav's 100% is measured only in its own vd-client-harness test run under coverage-fast. The bin drive_* fns and own_pose helper are Tier-B/subprocess-proven \u2014 correct per the design, provided they stay branchless shims (own_pose = one find().map() with a single `?`; drive_* copy wait_until's exact structure at client.rs:832-864). AVOIDANCE: add NO novel match/if beyond the WaitUntil-mirror, or the uncovered arm surfaces if vd-bins is ever added to Tier-A.",
    "sanitize_quat false-arm trap: the corrected `if q.is_finite() {..} else {..}` has TWO arms; both must be hit in vd-client's own tests (Tier-A). Feed one finite quat (roundtrips) and one non-finite (\u2192 identity). If you keep the component-wise sanitize_f64 form instead, sanitize_f64's else-branch is ALREADY covered via sanitize_vec3, but you inherit the zero-quat defect \u2014 the identity-preserving form is both safer and still fully coverable.",
    "DevEntityRow serde trap: adding `orient: [f64;4]` requires updating BOTH construction sites \u2014 state.rs:99 sample() and net.rs:395 \u2014 AND the roundtrip assertion at state.rs:118 (assert the quat substring + `back == state`). Miss either and either a test fails to compile or the PartialEq roundtrip silently under-asserts. There is no third literal site (grep confirms only these two).",
    "E2E leg is coverage-EXEMPT (subprocess, Tier-B) by design \u2014 do NOT try to make it count toward the 100% gate; its role is behavior/wiring proof (own-pose convergence, orient wired end-to-end via reconstructing DQuat::from_xyzw, timeout on unreachable), asserted through DevState, not llvm regions."
  ],
  "endorse": "After the four must-fix items \u2014 chiefly the once-per-sim-step send gate for look_at \u2014 the design is ready to implement: the reused nav math (nav.rs) and kinematics convention (kinematics.rs) are correct and 100% Tier-A already; the frozen-wire claim is verified (DevEntityRow lives in vd-devproto, not vd-wire; StampedPose.orient is vd-core and already rides the wire); the world-space/no-`up` frame decision is sound and provably non-cornering at P5 (all live spawns are FrameRef::SystemSpace, and the fix lands in the sim's one orient_from_angles, keeping orient a true world rotation the driver reads unchanged); and no-client-prediction holds (the loop emits ordinary Move/Look on lagged delivered state, server stays authority). The ONE thing that would most improve robustness: pin the cadence contract with a NEW Tier-A nav-level test that drives look_at at N controller-steps-per-applied-tick (N>1, accumulating like add_look) and asserts it still converges without the yaw-overshoot oscillation \u2014 turning the 5ms-poll-vs-50ms-tick hazard into a regression-guarded invariant instead of a reviewer's catch."
}