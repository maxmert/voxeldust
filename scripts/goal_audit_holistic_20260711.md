# Holistic /goal audit — post cloud-base (HEAD f5d8f27, 2026-07-11)

## VERDICT: DONE_NO_CRITICAL

YES — the base is AAA-sound and NOT cornered for the Star-Citizen+Minecraft end-goal: zero CONFIRMED criticals, both HIGH deploy findings downgraded (loss window is dormant in the single-shard triad), and every end-goal seam (signals/blocks/ships/planets/multi-mesh/PvP/hundreds) is structurally planted, additive, and compiler-forced — not painted into a corner.

## Dimension verdicts

interplay: SOUND (closed 17-arm taxonomy, wildcard-free classifiers force future arms, single-decode demux; one HALF-enforced HR1 send seam). DRY: SOUND (one Saga FSM w/ 2-branch HR2 fan-out, push_flow DRY-1, ShardProfile is data; one un-extracted byte-identical ghost tail + loose-param god-modules). robust: STRONGEST dimension (total fence discipline, length-capped decodes, decode-to-Default banned, fail-loud store); gaps are deploy-wiring not logic. scale: SOUND + not cornered (SCALE-1 Arc-share real, per-dest send isolation real; D-9 whole-realm broadcast + RX-mutex are ledgered walls, hundreds is correctness-proven on fabric not load-proven on QUIC). cloud: SOUND for single-triad scope (durable boot-counter, fail-loud preflight, S6 reschedule e2e real; outbox/store unwired but dormant, multi-shard needs ledgered D-32/D-34). elegant: SOUND with real debt (stub.rs 7151 lines / 17 too_many_arguments allows — maintainability not correctness). error-prone: LOW — no silent-loss/panic-on-input/decode-to-Default defect found in pure logic. AAA: SOUND (empty coverage-exemptions, TLV ban airtight, magic-numbers funneled; admin-bind ledger-vs-manifest discrepancy). feature-readiness: SOUND — Signal/BlockEdit reserved as prose + compiler-forced, capability DAG carves signal shards, block-built kinds registered, FrameRef/RealmId enumerate planet/ship/system/galaxy, compositing chokepoint correct; all owed pieces additive at verified insertion points.

## Confirmed criticals: NONE
## Confirmed highs: NONE (both downgraded)

## Top findings (survived adversarial verification)

1. MEDIUM (interplay-01, CONFIRMED): HR1 send-time invariant is HALF-enforced — push_flow_durable (sim/src/runtime.rs:92) runtime-asserts durability (ProducerLessReliable⇒Retained) but NOT carrier reliability; a SideEffecting flow can be pushed on an Unreliable MsgClass (Snapshot/Input/GhostDelta) and silently drop an authority-gating message on datagram loss with a green Tier-A suite. Grep confirms ZERO effect_class-vs-reliability check at any push site. FIX: add the symmetric debug_assert! forbidding effect_class==SideEffecting && class.reliability()==Unreliable at the same DRY choke-point. Load-bearing before P9-Signal / P6-BlockEdit / PvP-fire push sites land.
2. MEDIUM (interplay-02, CONFIRMED): DRAIN-BURST cross-config invariant is compile-time-only against the DEV const (bins/src/lib.rs:288) with NO runtime validate_drain_burst — unlike its TICK-PAIR sibling which has BOTH the const assert AND validate_tick_pair (lib.rs:269) precisely because env vars drift. A cloud ConfigMap lowering VD_OUTBOUND_CAP without touching the gateway's VD_MAX_BUFFERED_INPUTS silently sheds a conserved unreliable resume-input batch at the transfer-commit moment (input-conservation break), no boot detection. FIX: mirror validate_tick_pair with a boot-time validate_drain_burst against the env-supplied pair, fail LOUD.
3. MEDIUM (rob-01/cloud-01/aaa-02, CONFIRMED-then-DOWNGRADED to MEDIUM/LOW): the R-6d durable outbox + shard/gateway RedbStore are code-complete + proptested but UNWIRED in the live k3d cluster — zero VD_OUTBOX_PATH in any manifest, VD_STORE_PATH orch-only. Independently verified DORMANT: the cluster is a single-shard triad (all replicas:1, VD_SHARD=3) and every TransientStatus::Crossing producer feeding emit_transient_batch is #[cfg(test)] (stub.rs mod tests @2949) with no production geometric trigger until P4/P5 — so no producer-less-reliable flow is produced today; a pod SIGKILL cannot lose a batch that is never created. Correctly ledgered (DEFERRED D-6, orchestrator.rs:196 boot-warn). FIX (owed before rolling deploy, pre-P4): set VD_OUTBOX_PATH+PVC on shard/gateway, flip orchestrator spawn_mesh(None)→boot_mesh_and_replay, add a Profile::Cloud preflight assertion so the gap fails loud.
4. MEDIUM (aaa-01, CONFIRMED): the unauth /admin/snapshot (directory+leases+sagas+fences topology) + /metrics are mounted as bare get() handlers (io-prod/src/admin.rs:109-110) and bound to routable 0.0.0.0:9100 (30-orch.yaml:52) behind a self-admittedly non-enforcing flannel NetworkPolicy — contradicting the DEFERRED.md:2838 'safe only on loopback' rationale. Read-only, no mutating route, single-node-isolated today. FIX: bind 127.0.0.1:9100 (reach via kubectl port-forward, the stated intended path) OR land the D-13 admin-auth token gate before any shared/multi-tenant bind; update the stale ledger rationale.
5. MEDIUM (scale-01 D-9, CONFIRMED as ledgered wall): whole-realm snapshot broadcast has no within-realm per-entity AoI (stub.rs:2899 emits every emitting dot filtered only by authority-state, gateway fans identical body to every sub) + double-encode in partition_entities — the O(entities×clients) byte wall for dense PvP/hundreds. Ledgered P6 reshape (per-cell interest-group index re-keying SCALE-1's Arc-share from per-sub to per-cell; client SnapshotDatagram stays frozen). Correctness-proven at N≥128, byte-VOLUME is the owed pillar. Not cornered — additive internal seam.
6. MEDIUM (scale-02, CONFIRMED honesty gap): 'hundreds-in-one-location' is CORRECTNESS-proven on the loss-free in-process FaultFabric (Durability-ignoring, one Arc<Mutex> queue), NOT LOAD-proven on real QUIC where the node-wide SharedInbox RX mutex — the self-documented next serialization ceiling (mesh.rs:472) — actually lives; the QUIC load test is sender-fan-in N=16 only, process_parity runs 2 clients. FIX before first dense-PvP soak: a real-QUIC hundreds-of-clients-through-one-gateway load gate + the ledgered per-peer-inbox lock-free drain, matching the per-peer SEND isolation already shipped.
7. LOW (dry-01/interplay-05, CONFIRMED): byte-identical GhostNeighbor+GhostFlow::Spawn tail duplicated across promote_apply (stub.rs:1541, hot path) and re_home_apply (stub.rs:1655, D-37 kill-cell path only) — DRY-pinned, exercised by disjoint suites, drift-prone when GhostFlow::Spawn grows a PvP combat-state field at P11. FIX: extract register_and_spawn_source_ghost now (both call sites already pass identical args) rather than waiting for the D-39.6 grow-trigger.
8. LOW (dry-03, CONFIRMED elegance debt): pervasive loose-param threading (stub.rs 7151 lines / 12 too_many_arguments allows, on_directory_reply threads 13 refs; mesh.rs write_frame 17 params) — the opposite of modular, worsens as voxels/signals pile onto the god-module. FIX: introduce borrow-grouping structs (read-only StubTickCtx + mutable StubShardMut) before P4 decomposition; behavior-preserving.
9. LOW (mesh-01/D-41, CONFIRMED additive owed-with-phase): standing (no-transfer) cross-shard overlap has NO steady-state AuthorityChanged producer — a client subscribed to two meshes falls back to lowest-SubId (view.rs:144) and can render a stale ghost over the owned pose. Blocks persistent dual-mesh composite (walk-inside-ship-on-its-own-shard). FIX: land an owner-sub-beats-ghost steady-state producer WITH the P8 ships-on-own-shard client work (ledgered D-41, additive, view consumer already generalizes to N subs).
10. LOW (mesh-02/D-35, CONFIRMED frozen-wire GROW owed-with-P8): client ship-interior compositing is TIME-coherent but NOT version-matched (world_pos @view.rs:227 samples hull+interior at same cursor only); under datagram loss once a ship rides its own shard a passenger visibly jumps relative to its hull. Requires a NEW additive parent_version field on EntitySnap/SnapshotDatagram + hold-buffer — NOT free, must be planned into the frozen wire before P8. Chokepoint shape is correct; only the version gate + field are owed.

## Narrative

How it composes: the base is a genuinely closed contract machine. InterShardFlow is one 17-arm enum consumed through a single-decode demux (stub.rs:2534) that delegates each arm to a monomorphic handler; the two exhaustive, wildcard-free classifiers (effect_class + durability_class — verified 0 `_ =>` arms) plus nested-variant compile-time tripwires mean a future Signal/BlockEdit/Coupling arm literally cannot compile without being classified for both idempotency and durability. Transfer is ONE pure Saga step() FSM where Durable and Transient are two arms of the same machine (HR2 fan-out = exactly two branches), pushed through ONE push_flow choke-point. ShardProfile is data with a branchless bitwise satisfies — a moon is a new data value, zero feature code. This is why the un-built features are fine: the seams they will occupy are compiler-forced, additive at verified insertion points (opaque TLV state blob, additive TransitionPayload variants, reserved-in-prose Signal arm), and never require a match-on-shard-kind.\n\nGenuinely robust/DRY/scalable/cloud-ready/elegant: robustness is the strongest dimension — total fence discipline through one saturating commit point, every wire read length-caps before allocation and decodes total, decode-to-Default is structurally banned for durable kinds, the store is fail-loud-never-degrade, and the mesh rolls back its highwater on a reliable-inbox-full drop so redelivery re-drives. The cross-subsystem interplay is closed by construction. SCALE-1's Arc-share fan-out and per-dest QUIC send isolation are real optimizations, not aspirations. The cloud self-heal story (durable monotone boot-counter, fail-loud preflight vetoing every ephemeral escape, graceful SIGTERM drain, the real S6 kill-and-reschedule e2e) holds under inspection. The coordinate base (tiered-i64 LatticePos) rides the frozen wire bit-exact and is behaviour-identical to a bare DVec3 through P3.\n\nHonest gaps ledgered for later phases (NOT cornering): (1) the HR1 send-time reliability half and the DRAIN-BURST runtime cross-check are two small additive asserts owed at the same DRY choke-points that already host their siblings — close both before P6/P9 push sites accrue. (2) The durable outbox + shard/store are code-complete-but-unwired in the manifests; I independently confirmed this is DORMANT (single-shard triad + test-only crossing producer ⇒ no producer-less flow exists to lose today), correctly ledgered as a before-rolling-deploy precondition — hence both HIGH findings rightly downgraded. (3) D-9 whole-realm broadcast and the RX-mutex are the two real scale walls: hundreds is correctness-proven on the fabric but not yet load-proven on QUIC — schedule the per-cell AoI reshape (P6) and a real-QUIC crowd gate before the first dense-PvP soak. (4) The ship-interior version-match (D-35) and standing-overlap AuthorityChanged producer (D-41) are frozen-wire GROWS / new mechanisms that must land WITH P8, not retrofitted for free — the load-bearing owed pieces to put on the calendar. (5) Real elegance debt in stub.rs's loose-param god-module surface — no correctness impact but decompose before voxels/signals pile on. None of these reshapes a landed byte or forces a per-shard fork; the transfer-first strategy has, as intended, proven the base robust before features.